---
phase: 01-foundation-and-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/components/layout/Header.tsx
  - app/components/layout/Footer.tsx
  - app/components/layout/Container.tsx
  - app/layout.tsx
  - app/page.tsx
  - app/briefs/page.tsx
  - app/methodology/page.tsx
  - app/experts/page.tsx
  - app/contact/page.tsx
autonomous: false
requirements:
  - FOUN-03

must_haves:
  truths:
    - "All 5 routes (/, /briefs, /methodology, /experts, /contact) render in a browser without errors"
    - "Each page displays the shared Header and Footer"
    - "Header shows the GGHN STARR logo and 5 navigation links (Home, Briefs, Methodology, Experts, Contact)"
    - "Header shows a hamburger icon on mobile (< 768px) that opens a slide-out nav panel"
    - "Footer displays GGHN STARR branding, initiative tagline, partner acknowledgment line, contact email, and LinkedIn link"
    - "Footer does NOT repeat the main navigation links"
    - "Pages render correctly with brand colors (navy/teal) in both light and dark mode"
    - "Container max-width is 1024px (max-w-5xl) with appropriate horizontal padding"
  artifacts:
    - path: "app/components/layout/Header.tsx"
      provides: "Navigation header with mobile hamburger menu"
      contains: "'use client'"
    - path: "app/components/layout/Footer.tsx"
      provides: "Footer with branding, partners, contact"
      contains: "GGHN STARR"
    - path: "app/components/layout/Container.tsx"
      provides: "Max-width wrapper component"
      contains: "max-w-5xl"
    - path: "app/layout.tsx"
      provides: "Root layout importing Header and Footer"
      contains: "Header"
    - path: "app/page.tsx"
      provides: "Homepage skeleton"
  key_links:
    - from: "app/layout.tsx"
      to: "app/components/layout/Header.tsx"
      via: "import and render"
      pattern: "import.*Header"
    - from: "app/layout.tsx"
      to: "app/components/layout/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
    - from: "app/components/layout/Header.tsx"
      to: "lucide-react"
      via: "Menu and X icon imports"
      pattern: "from 'lucide-react'"
---

<objective>
Build the shared layout shell — Header with mobile hamburger navigation, Footer with branding and partner info, and Container wrapper — then wire all 5 page skeletons into the root layout. Human verification confirms the live site renders correctly.

Purpose: The layout shell is the visual skeleton every subsequent phase builds on. Brand colors, navigation structure, and mobile responsiveness must be correct from the start.
Output: All 5 routes live in a browser with Header/Footer, correct brand colors, and functional mobile nav.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build layout shell components (Header, Footer, Container)</name>
  <files>
    app/components/layout/Header.tsx
    app/components/layout/Footer.tsx
    app/components/layout/Container.tsx
  </files>
  <action>
    Create the `app/components/layout/` directory and three component files.

    **File 1: `app/components/layout/Container.tsx`**

    Simple centered max-width wrapper. Server Component (no 'use client' needed).

    ```tsx
    // app/components/layout/Container.tsx
    import { clsx } from 'clsx';

    interface ContainerProps {
      children: React.ReactNode;
      className?: string;
    }

    export function Container({ children, className }: ContainerProps) {
      return (
        <div className={clsx('max-w-5xl mx-auto px-4 sm:px-6', className)}>
          {children}
        </div>
      );
    }
    ```

    **File 2: `app/components/layout/Header.tsx`**

    Client Component (hamburger menu uses `useState`). The mobile menu is a slide-out panel — visible only below `md` breakpoint (768px). Desktop nav is hidden on mobile.

    LOCKED decisions to honor exactly:
    - Navigation links: Home (/), Briefs (/briefs), Methodology (/methodology), Experts (/experts), Contact (/contact)
    - Mobile: hamburger menu with slide-out nav panel
    - No Google Fonts — system fonts from globals.css apply automatically via font-serif class

    ```tsx
    // app/components/layout/Header.tsx
    'use client';

    import { useState } from 'react';
    import Link from 'next/link';
    import { Menu, X } from 'lucide-react';

    const navLinks = [
      { href: '/', label: 'Home' },
      { href: '/briefs', label: 'Briefs' },
      { href: '/methodology', label: 'Methodology' },
      { href: '/experts', label: 'Experts' },
      { href: '/contact', label: 'Contact' },
    ];

    export function Header() {
      const [isOpen, setIsOpen] = useState(false);

      return (
        <header className="bg-navy-950 text-white sticky top-0 z-50">
          <div className="max-w-5xl mx-auto px-4 sm:px-6 flex items-center justify-between h-16">
            {/* Logo */}
            <Link
              href="/"
              className="font-serif font-bold text-lg text-teal-400 hover:text-teal-300 transition-colors"
            >
              GGHN STARR
            </Link>

            {/* Desktop nav — hidden on mobile */}
            <nav className="hidden md:flex items-center gap-6" aria-label="Main navigation">
              {navLinks.map((link) => (
                <Link
                  key={link.href}
                  href={link.href}
                  className="text-slate-200 hover:text-teal-400 transition-colors text-sm font-medium"
                >
                  {link.label}
                </Link>
              ))}
            </nav>

            {/* Mobile hamburger button — visible on mobile only */}
            <button
              className="md:hidden text-slate-200 hover:text-teal-400 transition-colors p-1"
              onClick={() => setIsOpen(!isOpen)}
              aria-label={isOpen ? 'Close navigation menu' : 'Open navigation menu'}
              aria-expanded={isOpen}
              aria-controls="mobile-nav"
            >
              {isOpen ? <X size={24} /> : <Menu size={24} />}
            </button>
          </div>

          {/* Mobile slide-out nav panel */}
          {isOpen && (
            <nav
              id="mobile-nav"
              className="md:hidden bg-navy-900 border-t border-navy-800 px-4 py-4 flex flex-col gap-1"
              aria-label="Mobile navigation"
            >
              {navLinks.map((link) => (
                <Link
                  key={link.href}
                  href={link.href}
                  className="text-slate-200 hover:text-teal-400 hover:bg-navy-800 transition-colors text-base font-medium px-3 py-2 rounded"
                  onClick={() => setIsOpen(false)}
                >
                  {link.label}
                </Link>
              ))}
            </nav>
          )}
        </header>
      );
    }
    ```

    **File 3: `app/components/layout/Footer.tsx`**

    Server Component. LOCKED decisions:
    - Content: GGHN STARR branding, initiative tagline, partner acknowledgment (Fleming Fund / Africa CDC / WHO AFRO), contact email, LinkedIn link
    - Does NOT repeat main navigation links

    ```tsx
    // app/components/layout/Footer.tsx
    import Link from 'next/link';

    export function Footer() {
      return (
        <footer className="bg-navy-950 text-slate-400 border-t border-navy-800 mt-auto">
          <div className="max-w-5xl mx-auto px-4 sm:px-6 py-10">
            <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-8">

              {/* Branding block */}
              <div className="max-w-sm">
                <p className="font-serif font-bold text-teal-400 text-lg leading-tight">
                  GGHN STARR
                </p>
                <p className="text-slate-300 text-sm mt-1">
                  Africa AMR Modeling Initiative
                </p>
                <p className="text-slate-500 text-sm mt-2 italic">
                  Evidence. Advocacy. Action.
                </p>
              </div>

              {/* Partners + contact block */}
              <div className="flex flex-col gap-3">
                <p className="text-xs text-slate-500 uppercase tracking-wider font-medium">
                  Partners
                </p>
                <p className="text-sm text-slate-400">
                  Fleming Fund &middot; Africa CDC &middot; WHO AFRO
                </p>

                <div className="flex flex-col gap-1 mt-2">
                  <a
                    href="mailto:starr@gghn.org.ng"
                    className="text-sm text-teal-500 hover:text-teal-400 transition-colors"
                  >
                    starr@gghn.org.ng
                  </a>
                  <a
                    href="https://linkedin.com/company/gghn-nigeria"
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-sm text-teal-500 hover:text-teal-400 transition-colors"
                  >
                    LinkedIn
                  </a>
                </div>
              </div>
            </div>

            {/* Copyright */}
            <div className="mt-8 pt-6 border-t border-navy-800 text-xs text-slate-600 text-center">
              &copy; {new Date().getFullYear()} Georgetown Global Health Nigeria (GGHN). All rights reserved.
            </div>
          </div>
        </footer>
      );
    }
    ```
  </action>
  <verify>
    ```bash
    ls app/components/layout/Header.tsx app/components/layout/Footer.tsx app/components/layout/Container.tsx
    npx tsc --noEmit
    ```
    TypeScript must pass with no errors. All three files must exist.
  </verify>
  <done>
    Header.tsx, Footer.tsx, and Container.tsx exist in `app/components/layout/`. TypeScript compilation passes. Header has 'use client' directive and uses Menu/X from lucide-react. Footer does not contain navigation links to the 5 pages.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire layout shell into root layout and create 5 page skeletons</name>
  <files>
    app/layout.tsx
    app/page.tsx
    app/briefs/page.tsx
    app/methodology/page.tsx
    app/experts/page.tsx
    app/contact/page.tsx
  </files>
  <action>
    **Step A: Update root layout to include Header and Footer**

    Update `app/layout.tsx` to import and render the layout components. The body must use `flex flex-col min-h-screen` so the footer anchors to the bottom even on short pages.

    ```tsx
    // app/layout.tsx
    import type { Metadata } from 'next';
    import './globals.css';
    import { Header } from '@/components/layout/Header';
    import { Footer } from '@/components/layout/Footer';

    export const metadata: Metadata = {
      title: {
        default: 'GGHN STARR | AMR Policy Intelligence for Africa',
        template: '%s | GGHN STARR',
      },
      description: 'Evidence-backed AMR policy briefs for African health decision-makers.',
    };

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode;
    }) {
      return (
        <html lang="en" suppressHydrationWarning>
          <body className="bg-slate-50 dark:bg-navy-950 font-sans antialiased flex flex-col min-h-screen">
            <Header />
            <main className="flex-grow">
              {children}
            </main>
            <Footer />
          </body>
        </html>
      );
    }
    ```

    **Step B: Create 5 page skeleton files**

    Each page is a Server Component with a placeholder heading and coming-soon text. Pages use the Container component for consistent layout.

    `app/page.tsx` (Homepage):
    ```tsx
    import { Container } from '@/components/layout/Container';

    export default function HomePage() {
      return (
        <Container className="py-16">
          <h1 className="font-serif text-3xl text-navy-950 dark:text-slate-50">
            AMR Policy Intelligence for Africa
          </h1>
          <p className="text-slate-600 dark:text-slate-400 mt-4">
            Homepage content coming in Phase 2.
          </p>
        </Container>
      );
    }
    ```

    `app/briefs/page.tsx`:
    ```tsx
    import { Container } from '@/components/layout/Container';

    export const metadata = { title: 'Policy Briefs' };

    export default function BriefsPage() {
      return (
        <Container className="py-16">
          <h1 className="font-serif text-3xl text-navy-950 dark:text-slate-50">
            Policy Briefs
          </h1>
          <p className="text-slate-600 dark:text-slate-400 mt-4">
            Briefs library coming in Phase 3.
          </p>
        </Container>
      );
    }
    ```

    `app/methodology/page.tsx`:
    ```tsx
    import { Container } from '@/components/layout/Container';

    export const metadata = { title: 'Methodology' };

    export default function MethodologyPage() {
      return (
        <Container className="py-16">
          <h1 className="font-serif text-3xl text-navy-950 dark:text-slate-50">
            Methodology
          </h1>
          <p className="text-slate-600 dark:text-slate-400 mt-4">
            Methodology page coming in Phase 4.
          </p>
        </Container>
      );
    }
    ```

    `app/experts/page.tsx`:
    ```tsx
    import { Container } from '@/components/layout/Container';

    export const metadata = { title: 'Experts' };

    export default function ExpertsPage() {
      return (
        <Container className="py-16">
          <h1 className="font-serif text-3xl text-navy-950 dark:text-slate-50">
            Experts
          </h1>
          <p className="text-slate-600 dark:text-slate-400 mt-4">
            Expert profiles coming in Phase 4.
          </p>
        </Container>
      );
    }
    ```

    `app/contact/page.tsx`:
    ```tsx
    import { Container } from '@/components/layout/Container';

    export const metadata = { title: 'Contact' };

    export default function ContactPage() {
      return (
        <Container className="py-16">
          <h1 className="font-serif text-3xl text-navy-950 dark:text-slate-50">
            Contact
          </h1>
          <p className="text-slate-600 dark:text-slate-400 mt-4">
            Contact form coming in Phase 4.
          </p>
        </Container>
      );
    }
    ```

    **Step C: Build and verify**

    ```bash
    npm run build
    ```

    Must complete without errors. The `out/` directory must contain:
    - `index.html`
    - `briefs/index.html`
    - `methodology/index.html`
    - `experts/index.html`
    - `contact/index.html`

    Commit all changes:
    ```bash
    git add -A
    git commit -m "feat(01): add layout shell and 5 page skeletons"
    git push origin main
    ```

    The push to main will trigger a Vercel deployment.
  </action>
  <verify>
    1. `npm run build` exits 0
    2. `ls out/` shows `index.html`, `briefs/`, `methodology/`, `experts/`, `contact/`
    3. `grep -r "Header" app/layout.tsx` returns match
    4. `grep -r "Footer" app/layout.tsx` returns match
    5. `npx tsc --noEmit` passes
  </verify>
  <done>
    Root layout imports and renders Header and Footer. All 5 page routes exist. `npm run build` succeeds. `out/` contains all 5 page HTML files. Changes committed and pushed to main, triggering Vercel deployment.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify layout shell on live Vercel deployment</name>
  <what-built>
    Complete Phase 1 layout shell deployed to Vercel:
    - Header with GGHN STARR logo, 5 navigation links, hamburger menu on mobile
    - Footer with branding, initiative tagline, partner acknowledgment, contact email, LinkedIn
    - All 5 page routes rendering within the shared shell
    - Brand colors (Deep Navy / Medical Teal) applied throughout
  </what-built>
  <how-to-verify>
    1. Open the Vercel URL (find it at https://vercel.com/dashboard or in STATE.md).

    2. Desktop checks (browser window > 768px wide):
       - Header shows "GGHN STARR" logo in teal on dark navy background
       - 5 navigation links visible: Home, Briefs, Methodology, Experts, Contact
       - Each link navigates to the correct page (no 404s)
       - Footer shows "GGHN STARR" branding, "Evidence. Advocacy. Action." tagline
       - Footer shows "Fleming Fund · Africa CDC · WHO AFRO" partner acknowledgment
       - Footer shows contact email and LinkedIn link (no nav links)

    3. Mobile checks (resize browser to < 768px or use DevTools mobile emulation):
       - Navigation links are HIDDEN on mobile (hamburger menu replaces them)
       - Hamburger icon (three lines) is visible in the header
       - Tapping/clicking hamburger opens a slide-out panel with all 5 nav links
       - Tapping a nav link in the panel closes the panel and navigates

    4. Dark mode check (optional — no toggle button yet):
       - In browser DevTools, manually add class="dark" to the <html> element
       - Background should shift to deep navy (#0F172A)
       - Text should remain readable (light colors on dark background)

    5. `npm run dev` locally and verify all pages load without console errors
  </how-to-verify>
  <action>Human verification of the deployed layout shell. Follow the how-to-verify steps above to check desktop nav, mobile hamburger menu, footer content, and brand colors on the live Vercel URL.</action>
  <verify>All 5 pages load on the Vercel URL with no 404s; Header and Footer render on every page; mobile hamburger opens/closes correctly; footer does not show nav links</verify>
  <done>User has typed "approved" confirming all desktop and mobile visual checks pass</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After all tasks including human verification:
1. Vercel deployment is live and accessible over HTTPS
2. All 5 routes return 200 (no 404s or 500s)
3. Header renders on every page with correct nav links
4. Footer renders on every page WITHOUT repeating nav links
5. Mobile hamburger menu opens and closes correctly
6. Brand colors are visible (navy header, teal logo/accents)
7. `npm run build` produces `out/` with all 5 page HTML files
</verification>

<success_criteria>
- All 5 routes (/, /briefs, /methodology, /experts, /contact) accessible on Vercel
- Header: GGHN STARR logo (teal), 5 nav links (desktop), hamburger slide-out (mobile)
- Footer: branding, "Evidence. Advocacy. Action." tagline, partner acknowledgment, contact, NO nav links
- Container max-width 1024px on all pages
- Brand colors (navy-950 header/footer, teal-400/600 accents) applied correctly
- Human verified: approved
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-03-SUMMARY.md`
</output>
