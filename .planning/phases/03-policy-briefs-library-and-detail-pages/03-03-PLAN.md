---
phase: 03-policy-briefs-library-and-detail-pages
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - CONTENT-GUIDE.md
autonomous: false
requirements:
  - BREF-01
  - BREF-02
  - BREF-03
  - BREF-04
  - BDET-01

must_haves:
  truths:
    - "CONTENT-GUIDE.md exists at the project root and is readable by a non-technical person"
    - "The guide explains the Google Sheet column layout matching all 12 Brief interface fields"
    - "The guide explains how to upload PDFs/infographics to Google Drive and copy the shareable link"
    - "The guide explains how to run the GAS export script and paste output into briefs-index.json"
    - "The guide explains how to commit via GitHub Desktop (no terminal required)"
    - "A human reviewer confirms the full briefs library and detail page flows work end-to-end"
  artifacts:
    - path: "CONTENT-GUIDE.md"
      provides: "Non-technical content update workflow for adding new weekly briefs"
      contains: "Google Sheet"
  key_links:
    - from: "CONTENT-GUIDE.md"
      to: "content/briefs-index.json"
      via: "Step-by-step: export JSON from Sheet, paste into file, commit"
      pattern: "briefs-index.json"
---

<objective>
Write the non-technical content update workflow guide and perform end-to-end visual verification of Phase 3 features.

Purpose: Phase 3 success criterion #5 — "A non-developer can add a new brief by editing JSON data files and committing PDFs, triggering a Vercel rebuild." CONTENT-GUIDE.md is the deliverable enabling this. The verification checkpoint confirms the library and detail pages work correctly together before Phase 3 is marked complete.

Output: CONTENT-GUIDE.md at project root + human confirmation that briefs library and detail pages are working.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/lib/types.ts
@content/briefs-index.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write CONTENT-GUIDE.md for non-technical content editors</name>
  <files>CONTENT-GUIDE.md</files>
  <action>
    Create `CONTENT-GUIDE.md` at the project root (next to package.json, not inside app/ or .planning/).

    Write for a non-technical audience — a research assistant or comms officer who has never used a terminal. Use plain English, numbered steps, and clear section headers.

    CONTENT-GUIDE.md STRUCTURE AND CONTENT:

    ---

    # How to Add a New Policy Brief to the GGHN STARR Website

    This guide walks through adding a new weekly policy brief so it appears on the website automatically.

    **You will need:**
    - Access to the GGHN STARR Google Sheet (ask your team lead for the link)
    - The PDF and infographic files for the new brief
    - A Google Drive folder where files are stored (shared with you by your team)
    - GitHub Desktop installed ([download here](https://desktop.github.com/)) and the repository cloned

    **Time required:** 10–15 minutes

    ---

    ## Overview: How It Works

    1. You add a row in the Google Sheet with the brief's details
    2. You upload the PDF and infographic to Google Drive and copy the shareable links
    3. You run a Google Apps Script that exports the Sheet to JSON
    4. You paste the JSON output into a file in the repository
    5. You commit the change using GitHub Desktop
    6. Vercel (our hosting platform) detects the commit and automatically rebuilds the site within 2 minutes

    ---

    ## Step 1: Add the Brief to the Google Sheet

    Open the GGHN STARR Briefs Google Sheet. Add a new row at the bottom with these columns:

    | Column | What to Enter | Example |
    |--------|---------------|---------|
    | `slug` | A short URL-safe identifier — lowercase, hyphens only, no spaces | `week-04-antibiotic-stewardship` |
    | `title` | The full title of the brief | `Strengthening Antibiotic Stewardship in Sub-Saharan Africa` |
    | `weekNumber` | The week number as a plain number | `4` |
    | `publicationDate` | The publication date in YYYY-MM-DD format | `2026-03-31` |
    | `authorId` | The author's ID (from the Experts sheet) | `olawale-oladipo` |
    | `keyTakeaway` | A single sentence capturing the key finding | `Countries with stewardship programs reduce AMR by 30%.` |
    | `executiveSummary` | A 100–150 word paragraph summary | _(paste the full text)_ |
    | `keyMessages` | 3–7 bullet points, one per line within the cell (press Alt+Enter for new lines inside a cell) | `Finding 1`↵`Finding 2`↵`Finding 3` |
    | `pdfUrl` | Google Drive shareable link for the full brief PDF | `https://drive.google.com/file/d/ABC123/view?usp=sharing` |
    | `infographicPdfUrl` | Google Drive shareable link for the 1-page infographic PDF | `https://drive.google.com/file/d/XYZ789/view?usp=sharing` |
    | `thumbnailUrl` | Path to the thumbnail image: `/images/thumbnails/{slug}.jpg` | `/images/thumbnails/week-04-antibiotic-stewardship.jpg` |
    | `themes` | Comma-separated themes from this list: `Governance`, `Laboratory Systems`, `Predictive Analytics`, `One Health`, `Stewardship` | `Governance, Stewardship` |

    **Tip:** Copy an existing row as a template to get the format right, then edit each cell.

    ---

    ## Step 2: Upload Files to Google Drive

    1. Open the GGHN STARR shared Google Drive folder
    2. Upload the full brief PDF and name it: `week-04-{brief-topic}.pdf`
    3. Upload the infographic PDF and name it: `week-04-{brief-topic}-infographic.pdf`
    4. Right-click each file → **Share** → **Copy link**
       - Make sure the link is set to **"Anyone with the link can view"**
    5. Paste each link into the `pdfUrl` and `infographicPdfUrl` cells in the Google Sheet

    ---

    ## Step 3: Upload the Thumbnail Image

    The thumbnail is a small preview image (JPEG, roughly 600×400 pixels) that appears on the brief card.

    1. Prepare the thumbnail image and name it: `week-04-{brief-topic}.jpg`
    2. In GitHub Desktop, find the cloned repository folder on your computer
    3. Navigate to: `public/images/thumbnails/`
    4. Copy your thumbnail file into this folder
    5. The thumbnail will be picked up automatically when you commit in Step 5

    ---

    ## Step 4: Export the Google Sheet to JSON

    A Google Apps Script converts your Sheet into the JSON format the website reads.

    1. In the Google Sheet, click the menu: **Extensions** → **Apps Script**
    2. In the script editor, find the function called `exportBriefsToJSON`
    3. Click the **Run** button (▶) next to the function name
    4. If prompted for permissions, click **Review permissions** → **Allow**
    5. After it runs, click **View** → **Logs** (or press Ctrl+Enter)
    6. You will see a block of text starting with `[` — this is the JSON output
    7. Select all the text in the log (Ctrl+A) and copy it (Ctrl+C)

    ---

    ## Step 5: Update the Briefs Data File

    1. In GitHub Desktop, click **Repository** → **Open in Explorer** (or Finder on Mac)
    2. Navigate to the folder: `content/`
    3. Open the file `briefs-index.json` with a text editor (Notepad, TextEdit, or VS Code)
    4. Select all the existing content (Ctrl+A) and delete it
    5. Paste the JSON you copied from the Apps Script log (Ctrl+V)
    6. Save the file (Ctrl+S)

    **Check:** The file should start with `[` and end with `]`. If it looks garbled, do not save — ask a developer for help.

    ---

    ## Step 6: Commit and Push Using GitHub Desktop

    1. Open **GitHub Desktop**
    2. You should see the changed files listed (briefs-index.json and the new thumbnail)
    3. In the **Summary** box at the bottom left, type a commit message:
       `Add Week 4 brief: Antibiotic Stewardship`
    4. Click the blue **Commit to main** button
    5. Click **Push origin** (top right) to send your changes to GitHub

    ---

    ## Step 7: Wait for Vercel to Rebuild

    After you push:

    1. Go to [vercel.com](https://vercel.com) and sign in
    2. Open the **gghn-starr** project
    3. Click **Deployments** — you should see a new deployment starting (status: Building)
    4. Wait 1–3 minutes for it to complete (status: Ready)
    5. Click **Visit** to open the live site and confirm the new brief appears on the Briefs page

    ---

    ## Troubleshooting

    **The brief does not appear on the site after deploying:**
    - Check that the `slug` in the JSON matches the `thumbnailUrl` path exactly
    - Check that the Vercel deployment shows "Ready" (not "Error")
    - Check that `briefs-index.json` is valid JSON — it must start with `[` and end with `]`

    **The thumbnail image is broken (shows a grey box):**
    - Check that the thumbnail file is in `public/images/thumbnails/` with the exact filename matching `thumbnailUrl`
    - Check that the file was included in your GitHub Desktop commit

    **The PDF download opens a Google Drive page instead of downloading:**
    - This is expected — Google Drive opens a preview. Users can download from the preview page.
    - To force a direct download link, replace `/view?usp=sharing` with `/uc?export=download` in the URL.

    **The Apps Script asks for permissions every time:**
    - This is normal for first-time use. Click Allow. It will not ask again on the same Google account.

    ---

    ## Author IDs Reference

    When filling in `authorId`, use one of these values exactly:

    | Author | authorId |
    |--------|----------|
    | Dr. Olawale Oladipo | `olawale-oladipo` |
    | Dr. Samson Adeyemo | `samson-adeyemo` |
    | Piringar Mercy Niyang | `piringar-niyang` |

    ---

    ## Theme Values Reference

    Use only these exact values in the `themes` column (comma-separated):

    - `Governance`
    - `Laboratory Systems`
    - `Predictive Analytics`
    - `One Health`
    - `Stewardship`

    ---

    *Guide maintained by the GGHN STARR technical team. Last updated: 2026-03-30.*

    ---

    END OF CONTENT-GUIDE.md

    Write this file as clean Markdown. Use the exact author IDs and theme values from the project's content files (verified against content/experts.json and app/lib/types.ts).
  </action>
  <verify>Confirm CONTENT-GUIDE.md exists at project root (same level as package.json). Confirm it contains sections for: Sheet columns, Google Drive upload, Apps Script export, briefs-index.json update, GitHub Desktop commit, Vercel rebuild. Confirm all 12 Brief interface fields are listed in the column table.</verify>
  <done>CONTENT-GUIDE.md exists at project root with complete step-by-step workflow for non-technical content editors. All 12 Brief fields documented. Author IDs and theme values match actual data.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end visual verification of briefs library and detail pages</name>
  <what-built>
    Plan 03-01: Briefs library page at /briefs with BriefCard grid, BriefGrid filter bar (month + theme dropdowns), empty state, responsive layout, placeholder thumbnails.
    Plan 03-02: Brief detail pages at /briefs/[slug] with hero layout, download buttons, key messages, author bio, prev/next navigation.
    Plan 03-03 Task 1: CONTENT-GUIDE.md at project root.
  </what-built>
  <how-to-verify>
    Run `npm run dev` in the terminal (if not already running). Then verify:

    **Briefs Library (/briefs)**
    1. Visit http://localhost:3000/briefs
    2. Confirm: 3 brief cards visible in a grid (3 columns on desktop, 2 on tablet, 1 on mobile)
    3. Confirm: Each card shows a thumbnail image in the top half, then "Week N · Month D, Year" label, bold title, and author name
    4. Confirm: Each card has two side-by-side buttons — "Download PDF" and "Infographic"
    5. Select "March 2026" in the Month dropdown — all 3 cards should still show (all seeded in March)
    6. Select a Theme (e.g. "Governance") — only briefs with that theme should show
    7. Select a theme that matches only 1 brief — confirm the others disappear
    8. Clear filters — all 3 cards return
    9. Resize browser to ~375px width — cards should stack to 1 column

    **Brief Detail Page (/briefs/[slug])**
    10. Click on any brief card title or thumbnail
    11. Confirm URL changes to /briefs/week-XX-slug-name
    12. Confirm: week/date label, title, author name visible in the left column of the hero
    13. Confirm: thumbnail visible on the right side of the hero (smaller than card)
    14. Confirm: "Download PDF" and "Download Infographic" buttons in the hero area
    15. Confirm: Executive Summary section with full paragraph text below the hero
    16. Confirm: Key Messages section with dash-prefixed bullet list
    17. Confirm: About the Author section with photo, name, title, bio excerpt
    18. Confirm: Prev/Next navigation at the bottom — week-01 has no "Previous", week-03 has no "Next"
    19. Visit http://localhost:3000/briefs/nonexistent-slug — confirm a 404 page appears (not a crash)

    **Content Guide**
    20. Open CONTENT-GUIDE.md at the project root and skim it — confirm it is written in plain language, covers all 12 Brief fields, and has sections for each step of the workflow

    **Build**
    21. Run `npm run build` — confirm it completes without errors
    22. Confirm build output lists /briefs and /briefs/week-01-amr-governance-frameworks etc. as generated pages
  </how-to-verify>
  <action>Human verification step — no automated action. Run `npm run dev` and walk through the how-to-verify checklist above.</action>
  <verify>All 22 checks in how-to-verify pass. Run `npm run build` and confirm no errors.</verify>
  <done>Human confirms "approved" — all library, detail page, and content guide checks pass. Build succeeds.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found (e.g. "filter not working", "thumbnail broken", "build error: [message]")</resume-signal>
</task>

</tasks>

<verification>
All checks in the human-verify checkpoint above must pass before Phase 3 is marked complete. Build must succeed cleanly.
</verification>

<success_criteria>
- CONTENT-GUIDE.md exists at project root, written for non-technical editors
- Briefs library renders correctly with filtering
- Brief detail pages render correctly at full slug URLs
- build succeeds
- Human verifier confirms "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/03-policy-briefs-library-and-detail-pages/03-03-SUMMARY.md` following the summary template.
</output>
