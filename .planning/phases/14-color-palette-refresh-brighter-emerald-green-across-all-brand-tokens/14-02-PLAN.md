---
phase: 14-color-palette-refresh-brighter-emerald-green-across-all-brand-tokens
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 11 routes render the new brighter emerald green (#319974) visibly in CTAs, buttons, badges, and section accents"
    - "The homepage hero CTA button is visibly brighter/lighter green compared to the previous #0A7050 dark green"
    - "No UI element shows the old dark #0A7050 green — the color shift is visible everywhere teal-600 was used"
    - "Gold, navy, and slate elements are visually unchanged across all pages"
  artifacts:
    - path: "app/globals.css"
      provides: "Token definitions driving the visual color change"
      contains: "--color-teal-600: #319974"
  key_links:
    - from: "app/globals.css --color-teal-600"
      to: "all teal-600 class usages site-wide"
      via: "Tailwind v4 CSS variable resolution at build time"
      pattern: "teal-600"
---

<objective>
Human visual verification that the new brighter emerald green (#319974) renders correctly across all 11 site routes after the token swap in Plan 01.

Purpose: Token changes propagate automatically via Tailwind class resolution, but visual rendering can only be confirmed by a human looking at a running browser. This checkpoint catches any missed teal class, unexpected color collision, or visual regression before the phase is signed off.

Output: Signed-off confirmation that Phase 14 color palette refresh is production-ready.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server for visual inspection</name>
  <files></files>
  <action>
    Run the Next.js dev server so the human can inspect the updated palette in a browser.

    Command: npm run dev

    The server starts on http://localhost:3000. Output the URL clearly so the human knows where to navigate.

    Do not wait — just confirm the server has started (output includes "Ready" or "started server on") and hand off to the checkpoint task.
  </action>
  <verify>
    Terminal output shows "Ready in" or "Local: http://localhost:3000" — dev server is running.
  </verify>
  <done>
    Dev server running at http://localhost:3000 — ready for visual inspection.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — all 11 routes</name>
  <files></files>
  <action>
    Pause execution and present the verification checklist below to the human. Wait for their "approved" signal or issue report before proceeding to the summary.
  </action>
  <what-built>
    Plan 01 replaced the teal token scale in app/globals.css. teal-600 moved from #0A7050 (dark emerald) to #319974 (brighter emerald). teal-700, teal-100, teal-200, teal-900 were added as new stops. The change propagates automatically to every component using teal-* Tailwind classes — no component files were modified.
  </what-built>
  <how-to-verify>
    Visit each route and confirm the new brighter emerald green is visible. The new color is noticeably lighter and brighter than the old #0A7050. Pay attention to CTAs, buttons, badges, section borders, and link hovers.

    Routes to check (navigate each in order):

    1. http://localhost:3000/ (Homepage)
       - Hero CTA button ("View All Briefs" / primary button) — should be bright emerald, not dark forest green
       - Featured Brief section — download button color
       - Audience CTA section — card borders or accents

    2. http://localhost:3000/briefs (Policy Briefs Library)
       - Brief card download buttons — bright emerald
       - Filter controls if teal-colored

    3. http://localhost:3000/briefs/brief-15 (Any brief detail page)
       - Download buttons, badges, links

    4. http://localhost:3000/awareness (Awareness Hub)
       - Section headings or accents in teal

    5. http://localhost:3000/news (News Feed)
       - Source badges or card accents

    6. http://localhost:3000/take-action (Take Action)
       - Action card buttons, form submit buttons

    7. http://localhost:3000/experts (Experts)
       - Card accents or borders

    8. http://localhost:3000/conference (Conference Hub)
       - Section accents (note: ConferenceBar is crimson — that should NOT change)

    9. http://localhost:3000/education (Education Library)
       - Tab controls, filter pills, card accents — teal-100 and teal-200 tints should appear in these UI elements

    10. http://localhost:3000/contact (Contact)
        - Form submit button

    11. http://localhost:3000/methodology (Methodology)
        - Tab controls or accents

    What to confirm on each route:
    - Primary buttons/CTAs are visibly brighter emerald (#319974) vs the old dark green
    - No UI element shows the old muddy dark green — the color shift should be clear
    - Gold elements (AMR gold badges, accents) are unchanged — still #F2A900 yellow-gold
    - Dark navy backgrounds are unchanged — still dark green/navy
    - No layout breakage or missing colors (if a teal class renders no color, a token is missing)
  </how-to-verify>
  <verify>
    Human has typed "approved" confirming all 11 routes show correct brighter emerald green with no regressions.
  </verify>
  <done>
    All 11 routes visually confirmed: new emerald green (#319974) renders on all CTAs and accents, gold/navy tokens unchanged, no missing or broken colors.
  </done>
  <resume-signal>
    Type "approved" if all 11 routes show correct brighter emerald green with no regressions.
    Or describe any issues seen (which route, what element, what looks wrong).
  </resume-signal>
</task>

</tasks>

<verification>
Human has confirmed on all 11 routes:
- teal-600 (#319974) renders as visibly brighter/lighter emerald vs old #0A7050
- No UI element shows old dark green
- Gold and navy tokens visually unchanged
- No missing colors (no invisible teal elements)
- next build passed in Plan 01 with zero errors
</verification>

<success_criteria>
- Human approves visual rendering across all 11 routes
- New brighter emerald green (#319974) is clearly visible on primary CTAs, buttons, and accents
- No color regressions on gold, navy, or slate elements
- Phase 14 color palette refresh confirmed production-ready
</success_criteria>

<output>
After completion, create `.planning/phases/14-color-palette-refresh-brighter-emerald-green-across-all-brand-tokens/14-02-SUMMARY.md`
</output>
