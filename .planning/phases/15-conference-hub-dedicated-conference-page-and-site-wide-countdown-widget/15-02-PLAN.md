---
phase: 15-conference-hub-dedicated-conference-page-and-site-wide-countdown-widget
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/conference/page.tsx
  - app/components/conference/ConferenceHero.tsx
  - app/components/conference/ConferenceAbout.tsx
  - app/components/conference/ConferenceThemes.tsx
autonomous: false
requirements:
  - CONF-02
  - CONF-03

must_haves:
  truths:
    - "Navigating to /conference shows a page with hero, about, and themes sections"
    - "The hero displays a large days countdown, the full event name, date, location, and a Register Now CTA"
    - "The about section contains the extracted 2-3 sentence overview of the meeting's significance"
    - "The themes section shows the 5 agenda highlights with icons or bullets"
    - "All Register Now CTAs link to https://www.5thhighlevelministerialng.com/ in a new tab"
    - "After June 28, 2026, the hero automatically shows 'Conference Held June 28–30, 2026' with a View Conference Outcomes CTA instead of a countdown"
    - "The page is statically generated (no server-side runtime)"
  artifacts:
    - path: "app/conference/page.tsx"
      provides: "Static /conference route composing the three section components"
      exports: ["default (ConferencePage)"]
    - path: "app/components/conference/ConferenceHero.tsx"
      provides: "Hero section with client-side countdown and pre/post conference state"
      exports: ["ConferenceHero"]
    - path: "app/components/conference/ConferenceAbout.tsx"
      provides: "Static about section with extracted conference overview text"
      exports: ["ConferenceAbout"]
    - path: "app/components/conference/ConferenceThemes.tsx"
      provides: "Static themes section with 5 agenda highlights"
      exports: ["ConferenceThemes"]
  key_links:
    - from: "app/conference/page.tsx"
      to: "app/components/conference/ConferenceHero.tsx"
      via: "import and render <ConferenceHero />"
      pattern: "ConferenceHero"
    - from: "app/components/conference/ConferenceHero.tsx"
      to: "ConferenceBadge pattern"
      via: "useState<number | null>(null) + useEffect countdown"
      pattern: "useState.*null"
---

<objective>
Build the /conference gateway page with hero countdown, about section, and themes section.

Purpose: Provide a branded teaser page that routes African health policymakers to the official 5th High-Level Inter-Ministerial Meeting registration site. Auto-archives after June 28.
Output: app/conference/ route with three composed section components.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@app/components/sections/ConferenceBadge.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConferenceHero, ConferenceAbout, ConferenceThemes components</name>
  <files>
    app/components/conference/ConferenceHero.tsx
    app/components/conference/ConferenceAbout.tsx
    app/components/conference/ConferenceThemes.tsx
  </files>
  <action>
    **ConferenceHero.tsx** — `'use client'` component:

    Mirror ConferenceBadge's countdown pattern:
    - `TARGET_DATE = new Date('2026-06-28T00:00:00')`
    - `useState<number | null>(null)` for daysLeft
    - `useEffect` + `setInterval(60_000)` to keep count fresh
    - `isPast` derived: `daysLeft !== null && daysLeft <= 0`

    Layout — full-width dark green hero section (`bg-navy-950 text-white`):

    **Pre-conference state** (isPast === false OR daysLeft === null):
    ```
    <section className="bg-navy-950 text-white py-20 px-4 text-center">
      <p className="text-amr-gold font-semibold uppercase tracking-widest text-sm mb-4">
        5th High-Level Inter-Ministerial Meeting on Antimicrobial Resistance
      </p>
      {/* Large countdown numeral */}
      <div className="text-9xl font-bold tabular-nums leading-none mb-2 text-white">
        {daysLeft === null ? '—' : daysLeft}
      </div>
      <p className="text-2xl text-slate-300 mb-2">
        {daysLeft === null ? 'Loading…' : daysLeft === 1 ? 'day to go' : 'days to go'}
      </p>
      <p className="text-lg text-slate-400 mb-8">
        June 28–30, 2026 · Transcorp Hilton Conference Center, Abuja, Nigeria
      </p>
      <a
        href="https://www.5thhighlevelministerialng.com/"
        target="_blank"
        rel="noopener noreferrer"
        className="inline-block bg-[#DC2626] text-white font-bold px-8 py-3 rounded-lg text-lg hover:bg-red-700 transition-colors animate-cta-pulse hover:animate-none"
      >
        Register Now
      </a>
    </section>
    ```

    **Post-conference state** (isPast === true):
    ```
    <section className="bg-navy-950 text-white py-20 px-4 text-center">
      <p className="text-amr-gold font-semibold uppercase tracking-widest text-sm mb-6">
        5th High-Level Inter-Ministerial Meeting on Antimicrobial Resistance
      </p>
      <h1 className="text-4xl md:text-5xl font-bold mb-4">
        Conference Held June 28–30, 2026
      </h1>
      <p className="text-slate-300 text-lg mb-8 max-w-xl mx-auto">
        The 5th High-Level Inter-Ministerial Meeting on AMR took place at the Transcorp Hilton Conference Center, Abuja, Nigeria.
      </p>
      <a
        href="https://www.5thhighlevelministerialng.com/"
        target="_blank"
        rel="noopener noreferrer"
        className="inline-block bg-teal-600 text-white font-bold px-8 py-3 rounded-lg text-lg hover:bg-teal-500 transition-colors"
      >
        View Conference Outcomes
      </a>
    </section>
    ```

    ---

    **ConferenceAbout.tsx** — static server component (no `'use client'`):

    ```tsx
    export function ConferenceAbout() {
      return (
        <section className="py-16 px-4 bg-white">
          <div className="max-w-3xl mx-auto">
            <h2 className="text-2xl font-bold text-navy-950 mb-6 text-center">
              About the Meeting
            </h2>
            <p className="text-slate-700 text-lg leading-relaxed">
              The 5th High-Level Inter-Ministerial Meeting on Antimicrobial Resistance convenes ministers
              and health leaders to translate global AMR commitments into local action — marking the first
              time this flagship ministerial conference is held on the African continent. With microorganisms
              evolving resistance to life-saving medicines, the "silent pandemic" of AMR demands urgent,
              coordinated policy response. This meeting, held under the patronage of President Bola Ahmed
              Tinubu, positions Nigeria and Africa at the centre of global health security.
            </p>
            <p className="text-slate-500 text-base mt-4 text-center italic">
              Overarching theme: "One Health: Advancing Global AMR Commitments through Local Action"
            </p>
          </div>
        </section>
      );
    }
    ```

    ---

    **ConferenceThemes.tsx** — static server component:

    5 agenda highlights with icons from lucide-react. Use these icons:
    - `ClipboardList` — NAP tracking
    - `Pill` — antimicrobial availability
    - `DollarSign` — sustainable financing
    - `Globe` — African leadership
    - `Calendar` — side events

    ```tsx
    import { ClipboardList, Pill, DollarSign, Globe, Calendar } from 'lucide-react';

    const themes = [
      { icon: ClipboardList, text: 'Tracking implementation of AMR National Action Plans (NAPs)' },
      { icon: Pill,          text: 'Addressing antimicrobial and diagnostic availability gaps' },
      { icon: DollarSign,    text: 'Sustainable financing for AMR response in LMICs' },
      { icon: Globe,         text: 'African leadership in global health security frameworks' },
      { icon: Calendar,      text: 'Side events: Non-State Actor\'s Day (Jun 28) · Ministerial session (Jun 29) · Declaration and closing (Jun 30)' },
    ];

    export function ConferenceThemes() {
      return (
        <section className="py-16 px-4 bg-slate-50">
          <div className="max-w-3xl mx-auto">
            <h2 className="text-2xl font-bold text-navy-950 mb-8 text-center">
              Key Themes & Agenda
            </h2>
            <ul className="space-y-4">
              {themes.map(({ icon: Icon, text }, i) => (
                <li key={i} className="flex items-start gap-4 bg-white rounded-lg px-5 py-4 shadow-sm">
                  <Icon className="text-teal-600 mt-0.5 shrink-0" size={22} />
                  <span className="text-slate-700">{text}</span>
                </li>
              ))}
            </ul>
          </div>
        </section>
      );
    }
    ```
  </action>
  <verify>
    `npx tsc --noEmit` — no TypeScript errors in any of the three component files.
    Confirm each file exports its named component.
  </verify>
  <done>All three components compile. ConferenceHero has pre/post conference conditional rendering. ConferenceAbout and ConferenceThemes are static with correct content.</done>
</task>

<task type="auto">
  <name>Task 2: /conference page.tsx assembly</name>
  <files>app/conference/page.tsx</files>
  <action>
    Create `app/conference/page.tsx` — server component (no `'use client'` on the page itself).

    ```tsx
    import type { Metadata } from 'next';
    import { ConferenceHero } from '@/components/conference/ConferenceHero';
    import { ConferenceAbout } from '@/components/conference/ConferenceAbout';
    import { ConferenceThemes } from '@/components/conference/ConferenceThemes';

    export const metadata: Metadata = {
      title: '5th High-Level Inter-Ministerial Meeting on AMR',
      description:
        'Join ministers and health leaders at the 5th High-Level Inter-Ministerial Meeting on Antimicrobial Resistance — June 28–30, 2026, Abuja, Nigeria.',
      openGraph: {
        title: '5th High-Level Inter-Ministerial Meeting on AMR',
        description:
          'The first flagship AMR ministerial conference on the African continent. June 28–30, 2026 · Abuja, Nigeria.',
      },
    };

    export default function ConferencePage() {
      return (
        <div>
          <ConferenceHero />
          <ConferenceAbout />
          <ConferenceThemes />
        </div>
      );
    }
    ```

    NOTE: ConferenceHero is a client component — it can be imported and rendered by a server component page. This is the standard Next.js pattern and works fine with static export.

    NOTE: The `animate-cta-pulse` class defined in globals.css (from plan 15-01) must exist for the hero's Register Now button animation. If plan 15-01 has not yet run, the button renders without animation but still functions — animation is a visual enhancement only.
  </action>
  <verify>
    `npx next build` — no build errors, /conference route appears in output.
    `npx tsc --noEmit` — zero errors.
  </verify>
  <done>Build passes. /conference page renders all three sections. Route is included in the static export.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of /conference page and ConferenceBar widget</name>
  <what-built>
    /conference gateway page with:
    - Full-width dark-green hero: large days countdown (9xl numeral), event name, date/location, pulsing Register Now CTA
    - About section: extracted conference overview + theme tagline
    - Themes section: 5 agenda highlights with lucide icons in white cards
    Plus (from plan 15-01 if run in parallel):
    - Crimson sticky bar above header on all pages except /conference
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and navigate to http://localhost:3000/conference
    2. Confirm hero shows a large number (days until June 28) + "Register Now" button with pulse animation
    3. Confirm "About the Meeting" section shows the conference overview paragraph
    4. Confirm "Key Themes & Agenda" section shows 5 items with icons
    5. Click Register Now — should open https://www.5thhighlevelministerialng.com/ in a new tab
    6. Navigate to any other page (e.g. /): confirm the crimson bar appears above the header (if plan 15-01 has run)
    7. Confirm the crimson bar does NOT appear on /conference
    8. Click the X on the crimson bar — confirm it disappears; open new tab → confirm it returns
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
  <files>none</files>
  <action>Human visually verifies the /conference page and ConferenceBar widget match design intent per the how-to-verify steps above.</action>
  <verify>User types "approved" or describes issues.</verify>
  <done>User has confirmed visual layout, countdown accuracy, Register Now link, bar dismiss behavior, and bar absence on /conference.</done>
</task>

</tasks>

<verification>
1. `npx next build` — full static export succeeds
2. `npx tsc --noEmit` — zero TypeScript errors
3. /conference page renders with all three sections
4. Pre/post conference conditional rendering logic verified in ConferenceHero
5. All external links use target="_blank" rel="noopener noreferrer"
6. Metadata title and description set on the page
</verification>

<success_criteria>
- /conference is a working static route with hero + about + themes sections
- Hero shows a days countdown pre-conference and archive heading post-conference
- Register Now CTAs link correctly to the official site in a new tab
- Page renders on Vercel static export with no server-side dependencies
- Human verifies layout and content match design intent
</success_criteria>

<output>
After completion, create `.planning/phases/15-conference-hub-dedicated-conference-page-and-site-wide-countdown-widget/15-02-SUMMARY.md`
</output>
