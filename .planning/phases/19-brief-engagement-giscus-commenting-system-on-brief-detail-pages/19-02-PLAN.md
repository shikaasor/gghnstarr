---
phase: 19-brief-engagement-giscus-commenting-system-on-brief-detail-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - app/briefs/[slug]/page.tsx
  - .github/workflows/fetch-comments.yml
autonomous: true

requirements:
  - BENG-01
  - BENG-02

must_haves:
  truths:
    - "Every brief detail page shows a Discussion section below the Prev/Next navigation"
    - "CommentList renders approved comments from content/comments.json keyed by slug"
    - "CommentForm allows anonymous submission to GAS endpoint"
    - "A GitHub Actions cron workflow fetches approved comments daily from GAS_COMMENTS_URL secret"
    - "The Discussion section is hidden when the page is printed (no-print class)"
  artifacts:
    - path: "app/briefs/[slug]/page.tsx"
      provides: "Brief detail page with Discussion section below Prev/Next nav"
      contains: "CommentList"
    - path: ".github/workflows/fetch-comments.yml"
      provides: "Daily cron to fetch approved comments from GAS and commit to content/comments.json"
      contains: "GAS_COMMENTS_URL"
  key_links:
    - from: "app/briefs/[slug]/page.tsx"
      to: "app/components/briefs/CommentForm.tsx"
      via: "import { CommentForm } from '@/components/briefs/CommentForm'"
      pattern: "CommentForm"
    - from: "app/briefs/[slug]/page.tsx"
      to: "app/components/briefs/CommentList.tsx"
      via: "import { CommentList } from '@/components/briefs/CommentList'"
      pattern: "CommentList"
---

<objective>
PIVOT: Original plan wired Giscus iframe + created giscus.json origin allowlist + required GitHub setup. Replaced with wiring CommentForm/CommentList into brief detail page and creating fetch-comments GitHub Actions cron workflow.

No GitHub account required — commenters submit anonymously via GAS form. Approved comments are served as static JSON updated daily by cron.
Output: Updated page.tsx (Discussion section) + .github/workflows/fetch-comments.yml
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Wire Discussion section into brief detail page</name>
  <files>app/briefs/[slug]/page.tsx</files>
  <action>Add CommentForm and CommentList imports. Add Discussion section after Prev/Next nav, inside Container but outside max-w-3xl.</action>
  <done>page.tsx imports and renders CommentList and CommentForm. TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add fetch-comments GitHub Actions cron workflow</name>
  <files>.github/workflows/fetch-comments.yml</files>
  <action>Create fetch-comments.yml — daily 06:00 UTC cron, curl GAS_COMMENTS_URL secret to content/comments.json, commit with [skip ci].</action>
  <done>.github/workflows/fetch-comments.yml exists. Mirrors fetch-news.yml pattern.</done>
</task>

</tasks>
