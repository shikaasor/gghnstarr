---
phase: 22-icars-projects-on-awareness-showcase-40-funded-amr-intervent
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/components/awareness/IcarsProjectCard.tsx
  - app/components/awareness/IcarsProjectsSection.tsx
  - app/awareness/page.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "The /awareness page renders an 'AMR Interventions Funded by ICARS' section below the existing 'AMR Explained' section"
    - "Each project card displays country, sector badge, title, description excerpt, and key outcomes"
    - "Cards with no sector (ZAMBIA row) render without a badge and do not crash"
    - "Selecting a sector chip filters the visible cards to that sector; multiple selections widen results (OR-within)"
    - "The result count updates as filters change"
    - "next build succeeds with the new section"
  artifacts:
    - path: "app/components/awareness/IcarsProjectCard.tsx"
      provides: "Presentational card rendering country, sector badge, title, description, outcomes"
      contains: "project.country"
    - path: "app/components/awareness/IcarsProjectsSection.tsx"
      provides: "Client stateful sector-filter grid"
      contains: "'use client'"
    - path: "app/awareness/page.tsx"
      provides: "Build-time JSON load + section wiring"
      contains: "icars-projects.json"
  key_links:
    - from: "app/awareness/page.tsx"
      to: "content/icars-projects.json"
      via: "readFileSync at build time"
      pattern: "readFileSync.*icars-projects\\.json"
    - from: "app/awareness/page.tsx"
      to: "IcarsProjectsSection"
      via: "import + render with projects prop"
      pattern: "<IcarsProjectsSection projects="
    - from: "IcarsProjectsSection.tsx"
      to: "IcarsProjectCard"
      via: "filtered.map render"
      pattern: "IcarsProjectCard key="
---

<objective>
Build the presentational `IcarsProjectCard`, the client-stateful `IcarsProjectsSection` (sector chip filter), and wire them into `app/awareness/page.tsx` as a new "AMR Interventions Funded by ICARS" section below the existing "AMR Explained" section. End with a human visual-verification checkpoint.

Purpose: Delivers the user-facing phase goal — 25 ICARS projects as sector-filterable cards on /awareness.
Output: 2 new components, an edited awareness page, and a visually verified section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-RESEARCH.md
@.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-PATTERNS.md
@.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IcarsProjectCard presentational component</name>
  <files>app/components/awareness/IcarsProjectCard.tsx</files>
  <read_first>
    - app/components/tools/ToolCard.tsx (exact analog: type-only import, named props interface, default export, card shell classes, uppercase meta row, serif title, line-clamp-3 description, footer pill row, conditional-render guards like `tool.year !== undefined`)
    - app/lib/types.ts (the IcarsProject interface + IcarsSector union from Plan 22-01 — confirm field names: country, sector?, title, description, outcomes, fundingPartners)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-RESEARCH.md (Pattern 3 SECTOR_STYLES color map lines 165-181; Pitfall 5 empty-sector guard; Pitfall 7 outcomes rendering; Security section — never dangerouslySetInnerHTML)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-PATTERNS.md (IcarsProjectCard pattern assignments)
  </read_first>
  <action>
    Create app/components/awareness/IcarsProjectCard.tsx as a presentational Server Component (NO 'use client'). Type-only import `import type { IcarsProject, IcarsSector } from '@/lib/types'`. Named props interface `IcarsProjectCardProps { project: IcarsProject }`, default export `IcarsProjectCard({ project })`.

    Reuse the ToolCard shell exactly: outer `<div className="group bg-white rounded-lg border border-slate-200 shadow-sm hover:shadow-md transition-shadow p-5 flex flex-col gap-3">`. Top meta row shows the country uppercase: `<span className="text-xs text-slate-400 font-medium uppercase tracking-wide">{project.country}</span>`. Render the title as a plain serif heading (ICARS has NO url column, so drop ToolCard's `tool.url ? <a> : <span>` branch): `<h3 className="font-serif font-semibold text-navy-950 text-sm leading-snug">{project.title}</h3>`. Render the description with the line-clamp guard: `{project.description && (<p className="text-xs text-slate-600 line-clamp-3 leading-relaxed">{project.description}</p>)}`.

    Render key outcomes as a mapped list (NEVER dangerouslySetInnerHTML — Security/Pitfall 7): when `project.outcomes.length > 0`, render a `<ul>` of `<li>` items mapping over project.outcomes with small slate text styling (e.g. `text-xs text-slate-600 list-disc list-inside`). Each `<li key={index}>{outcome}</li>` uses plain `{outcome}` interpolation (React auto-escapes — XSS-safe).

    Define a module-level `const SECTOR_STYLES: Record<IcarsSector, string>` mapping the 5 sectors to Tailwind pill classes (color choice is discretion per RESEARCH A1; suggested: Humans -> 'bg-teal-50 text-teal-700', One Health -> 'bg-amber-100 text-amber-800', Terrestrial and Aquatic Animals -> 'bg-slate-100 text-slate-700', Food and Feed -> 'bg-emerald-50 text-emerald-700', Environment -> 'bg-sky-50 text-sky-700'). In the footer pill row (`<div className="mt-auto flex flex-wrap items-center gap-2">`), conditionally render the sector badge ONLY when sector is truthy (Pitfall 5 — the ZAMBIA row has no sector): `{project.sector && (<span className={`text-xs px-2 py-0.5 rounded-full font-medium ${SECTOR_STYLES[project.sector]}`}>{project.sector}</span>)}`. Optionally show fundingPartners as muted text in the footer; do NOT surface fundingAmount (RESEARCH Open Q1 default). Do NOT place fenced code in this plan — follow the cited analog.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - app/components/awareness/IcarsProjectCard.tsx contains `import type { IcarsProject` 
    - app/components/awareness/IcarsProjectCard.tsx contains `project.country`
    - app/components/awareness/IcarsProjectCard.tsx contains `SECTOR_STYLES`
    - app/components/awareness/IcarsProjectCard.tsx contains `project.sector &&` (conditional badge guard)
    - app/components/awareness/IcarsProjectCard.tsx contains `project.outcomes` mapped to list items
    - app/components/awareness/IcarsProjectCard.tsx does NOT contain `dangerouslySetInnerHTML`
    - app/components/awareness/IcarsProjectCard.tsx does NOT contain `'use client'`
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>Card renders country, sector badge (guarded), title, description excerpt, and outcomes; lint passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create IcarsProjectsSection client component with sector filter, and wire into awareness page</name>
  <files>app/components/awareness/IcarsProjectsSection.tsx, app/awareness/page.tsx</files>
  <read_first>
    - app/components/tools/ToolsGrid.tsx (analog: 'use client', useState/useMemo, derive available options, filter memo, result count, responsive grid, empty state)
    - app/components/tools/ToolsFilters.tsx (the generic immutable `toggleValue<T>` helper and the chip-row button styling: active `bg-teal-600 text-white`, inactive `bg-slate-100 text-slate-600 hover:bg-slate-200`)
    - app/awareness/page.tsx (the target Server Component — read in full: it has NO 'use client', NO readFileSync import, function `AwarenessPage()`, uses named `import { Container } from '@/components/layout/Container'`, and ends with the "AMR Explained" `<section className="py-16 bg-amber-50/60">` as the last section before `</main>`)
    - app/tools-directory/page.tsx (the build-time JSON load with graceful try/catch idiom to transplant)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-RESEARCH.md (Pattern 1 load idiom lines 127-147; Pattern 2 single-dimension filter lines 149-163; wiring example lines 368-398)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-PATTERNS.md (IcarsProjectsSection + awareness page edit pattern blocks)
  </read_first>
  <action>
    Create app/components/awareness/IcarsProjectsSection.tsx with `'use client'` at the top. Import `{ useState, useMemo } from 'react'`, `import type { IcarsProject, IcarsSector } from '@/lib/types'`, and `import IcarsProjectCard from './IcarsProjectCard'`. Props interface `IcarsProjectsSectionProps { projects: IcarsProject[] }`, default export `IcarsProjectsSection({ projects })`.

    Copy the generic `toggleValue<T>(current: T[], value: T): T[]` helper verbatim from ToolsFilters. Single state array: `const [selectedSectors, setSelectedSectors] = useState<IcarsSector[]>([])`. Derive available sectors (filtering out null) with `const availableSectors = useMemo(() => [...new Set(projects.map((p) => p.sector).filter(Boolean))].sort() as IcarsSector[], [projects])`. Filter memo (single dimension, OR-within per RESEARCH Pattern 2 — use `.includes`, NOT `.some()`): `const filtered = useMemo(() => projects.filter((p) => selectedSectors.length === 0 || (p.sector !== undefined && selectedSectors.includes(p.sector))), [projects, selectedSectors])`.

    Render: an inline sector chip row (no separate Filters file needed — single dimension) where each available sector is a `<button>` toggling via `setSelectedSectors(toggleValue(selectedSectors, sector))`, styled active `bg-teal-600 text-white` / inactive `bg-slate-100 text-slate-600 hover:bg-slate-200` per the ToolsFilters chip pattern. Below the chips, a result count `<p>` (`{filtered.length} {filtered.length === 1 ? 'project' : 'projects'}{filtered.length !== projects.length && ` (filtered from ${projects.length})`}`). Then a responsive grid `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">` mapping `filtered` to `<IcarsProjectCard key={project.id} project={project} />`. Omit pagination (25 items < page size, RESEARCH A5). Add an empty-state `<p>` when `filtered.length === 0`: "No projects match the selected sectors."

    Then EDIT app/awareness/page.tsx: add `import { readFileSync } from 'fs'`, `import { join } from 'path'`, `import IcarsProjectsSection from '@/components/awareness/IcarsProjectsSection'`, and `import type { IcarsProject } from '@/lib/types'` at the top. Inside the `AwarenessPage()` function body before the return, add the graceful load: `let icarsProjects: IcarsProject[] = []; try { icarsProjects = JSON.parse(readFileSync(join(process.cwd(), 'content/icars-projects.json'), 'utf-8')) as IcarsProject[]; } catch (err) { console.error('[AwarenessPage] Failed to load icars-projects.json:', err); }`. Append a NEW `<section className="py-16 bg-white">` AFTER the closing `</section>` of the "AMR Explained" block (the last section before `</main>`), containing a `<Container>`, an `<h2 className="font-serif text-2xl md:text-3xl font-bold text-navy-950 mb-2">AMR Interventions Funded by ICARS</h2>`, a descriptive `<p className="text-slate-600 text-sm mb-8">`, and `<IcarsProjectsSection projects={icarsProjects} />`. Do NOT place fenced code in this plan — follow the cited analogs.
  </action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - app/components/awareness/IcarsProjectsSection.tsx contains `'use client'`
    - app/components/awareness/IcarsProjectsSection.tsx contains `useState<IcarsSector[]>`
    - app/components/awareness/IcarsProjectsSection.tsx contains `selectedSectors.includes(` (single-dimension filter, not `.some()`)
    - app/components/awareness/IcarsProjectsSection.tsx contains `IcarsProjectCard key={project.id}`
    - app/awareness/page.tsx contains `readFileSync` and `content/icars-projects.json`
    - app/awareness/page.tsx contains `<IcarsProjectsSection projects={icarsProjects} />`
    - app/awareness/page.tsx contains the heading text `AMR Interventions Funded by ICARS`
    - `npm run lint` exits 0 and `npm run build` exits 0 (static export succeeds)
  </acceptance_criteria>
  <done>Section is wired below "AMR Explained"; build and lint pass; sector filter is functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The "AMR Interventions Funded by ICARS" section on /awareness, showing 25 ICARS project cards filterable by sector. Each card displays country, sector badge (color-coded), title, description excerpt, and key outcomes. The extraction script normalized 5 sectors and carried country headers down to project rows.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/awareness
    2. Scroll below the "AMR Explained" accordion section — confirm the new "AMR Interventions Funded by ICARS" section appears
    3. Confirm 25 project cards render (count visible in the result count text)
    4. Confirm each card shows: an uppercase country, a colored sector badge (one ZAMBIA card will have NO badge — that is correct, Pitfall 5), a title, a description excerpt (max 3 lines), and a list of key outcomes
    5. Click a sector chip (e.g. "Humans") — confirm cards filter to that sector and the result count updates; click a second chip — confirm results widen (OR-within); click the active chip again to deselect
    6. Check the funder text for any visible `?`/replacement characters (mojibake, Pitfall 6) — note them for a possible follow-up fix; they are non-blocking
    7. Confirm no console errors and no `Enviroment` typo appears anywhere
  </how-to-verify>
  <resume-signal>Type "approved" or describe any rendering/filter issues (e.g. wrong country on the ENVIRE card, missing outcomes, broken filter).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON data -> rendered JSX | xlsx-sourced text (description, outcomes, country, funder names) is interpolated into React components and served to public browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Tampering (XSS) | IcarsProjectCard rendering of description/outcomes/country | mitigate | Render ALL xlsx-sourced text via `{value}` interpolation and mapped `<li>` only; React auto-escapes. Forbid `dangerouslySetInnerHTML` (enforced by Task 1 acceptance criterion). Outcomes rendered as `<li>` list, never raw HTML |
| T-22-04 | Denial of Service | awareness page build-time JSON load | mitigate | readFileSync wrapped in try/catch; a missing/corrupt JSON degrades to an empty array ("0 projects") rather than crashing the build (RESEARCH Pattern 1) |
| T-22-05 | Tampering | external links from cards | accept | ICARS data has NO url column; no external `<a>` links are rendered, so tab-nabbing surface is absent. If a future link is added, use `rel="noopener noreferrer"` per ToolCard convention |
| T-22-SC | Tampering | npm installs | mitigate | Zero new packages this phase (RESEARCH Package Legitimacy Audit). slopcheck moot |
</threat_model>

<verification>
- `npm run lint` exits 0
- `npm run build` exits 0 (static export with the new section)
- Visual checkpoint approved: 25 cards, working sector filter, country/badge/title/description/outcomes per card, no `dangerouslySetInnerHTML`
</verification>

<success_criteria>
- /awareness shows an "AMR Interventions Funded by ICARS" section below "AMR Explained"
- 25 sector-filterable cards each display country, sector badge (guarded), title, description excerpt, and key outcomes
- Sector filter works (OR-within, result count updates); empty-sector card renders without crashing
- Build and lint green; XSS-safe rendering (no dangerouslySetInnerHTML)
</success_criteria>

<output>
Create `.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-02-SUMMARY.md` when done
</output>
