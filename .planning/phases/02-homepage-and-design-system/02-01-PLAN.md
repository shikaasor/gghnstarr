---
phase: 02-homepage-and-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/briefs-index.json
  - content/site.json
  - app/lib/types.ts
  - app/lib/content.ts
  - app/page.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "Running next build succeeds with no TypeScript errors after schema additions"
    - "getFeaturedBrief() returns the week-03 brief (featured: true)"
    - "getSiteContent().stats returns an array of 3 AMR statistics"
    - "app/page.tsx imports all 6 section components and calls getSiteContent() and getFeaturedBrief()"
  artifacts:
    - path: "content/briefs-index.json"
      provides: "Brief seed data with featured flag"
      contains: '"featured": true'
    - path: "content/site.json"
      provides: "Site content with stats array"
      contains: '"stats"'
    - path: "app/lib/types.ts"
      provides: "Updated Brief and SiteContent interfaces"
      exports: ["Brief", "SiteContent"]
    - path: "app/lib/content.ts"
      provides: "getFeaturedBrief() content function"
      exports: ["getFeaturedBrief"]
    - path: "app/page.tsx"
      provides: "Homepage Server Component orchestrator"
      contains: "getFeaturedBrief"
  key_links:
    - from: "app/page.tsx"
      to: "app/lib/content.ts"
      via: "getFeaturedBrief() and getSiteContent() imports"
      pattern: "getFeaturedBrief|getSiteContent"
    - from: "app/lib/content.ts"
      to: "content/briefs-index.json"
      via: "fs.readFileSync"
      pattern: "briefs-index\\.json"
---

<objective>
Patch the JSON schemas and content layer to support all homepage data needs, then wire app/page.tsx as the Server Component orchestrator that assembles sections.

Purpose: Without the `featured` field and `stats` array, the Featured Brief and Stats Strip sections cannot render. This plan fixes both data gaps and establishes the page orchestrator that all section components plug into.
Output: Patched JSON files, updated TypeScript interfaces, new getFeaturedBrief() function, and a working app/page.tsx shell that imports all 6 section components.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/lib/types.ts
@app/lib/content.ts
@content/briefs-index.json
@content/site.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch JSON schemas — add featured flag and stats array</name>
  <files>content/briefs-index.json, content/site.json</files>
  <action>
    In content/briefs-index.json: Add `"featured": true` to the week-03 entry (slug: "week-03-predictive-analytics-amr-burden") only. No other brief gets the featured flag.

    In content/site.json: Add a `"stats"` array after the `"footerTagline"` field. Use this exact data:
    ```json
    "stats": [
      { "value": "700,000", "label": "deaths per year attributed to AMR globally" },
      { "value": "4.95M", "label": "deaths associated with AMR in 2019" },
      { "value": "10M", "label": "projected AMR deaths annually by 2050" }
    ]
    ```
    Both changes are additive — no existing fields are removed or renamed.
  </action>
  <verify>
    Run: `node -e "const b = require('./content/briefs-index.json'); console.log(b.filter(x=>x.featured).map(x=>x.slug))"`
    Expected output: `[ 'week-03-predictive-analytics-amr-burden' ]`

    Run: `node -e "const s = require('./content/site.json'); console.log(s.stats.length)"`
    Expected output: `3`
  </verify>
  <done>briefs-index.json has exactly one entry with featured: true (week-03); site.json has a stats array with 3 entries.</done>
</task>

<task type="auto">
  <name>Task 2: Update TypeScript interfaces and add getFeaturedBrief()</name>
  <files>app/lib/types.ts, app/lib/content.ts</files>
  <action>
    In app/lib/types.ts:
    - Add `featured?: boolean;` to the Brief interface after the `themes` field. Mark it optional (?) because only one brief has it; all others will have it undefined.
    - Add `stats: Array<{ value: string; label: string; }>;` to the SiteContent interface after the `footerTagline` field.

    In app/lib/content.ts:
    - Add a new exported function below the existing functions:
    ```typescript
    export function getFeaturedBrief(): Brief | undefined {
      return getAllBriefs().find((b) => b.featured === true);
    }
    ```
    Do NOT modify any existing functions. Append only.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Expected: exits with no errors (exit code 0).
  </verify>
  <done>TypeScript compiles cleanly; Brief interface has optional featured field; SiteContent has stats array; getFeaturedBrief() is exported from content.ts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire app/page.tsx as homepage orchestrator</name>
  <files>app/page.tsx</files>
  <action>
    Replace the current placeholder content in app/page.tsx with a Server Component that:
    1. Imports getSiteContent and getFeaturedBrief from '@/lib/content'
    2. Imports all 6 section components (they will not exist yet — use forward-referenced imports; TypeScript will not error at this stage if the files are created before build):
       - HeroSection from '@/components/sections/HeroSection'
       - StatStrip from '@/components/sections/StatStrip'
       - ThreePillars from '@/components/sections/ThreePillars'
       - FeaturedBrief from '@/components/sections/FeaturedBrief'
       - PartnerLogos from '@/components/sections/PartnerLogos'
       - NewsletterSignup from '@/components/sections/NewsletterSignup'
    3. Reads data at build time: `const site = getSiteContent(); const featured = getFeaturedBrief();`
    4. Returns JSX rendering sections in this exact order:
       ```tsx
       <>
         <HeroSection conferenceDate={site.conferenceDate} />
         <StatStrip stats={site.stats} />
         <ThreePillars />
         {featured && <FeaturedBrief brief={featured} />}
         <PartnerLogos partners={site.partners} />
         <NewsletterSignup />
       </>
     ```
    No 'use client' directive — this is a Server Component.

    NOTE: Plans 02-02 and 02-03 will create the section component files. The import references in page.tsx are intentional placeholders that complete when those plans execute.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    TypeScript will report errors for missing section module files — this is EXPECTED at this stage (the files don't exist yet). Verify that the ONLY errors are "Cannot find module '@/components/sections/...'" — no type errors on the data calls (getSiteContent, getFeaturedBrief, site.stats, site.conferenceDate, site.partners).
  </verify>
  <done>app/page.tsx is a Server Component that reads data and renders 6 section imports in the correct order. The only TypeScript errors are missing module files (resolved by plans 02-02 and 02-03).</done>
</task>

</tasks>

<verification>
After all three tasks:
1. `node -e "const b=require('./content/briefs-index.json');console.log(b.find(x=>x.featured)?.slug)"` prints `week-03-predictive-analytics-amr-burden`
2. `node -e "const s=require('./content/site.json');console.log(s.stats.length)"` prints `3`
3. `npx tsc --noEmit` shows ONLY "Cannot find module" errors for section files — no data type errors
</verification>

<success_criteria>
- briefs-index.json: week-03 has featured: true, others do not
- site.json: stats array with 3 AMR stat objects
- types.ts: Brief.featured is optional boolean; SiteContent.stats is typed array
- content.ts: getFeaturedBrief() exported and working
- page.tsx: Server Component, reads site + featured data, renders 6 section components in correct order
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-and-design-system/02-01-SUMMARY.md`
</output>
