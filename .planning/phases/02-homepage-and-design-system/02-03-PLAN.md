---
phase: 02-homepage-and-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/components/sections/FeaturedBrief.tsx
  - app/components/sections/PartnerLogos.tsx
autonomous: true
requirements:
  - HOME-02
  - HOME-03

must_haves:
  truths:
    - "Featured Brief section shows title, key messages bullet list, and Download PDF button"
    - "Download PDF button is a native anchor with download attribute pointing to the brief's pdfUrl"
    - "Partner Logos strip shows all 4 partner logos in a horizontal row (wrapping on mobile)"
    - "Partner logos are full colour and each is a clickable link to the partner website"
  artifacts:
    - path: "app/components/sections/FeaturedBrief.tsx"
      provides: "Featured brief display with key messages and PDF download"
      contains: "download"
    - path: "app/components/sections/PartnerLogos.tsx"
      provides: "Partner logos strip with 4 logos"
      contains: "flex-wrap"
  key_links:
    - from: "app/components/sections/FeaturedBrief.tsx"
      to: "app/lib/types.ts"
      via: "Brief type import"
      pattern: "import.*Brief.*types"
    - from: "app/components/sections/PartnerLogos.tsx"
      to: "app/lib/types.ts"
      via: "SiteContent['partners'] type"
      pattern: "SiteContent"
---

<objective>
Build the Featured Brief section and Partner Logos strip — the lower-middle sections of the homepage.

Purpose: Featured Brief drives PDF downloads (the core action on the site). Partner Logos establishes institutional credibility. Both are pure Server Components reading data passed as props.
Output: FeaturedBrief.tsx and PartnerLogos.tsx — Server Components ready for page.tsx.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage-and-design-system/02-01-SUMMARY.md
@app/lib/types.ts
@app/components/layout/Container.tsx
@content/briefs-index.json
@content/site.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FeaturedBrief section</name>
  <files>app/components/sections/FeaturedBrief.tsx</files>
  <action>
    Create app/components/sections/FeaturedBrief.tsx as a Server Component (no 'use client'):
    - Import Container from '@/components/layout/Container'
    - Import Link from 'next/link'
    - Import Download from 'lucide-react'
    - Import type Brief from '@/lib/types'
    - Props: `{ brief: Brief }`
    - Outer section: `className="bg-slate-100 py-16"` (alternating grey background)
    - Inside Container:
      1. Label: `<p className="text-xs uppercase tracking-wider text-teal-600 font-medium mb-2">Featured Policy Brief</p>`
      2. Title: `<h2 className="font-serif text-2xl md:text-3xl text-navy-950 mb-6">{brief.title}</h2>`
      3. Key messages list: `<ul className="space-y-3 mb-8">` — map over brief.keyMessages:
         ```tsx
         <li key={i} className="flex gap-3 text-slate-600">
           <span className="text-teal-600 flex-shrink-0 mt-0.5 font-bold">—</span>
           <span>{msg}</span>
         </li>
         ```
         Use index i as key (array order is stable and messages have no unique id).
      4. Download PDF button: native `<a>` tag (NOT Link — downloads are not Next.js routes):
         ```tsx
         <a
           href={brief.pdfUrl}
           download
           className="inline-flex items-center gap-2 bg-teal-600 hover:bg-teal-500 text-white font-medium px-6 py-3 rounded transition-colors"
         >
           <Download size={18} />
           Download PDF
         </a>
         ```
    - Does NOT show executiveSummary — locked decision is key messages only for scannability.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Check: No TypeScript errors in FeaturedBrief.tsx. Confirm the `download` attribute is on an `<a>` tag (not a Link component).
  </verify>
  <done>FeaturedBrief.tsx created: bg-slate-100 section, title in serif font, key messages as dash-prefixed list, Download PDF anchor with download attribute. No executive summary shown.</done>
</task>

<task type="auto">
  <name>Task 2: Build PartnerLogos section</name>
  <files>app/components/sections/PartnerLogos.tsx</files>
  <action>
    Create app/components/sections/PartnerLogos.tsx as a Server Component (no 'use client'):
    - Import Container from '@/components/layout/Container'
    - Import type SiteContent from '@/lib/types'
    - Props: `{ partners: SiteContent['partners'] }`
    - Outer section: `className="bg-white py-12"` (alternating white background)
    - Inside Container:
      1. Label: `<p className="text-xs uppercase tracking-wider text-slate-400 font-medium text-center mb-8">Partners</p>`
      2. Logos row: `<div className="flex flex-wrap items-center justify-center gap-8 md:gap-12">`
      3. Each partner is a linked logo (use `<a>` not Link — external URLs):
         ```tsx
         <a
           key={p.name}
           href={p.url}
           target="_blank"
           rel="noopener noreferrer"
           className="opacity-80 hover:opacity-100 transition-opacity"
         >
           <img
             src={p.logoUrl}
             alt={p.name}
             className="h-10 w-auto object-contain"
           />
         </a>
         ```
    - Use plain `<img>` tag (not next/image) — this is a static export with images.unoptimized:true; plain img avoids Next.js image wrapper overhead for small logo images.
    - Mobile behaviour (Claude's Discretion): flex-wrap allows logos to reflow naturally on narrow viewports. This is preferred over horizontal scroll.
    - Partner logo files (PNG) are placeholders in public/images/partners/ — they will show broken image icons until actual logos are added. This is acceptable at build time.
    - NOTE: p.url may be undefined for some partners (optional field in type). Wrap with conditional: `href={p.url ?? '#'}`.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Check: No TypeScript errors in PartnerLogos.tsx.

    Then run the full build: `npm run build`
    Expected: Build succeeds with a complete static export. The `out/` directory is created. At this point plans 02-02 and 02-03 are both complete — all 6 section component files exist — so the build should fully succeed.
  </verify>
  <done>PartnerLogos.tsx created: bg-white section, 4 partner logos as linked images in flex-wrap row, plain img tags, 80% opacity with hover to 100%. Build succeeds after both plan 02-02 and 02-03 tasks complete.</done>
</task>

</tasks>

<verification>
After both tasks complete (and after plan 02-02 also completes):
1. `npx tsc --noEmit` exits with no errors
2. `npm run build` succeeds — out/ directory created
3. Check out/index.html exists and is non-empty
4. Grep for "Featured Policy Brief" in out/index.html — confirms FeaturedBrief rendered at build time
5. Grep for "Partners" in out/index.html — confirms PartnerLogos rendered
</verification>

<success_criteria>
- FeaturedBrief.tsx: Server Component, bg-slate-100, title + key messages list (no executive summary), Download PDF anchor with download attribute
- PartnerLogos.tsx: Server Component, bg-white, flex-wrap logos row, plain img tags, all 4 partners linked to external URLs
- Full next build succeeds with no errors once both plan 02-02 and 02-03 tasks are done
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-and-design-system/02-03-SUMMARY.md`
</output>
