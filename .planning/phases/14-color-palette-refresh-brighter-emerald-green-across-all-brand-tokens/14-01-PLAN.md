---
phase: 14-color-palette-refresh-brighter-emerald-green-across-all-brand-tokens
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements: []

must_haves:
  truths:
    - "The @theme block in app/globals.css defines a complete teal scale from teal-50 through teal-900"
    - "teal-600 is exactly #319974 (user-pinned, not the nearest emerald stop)"
    - "teal-700 is added as a new token (was missing from the prior @theme block)"
    - "teal-100, teal-200, teal-900 are added as new tokens (were missing and actively used)"
    - "All other teal stops use the corresponding Tailwind emerald values mapped to teal- names"
    - "A WCAG waiver comment is present in the @theme block documenting the deliberate contrast decision"
    - "next build completes without errors after the token changes"
  artifacts:
    - path: "app/globals.css"
      provides: "Full teal token scale including all 10 stops (teal-50 through teal-900)"
      contains: "--color-teal-600: #319974"
  key_links:
    - from: "app/globals.css @theme block"
      to: "all teal- Tailwind classes site-wide"
      via: "Tailwind v4 @theme CSS variable resolution"
      pattern: "--color-teal-\\d+"
---

<objective>
Replace the Phase 6 teal token values in app/globals.css with the new brighter emerald green palette, completing the full teal scale (teal-50 through teal-900) with pre-calculated exact hex values.

Purpose: The current teal-600 (#0A7050) was deliberately darkened for WCAG AA compliance in Phase 6. Phase 14 consciously waives WCAG AA in favor of brand vibrancy, moving to #319974 per user decision. Research confirmed this is a single-file edit — all green references in components use teal-* classes, zero hardcoded hex values anywhere.

Output: Updated app/globals.css with a complete 10-stop teal scale + WCAG waiver comment, verified by a passing next build.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild teal token scale in @theme block</name>
  <files>app/globals.css</files>
  <action>
    Replace the existing teal-* token lines in the @theme block (lines 10–14 of current globals.css) with the complete 10-stop teal scale. Do NOT touch navy-*, amr-gold, slate-*, or font tokens — those are unchanged.

    Replace the comment on line 3 to reflect the new palette accurately. The existing comment says "AMR Logo_Feb2026.jpeg: Green #0A7050" — update to say "#319974" (the new primary green) and note WCAG waiver.

    The exact replacement for the teal section of @theme (between the "Primary palette" comment and the "Neutral palette" comment) must be:

    ```css
    /* Primary palette — AMR Brand (Emerald Green / Gold / Dark Green) */
    /* WCAG NOTE: teal-600 (#319974) on white = ~3.5:1; white on teal-600 = ~4.1:1.
       WCAG AA compliance is deliberately waived in favor of brand vibrancy.
       teal-600 is the user-pinned brand green; all other stops map to Tailwind emerald scale. */
    --color-navy-950: #1A3A2A;      /* AMR dark green — primary dark background */
    --color-navy-900: #1F4A35;      /* AMR green — card backgrounds, secondary dark */
    --color-navy-800: #2D5A42;      /* AMR green lighter — borders, dividers */
    --color-teal-50:  #ecfdf5;      /* Tailwind emerald-50 — hover backgrounds */
    --color-teal-100: #d1fae5;      /* Tailwind emerald-100 — education page tints */
    --color-teal-200: #a7f3d0;      /* Tailwind emerald-200 — CommentList/CommentForm borders */
    --color-teal-300: #6ee7b7;      /* Tailwind emerald-300 — highlights */
    --color-teal-400: #34d399;      /* Tailwind emerald-400 — accent highlights */
    --color-teal-500: #10b981;      /* Tailwind emerald-500 — hover states */
    --color-teal-600: #319974;      /* AMR Brand Green — CTAs, accent, links (WCAG AA waived) */
    --color-teal-700: #2A8263;      /* Derived: darken #319974 ~15% via HSL — hover/focus states */
    --color-teal-800: #065f46;      /* Tailwind emerald-800 */
    --color-teal-900: #064e3b;      /* Tailwind emerald-900 — NewsCard hover */
    --color-amr-gold: #F2A900;      /* AMR Gold — accent uses */
    ```

    Also update the top comment block (lines 3-4) to read:
    ```css
    /* AMR Brand Tokens — Emerald Green / Gold / Slate */
    /* Phase 14: primary green refreshed to #319974 (brighter emerald). WCAG AA waived for brand vibrancy. */
    ```

    No other changes. No component files. No other CSS sections.
  </action>
  <verify>
    1. Open app/globals.css and confirm --color-teal-600: #319974 is present
    2. Confirm --color-teal-100, --color-teal-200, --color-teal-700, --color-teal-900 are all present (these were missing before)
    3. Confirm the WCAG waiver comment block is present above the teal token lines
    4. Confirm --color-amr-gold: #F2A900 is unchanged
    5. Confirm --color-navy-950, --color-navy-900, --color-navy-800 are unchanged
  </verify>
  <done>
    app/globals.css @theme block contains all 10 teal stops (teal-50 through teal-900) with teal-600 = #319974, plus WCAG waiver comment. Navy and gold tokens unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify next build passes with new token scale</name>
  <files></files>
  <action>
    Run next build to confirm zero build errors after the token changes. The build must produce a full static export. No code changes — this is a pure verification step.

    Command: npm run build

    Expected: Build completes with "Generating static pages (11/11)" (or similar — all routes). Zero TypeScript errors, zero Tailwind errors, zero Next.js errors.

    If the build fails: Read the error output carefully. Most likely causes:
    - A Tailwind class referencing a teal-* stop that wasn't added → add the missing stop to @theme
    - Syntax error in the CSS edit → fix the @theme block formatting

    Do NOT proceed to summary until the build passes cleanly.
  </action>
  <verify>
    npm run build exits with code 0. Terminal output shows all routes generated (/, /briefs, /briefs/[slug], /awareness, /news, /take-action, /experts, /conference, /education, /contact, /methodology).
  </verify>
  <done>
    next build exits 0 with all 11 routes statically generated. No errors.
  </done>
</task>

</tasks>

<verification>
1. `grep -n "teal-600" app/globals.css` shows `--color-teal-600: #319974`
2. `grep -c "color-teal-" app/globals.css` shows 10 (one per stop: 50, 100, 200, 300, 400, 500, 600, 700, 800, 900)
3. `grep "WCAG" app/globals.css` shows the waiver comment
4. `npm run build` exits 0
</verification>

<success_criteria>
- app/globals.css contains complete teal scale (10 stops: teal-50 through teal-900)
- teal-600 = #319974 exactly (user-pinned brand green)
- teal-700 = #2A8263 (derived hover state)
- teal-100, teal-200, teal-900 present (were missing, actively used by components)
- WCAG waiver comment present and clear
- navy-* and amr-gold tokens unchanged
- next build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-color-palette-refresh-brighter-emerald-green-across-all-brand-tokens/14-01-SUMMARY.md`
</output>
