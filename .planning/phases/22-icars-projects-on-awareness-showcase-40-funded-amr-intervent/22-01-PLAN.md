---
phase: 22-icars-projects-on-awareness-showcase-40-funded-amr-intervent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/tools/extract_icars_projects.py
  - content/icars-projects.json
  - app/lib/types.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running the extraction script prints exactly 25 (the project count, not 40 rows)"
    - "content/icars-projects.json exists with 25 typed IcarsProject records"
    - "Every project record carries a non-empty country derived from its country-header row"
    - "Sector values are normalized to the 5-value IcarsSector union (no 'Enviroment' typo)"
    - "TypeScript IcarsProject interface and IcarsSector union exist in app/lib/types.ts"
  artifacts:
    - path: "resources/tools/extract_icars_projects.py"
      provides: "Build-time xlsx->JSON extraction with country carry-down and sector normalization"
      contains: "def is_country_header"
    - path: "content/icars-projects.json"
      provides: "25 IcarsProject records"
      contains: "\"country\""
    - path: "app/lib/types.ts"
      provides: "IcarsSector union + IcarsProject interface"
      contains: "export interface IcarsProject"
  key_links:
    - from: "resources/tools/extract_icars_projects.py"
      to: "content/icars-projects.json"
      via: "json.dump build-time write"
      pattern: "icars-projects\\.json"
    - from: "content/icars-projects.json"
      to: "app/lib/types.ts IcarsProject"
      via: "field names match interface"
      pattern: "fundingPartners"
---

<objective>
Extract the 25 ICARS-funded AMR intervention projects from the source xlsx into a typed `content/icars-projects.json`, and define the `IcarsProject` interface + `IcarsSector` union in `app/lib/types.ts`.

Purpose: Establishes the build-time data layer that the awareness-page section (Plan 22-02) renders. The data model is non-trivial because the source xlsx interleaves country-header rows with project rows (country must be carried down) and has dirty sector values.
Output: `extract_icars_projects.py` (committed extraction script), `content/icars-projects.json` (25 records), appended types in `app/lib/types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-RESEARCH.md
@.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write extract_icars_projects.py with country carry-down + sector normalization</name>
  <files>resources/tools/extract_icars_projects.py</files>
  <read_first>
    - resources/tools/extract_oh_tools.py (the exact extraction idiom to transplant: clean(), slugify(), md5-suffix slug dedup, __main__ count gate, UTF-8 json.dump with ensure_ascii=False/indent=2/trailing newline)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-RESEARCH.md (Code Examples section lines 266-343 has the full script skeleton; Common Pitfalls 1-7 describe every data hazard)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-PATTERNS.md (extraction-script idiom block)
  </read_first>
  <action>
    Create resources/tools/extract_icars_projects.py by transplanting the scaffolding from extract_oh_tools.py (clean, slugify, md5-suffix dedup, json.dump). Set XLSX_PATH to `Path(__file__).parent / "International Center for Antimicrobial Resistance Solutions.xlsx"` and OUTPUT_PATH to `Path(__file__).parent.parent.parent / "content" / "icars-projects.json"`. Load the workbook with `data_only=True` and select sheet "Sheet1".

    Define SECTOR_CANON (lowercased keys) mapping: "humans"->"Humans", "one health"->"One Health", "terrestrial and aquatic animals"->"Terrestrial and Aquatic Animals", "food and feed"->"Food and Feed", "enviroment"->"Environment" (source typo), "environment"->"Environment". Define KNOWN_SECTORS as the set of SECTOR_CANON keys.

    Add `split_outcomes(v)` that returns a list[str]: split the cell on "\n", run each line through whitespace collapse, drop empties (preserves the numbered-list structure per Pitfall 7).

    Add `is_country_header(c1, c2, c3, c5)` returning True when the candidate token (c1 or c2) is non-empty, c3 and c5 are both empty, the token equals its own uppercase form, AND the token's lowercase form is NOT in KNOWN_SECTORS. This detects country-header rows and handles TUNISIA being mis-placed in C2 instead of C1 (Pitfall 3 — check BOTH C1 and C2).

    In build_items(): iterate `range(2, ws.max_row + 1)`. Maintain `current_country = None`. For each row read c1..c6 via ws.cell(r, n).value. If is_country_header matches, set current_country to the stripped token (c1 or c2) and `continue`. Otherwise compute `title = clean(c2)`; if title is empty, `continue` (skips blank rows). Derive `raw_sector = clean(c1).lower()` and `sector = SECTOR_CANON.get(raw_sector)` so a blank/unknown sector resolves to None (Pitfall 5 — ZAMBIA row 18 has no sector). Build each record dict with keys exactly: id (slug from title with md5-suffix dedup, base slug truncated to [:60]), country (current_country), sector (the normalized value or None), title, description (clean of C3), outcomes (split_outcomes of C4), fundingPartners (clean of C5), fundingAmount (clean of C6 — header mislabels it "IMPLEMENTING PARTNERS" but cells hold funding amounts per Pitfall / note at RESEARCH line 340). Do NOT crash on mojibake characters in funder text (Pitfall 6 — openpyxl returns them as-is; leave them).

    In `if __name__ == "__main__":` call build_items(), `print(len(items))` (this is the count gate — it MUST print 25, NOT 40, per Pitfall 2), then json.dump to OUTPUT_PATH with `ensure_ascii=False, indent=2` followed by a trailing newline. Do NOT place fenced code in this plan; follow the cited skeleton.
  </action>
  <verify>
    <automated>python resources/tools/extract_icars_projects.py</automated>
  </verify>
  <acceptance_criteria>
    - `python resources/tools/extract_icars_projects.py` prints exactly `25` to stdout and exits 0 (invoke with `python`, NOT `python3`, on this Windows machine)
    - resources/tools/extract_icars_projects.py contains `def is_country_header`
    - resources/tools/extract_icars_projects.py contains `SECTOR_CANON` with both `"enviroment"` and `"environment"` keys mapping to `"Environment"`
    - resources/tools/extract_icars_projects.py contains `def split_outcomes`
    - The script reads sheet `"Sheet1"` and iterates `range(2, ws.max_row + 1)`
  </acceptance_criteria>
  <done>Script runs clean, prints 25, and writes content/icars-projects.json.</done>
</task>

<task type="auto">
  <name>Task 2: Verify generated JSON and append IcarsProject types to app/lib/types.ts</name>
  <files>content/icars-projects.json, app/lib/types.ts</files>
  <read_first>
    - content/icars-projects.json (the file Task 1 generated — inspect the actual records to confirm 25 entries, non-empty country on each, no "Enviroment" typo, and that the ENVIRE/TUNISIA row 41 has the correct country not "CROSS-COUNTRY")
    - app/lib/types.ts (lines 120-153 — the Phase 21 OHScope/ToolItem block whose structure must be mirrored: phase comment, string-literal union per categorical field, interface with inline // Cn source-column comments. Append AFTER this block; do not modify it)
    - .planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-RESEARCH.md (IcarsProject type block lines 345-366)
  </read_first>
  <action>
    Open content/icars-projects.json (produced by Task 1) and confirm it contains 25 records, every record has a non-empty `country` string, no `sector` value equals the literal "Enviroment", and exactly one record has `sector: null` (the ZAMBIA row, Pitfall 5). If the count is not 25 or a country is empty/garbage (e.g. "Humans" appearing as a country, or the ENVIRE row showing the wrong country per Pitfall 3), fix the extraction logic in Task 1's script and re-run before proceeding.

    Then append to the END of app/lib/types.ts (do NOT touch the existing Phase 21 block): a `// Phase 22: ICARS-funded AMR interventions` comment, the `IcarsSector` union with exactly the 5 normalized literals `'Humans' | 'One Health' | 'Terrestrial and Aquatic Animals' | 'Food and Feed' | 'Environment'`, and the `IcarsProject` interface with fields: `id: string` (slug from title), `country: string` (carried down from country-header row), `sector?: IcarsSector` (optional — one row has no sector), `title: string` (C2), `description: string` (C3), `outcomes: string[]` (C4 split on newline), `fundingPartners: string` (C5), `fundingAmount?: string` (C6, messy mixed-currency string; header mislabeled). Use `export type` and `export interface`, mirroring the Phase 21 block's comment style with inline `// Cn` annotations.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/types.ts contains `export type IcarsSector =`
    - app/lib/types.ts contains `export interface IcarsProject`
    - app/lib/types.ts contains `sector?: IcarsSector` (optional sector field)
    - app/lib/types.ts contains `outcomes: string[]`
    - `npm run lint` exits 0 (no new lint/type errors)
    - content/icars-projects.json parses as a 25-element array (e.g. `node -e "const a=require('./content/icars-projects.json'); if(a.length!==25) process.exit(1)"` exits 0)
    - No object in content/icars-projects.json has a `sector` equal to `"Enviroment"`
  </acceptance_criteria>
  <done>Types appended without modifying Phase 21 block; lint passes; JSON verified at 25 records with normalized sectors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| xlsx file -> JSON | Untrusted/dirty source-data text (funder names, descriptions, outcomes) enters the build artifact here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | extract_icars_projects.py output | accept | Source xlsx is a trusted local file committed by maintainers; extraction is a one-shot build step, not runtime user input. Mojibake chars (Pitfall 6) are left as-is and flagged for the 22-02 visual checkpoint |
| T-22-02 | Information Disclosure | fundingAmount field | accept | Funding amounts are extracted into JSON but display is deferred to planner/user (RESEARCH Open Q1, default NOT shown). No secrets; all data is public-facing intervention metadata |
| T-22-SC | Tampering | python/npm installs | mitigate | No new packages installed this phase (RESEARCH Package Legitimacy Audit: zero installs). openpyxl 3.1.5 already present. slopcheck moot |
</threat_model>

<verification>
- `python resources/tools/extract_icars_projects.py` prints 25
- `npm run lint` exits 0
- content/icars-projects.json is a 25-element array with normalized sectors and non-empty countries
</verification>

<success_criteria>
- 25 IcarsProject records extracted with country carry-down and 5 normalized sectors
- IcarsProject interface + IcarsSector union defined in app/lib/types.ts without disturbing Phase 21 types
- Extraction script committed for provenance and re-runnable
</success_criteria>

<output>
Create `.planning/phases/22-icars-projects-on-awareness-showcase-40-funded-amr-intervent/22-01-SUMMARY.md` when done
</output>
