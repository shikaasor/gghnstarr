---
phase: 05-seo-performance-launch-readiness
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified: []
autonomous: false
requirements:
  - SEO-03

must_haves:
  truths:
    - "next build completes with zero errors or warnings"
    - "Lighthouse mobile performance score is 85 or above on the deployed Vercel site"
    - "LCP on simulated 3G is under 2.5 seconds"
    - "Total page weight (transferred bytes) is under 500KB on a representative page"
    - "All phase success criteria are confirmed before the Inter-Ministerial Conference deadline"
  artifacts:
    - path: "out/"
      provides: "Static export directory produced by next build"
      contains: "sitemap.xml, robots.txt, index.html"
  key_links:
    - from: "Lighthouse audit"
      to: "app/layout.tsx"
      via: "Google Fonts removal (05-01) is the primary LCP improvement being verified"
      pattern: "n/a — human verification"
    - from: "WhatsApp link preview"
      to: "app/briefs/[slug]/page.tsx"
      via: "OG meta tags (05-01) tested by sharing a brief URL in WhatsApp"
      pattern: "n/a — human verification"
---

<objective>
Run next build to verify the complete site compiles cleanly after all Phase 5 changes. Then verify all four phase success criteria: rich WhatsApp previews, sitemap/robots.txt inclusion, Lighthouse mobile score, and print output.

Purpose: Production confidence checkpoint before the Inter-Ministerial Conference (June 28, 2026). Confirms all SEO and performance work is effective on the live Vercel deployment.

Output: Verified production build; human-confirmed Lighthouse score and WhatsApp preview; phase marked complete.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-seo-performance-launch-readiness/05-01-SUMMARY.md
@.planning/phases/05-seo-performance-launch-readiness/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run next build and verify static output</name>
  <files></files>
  <action>
    Run the production build and verify the static output is correct.

    Run: `npm run build`

    If the build fails, diagnose and fix before proceeding. Common failure modes from RESEARCH.md:
    - TypeScript error about missing `playfair` or `inter` — means Task 1 of 05-01 has a leftover reference; remove it
    - ENOENT in sitemap.ts — means getAllBriefs() path is wrong; check that lib/content.ts uses process.cwd() correctly
    - metadataBase URL error — means NEXT_PUBLIC_SITE_URL is malformed in .env.local; verify it starts with https://

    After a successful build, verify the static output:

    Run: `ls out/sitemap.xml out/robots.txt`
    Expected: Both files listed.

    Run: `cat out/sitemap.xml`
    Expected: XML containing:
    - `<loc>https://gghnstarr.vercel.app</loc>` (homepage)
    - `<loc>https://gghnstarr.vercel.app/briefs</loc>`
    - `<loc>https://gghnstarr.vercel.app/briefs/week-01-amr-governance-frameworks</loc>` (or similar slug)
    - At minimum 8 total URL entries (5 static pages + 3 briefs)

    Run: `cat out/robots.txt`
    Expected: Contains "User-agent: *", "Allow: /", and a Sitemap line.

    Run: `grep -r "next/font/google" app/`
    Expected: No output — confirms Google Fonts are fully removed from all app files.

    Report the full output of `npm run build` in the task log so the checkpoint human-verify has context.
  </action>
  <verify>
    `npm run build` exits with code 0.
    `ls out/sitemap.xml out/robots.txt` shows both files.
    `cat out/sitemap.xml` contains at least 8 loc entries including brief detail URLs.
    `cat out/robots.txt` is non-empty and contains the sitemap URL.
  </verify>
  <done>
    next build completes with exit code 0. out/ directory exists with sitemap.xml and robots.txt. sitemap.xml contains all expected URLs. robots.txt is valid.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all Phase 5 success criteria</name>
  <files></files>
  <action>Human verifies all four SEO and performance success criteria on the Vercel deployment.</action>
  <verify>All four checks pass: WhatsApp preview, sitemap/robots.txt, Lighthouse score, print output.</verify>
  <done>Human types "approved" confirming all four checks pass.</done>
  <what-built>
    Phase 5 SEO and performance work:
    - 05-01: Google Fonts removed from layout.tsx; metadataBase added; OG/Twitter meta tags on brief detail pages; .no-print classes on nav and download buttons
    - 05-02: app/sitemap.ts and app/robots.ts (generating sitemap.xml and robots.txt); @media print block in globals.css
    - 05-03 Task 1: next build completed, output verified

    This is the final verification before the site is considered launch-ready for the June 28, 2026 Inter-Ministerial Conference.
  </what-built>
  <how-to-verify>
    Complete ALL four checks. Report results for each.

    ---

    CHECK 1 — WhatsApp Rich Preview (SEO-01)
    Prerequisites: Push to main branch so Vercel deploys the latest build.

    Option A (WhatsApp):
    1. Open WhatsApp on your phone
    2. Send yourself the URL: https://gghnstarr.vercel.app/briefs/week-01-amr-governance-frameworks
    3. Expected: WhatsApp shows a preview card with the brief title, key takeaway as description, and the thumbnail image (641x360px)
    4. If no image appears: wait 2-3 minutes for WhatsApp to re-fetch, or share with a second contact

    Option B (Facebook OG Debugger — faster):
    1. Visit https://developers.facebook.com/tools/debug/
    2. Paste the URL: https://gghnstarr.vercel.app/briefs/week-01-amr-governance-frameworks
    3. Click "Debug" then "Scrape Again"
    4. Expected: Shows og:title (brief title), og:description (key takeaway), og:image (thumbnail URL), og:image:width: 641

    PASS if: Brief title, description, and thumbnail image all appear in preview.
    FAIL if: No image shown, or wrong title/description, or OG debugger shows errors.

    ---

    CHECK 2 — Sitemap and robots.txt (SEO-02)
    1. Visit: https://gghnstarr.vercel.app/sitemap.xml
       Expected: XML file with at least 8 URLs including /briefs/week-01-amr-governance-frameworks (or current slugs)
    2. Visit: https://gghnstarr.vercel.app/robots.txt
       Expected: Plain text with "User-agent: *", "Allow: /", and "Sitemap: https://gghnstarr.vercel.app/sitemap.xml"

    PASS if: Both URLs return correct content in the browser.
    FAIL if: 404 error, XML parsing errors, or brief URLs missing from sitemap.

    ---

    CHECK 3 — Lighthouse Mobile Performance (SEO-03)
    Run Lighthouse against the deployed Vercel URL (NOT localhost):

    In Chrome:
    1. Open Chrome DevTools (F12) → Lighthouse tab
    2. Set: Mode = Navigation, Device = Mobile
    3. Run audit on: https://gghnstarr.vercel.app/briefs/week-01-amr-governance-frameworks
    4. After audit completes, note:
       - Performance score (target: ≥85)
       - LCP value (target: <2.5s)
       - Total page weight from Network tab (target: <500KB transferred)

    Report the scores even if they don't meet target — the executor will diagnose and fix if needed.

    PASS if: Performance ≥85 AND LCP <2.5s AND page weight <500KB transferred.
    FAIL if: Any metric misses its target.

    NOTE: If score is 80-84 (close but not 85), check the "Opportunities" section — the most common remaining issue after font removal is render-blocking resources. Report the top 3 Lighthouse recommendations so the executor can address them if needed.

    ---

    CHECK 4 — Print Output (SEO-04)
    1. Visit: https://gghnstarr.vercel.app/briefs/week-01-amr-governance-frameworks (or local dev: http://localhost:3000/briefs/week-01-amr-governance-frameworks)
    2. Open print dialog: Ctrl+P (Windows) or Cmd+P (Mac)
    3. Look at the print preview. Expected:
       - NO site header/navigation bar
       - NO site footer
       - NO "Download PDF" or "Download Infographic" buttons
       - NO prev/next navigation links
       - YES: Brief title, author, date
       - YES: Executive Summary text
       - YES: Key Messages bullet list
       - YES: Author bio excerpt
       - YES: Thumbnail image (small, constrained to ~180px)
    4. Cancel the print dialog (no need to actually print)

    PASS if: Print preview shows clean content without chrome elements.
    FAIL if: Header, footer, download buttons, or nav visible in print preview.

    ---

    ALSO: Confirm NEXT_PUBLIC_SITE_URL is set in Vercel environment variables:
    1. Visit Vercel dashboard → your gghnstarr project → Settings → Environment Variables
    2. Add NEXT_PUBLIC_SITE_URL = https://gghnstarr.vercel.app (use your actual Vercel subdomain)
    3. Trigger a redeploy after adding the variable

    ---

    Report format:
    - CHECK 1 (WhatsApp): PASS/FAIL — [what you saw]
    - CHECK 2 (Sitemap): PASS/FAIL — [what you saw]
    - CHECK 3 (Lighthouse): PASS/FAIL — Performance: [score], LCP: [value], Weight: [KB transferred]
    - CHECK 4 (Print): PASS/FAIL — [what you saw]
  </how-to-verify>
  <resume-signal>
    Type "approved" if all 4 checks pass.
    Or describe which checks failed and what you saw — the executor will diagnose and fix before re-running this checkpoint.

    If Lighthouse score is below 85, include the top 3 "Opportunities" from the Lighthouse report so the executor can address them.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 5 is complete when:
1. next build exits 0 with no errors
2. Human confirms WhatsApp/OG preview shows brief title + thumbnail
3. Human confirms sitemap.xml and robots.txt are accessible on Vercel
4. Human confirms Lighthouse mobile score ≥85, LCP <2.5s, weight <500KB
5. Human confirms print preview is clean (no nav/footer/buttons)
</verification>

<success_criteria>
All four phase success criteria confirmed by human:
1. Rich WhatsApp preview with brief title, description, and thumbnail
2. sitemap.xml and robots.txt accessible at Vercel deployment URL, including all brief detail pages
3. Lighthouse mobile Performance ≥85, LCP <2.5s, total page weight <500KB transferred
4. Browser print preview of brief detail page shows no header/footer/nav/download buttons

Site is launch-ready for the Inter-Ministerial Conference deadline (June 28, 2026).
</success_criteria>

<output>
After completion, create `.planning/phases/05-seo-performance-launch-readiness/05-03-SUMMARY.md`
</output>
