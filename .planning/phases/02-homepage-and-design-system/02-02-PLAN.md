---
phase: 02-homepage-and-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - app/components/sections/HeroSection.tsx
  - app/components/sections/ConferenceBadge.tsx
  - app/components/sections/StatStrip.tsx
  - app/components/sections/ThreePillars.tsx
autonomous: true
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "Hero section occupies full viewport height with white background"
    - "Conference badge shows live countdown days (or static fallback before hydration)"
    - "Stats strip auto-rotates through 3 AMR statistics on a 4-second timer"
    - "Three Pillars displays 3 columns with Dna, TrendingUp, Globe icons and bold titles"
    - "Three Pillars stacks to single column on mobile"
  artifacts:
    - path: "app/components/sections/HeroSection.tsx"
      provides: "100vh hero with headline, sub-headline, CTA button"
      contains: "min-h-screen"
    - path: "app/components/sections/ConferenceBadge.tsx"
      provides: "Client countdown timer component"
      contains: "'use client'"
    - path: "app/components/sections/StatStrip.tsx"
      provides: "Auto-rotating stats display"
      contains: "'use client'"
    - path: "app/components/sections/ThreePillars.tsx"
      provides: "3-column icon grid, mobile-responsive"
      contains: "grid-cols-1 md:grid-cols-3"
  key_links:
    - from: "app/components/sections/HeroSection.tsx"
      to: "app/components/sections/ConferenceBadge.tsx"
      via: "import and prop passing"
      pattern: "ConferenceBadge"
    - from: "app/components/sections/StatStrip.tsx"
      to: "app/page.tsx"
      via: "stats prop from getSiteContent()"
      pattern: "stats.*Stat\\[\\]"
---

<objective>
Build the Hero section (with live countdown badge), Stats Strip, and Three Pillars section — the top half of the homepage.

Purpose: These are the first things a visitor sees. The hero establishes institutional credibility and drives CTA clicks. The stats strip delivers immediate AMR urgency context. The pillars communicate the initiative's three areas of expertise.
Output: 4 component files: HeroSection.tsx, ConferenceBadge.tsx, StatStrip.tsx, ThreePillars.tsx — all ready to render from page.tsx.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-homepage-and-design-system/02-01-SUMMARY.md
@app/globals.css
@app/components/layout/Container.tsx
@app/lib/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build HeroSection and ConferenceBadge</name>
  <files>
    app/components/sections/HeroSection.tsx
    app/components/sections/ConferenceBadge.tsx
  </files>
  <action>
    Create app/components/sections/ConferenceBadge.tsx as a 'use client' component:
    - State: `const [daysLeft, setDaysLeft] = useState<number | null>(null)` — MUST initialize to null (not 0) to avoid hydration mismatch. Server renders null → renders static fallback text. Client mounts → useEffect computes and sets the number.
    - useEffect: compute days between now and new Date('2026-06-28T00:00:00'), use Math.ceil((target - now) / 86400000), clamp to Math.max(0, diff). Set an interval every 60_000ms to update. Clear interval on cleanup.
    - Render: When daysLeft is null, show static text "Road to the 5th Inter-Ministerial Conference • June 28, 2026". When daysLeft is a number, show `{daysLeft} days to June 28, 2026 — 5th Inter-Ministerial Conference`.
    - Style: `inline-flex items-center gap-2 bg-teal-600/10 border border-teal-600/20 text-teal-600 px-4 py-2 rounded-full text-sm font-medium`
    - Import CalendarDays from lucide-react for the icon (size={16}).
    - Props: `{ conferenceDate: string }` — accept the prop but use the hardcoded target date for the countdown (the prop is for display context only if needed).

    Create app/components/sections/HeroSection.tsx as a Server Component (no 'use client'):
    - Import ConferenceBadge from './ConferenceBadge'
    - Import Link from 'next/link'
    - Props: `{ conferenceDate: string }` — pass through to ConferenceBadge
    - Outer section: `className="bg-white min-h-screen flex flex-col items-center justify-center text-center px-4"`
    - Layout (top to bottom): ConferenceBadge → h1 → p → Link (CTA button)
    - h1: `className="font-serif text-4xl md:text-5xl lg:text-6xl text-teal-600 mt-6 max-w-3xl"` with text "AMR Policy Intelligence for Africa"
    - p: `className="text-slate-600 text-lg md:text-xl mt-4 max-w-2xl"` with text "Evidence-backed policy briefs from the GGHN STARR initiative — informing Africa's response to antimicrobial resistance"
    - Link href="/briefs": `className="mt-8 inline-flex items-center gap-2 bg-teal-600 hover:bg-teal-500 text-white font-medium px-6 py-3 rounded transition-colors"` — text "Read the Latest Policy Brief" with ArrowRight icon (size={18}) from lucide-react after the text.
    - No Container wrapper on hero — it's full-width with its own padding and centering.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Check: No TypeScript errors in these two files. The only remaining errors should be missing module files for sections not yet created (FeaturedBrief, PartnerLogos, NewsletterSignup).
  </verify>
  <done>HeroSection.tsx and ConferenceBadge.tsx created with no TS errors. ConferenceBadge initializes state to null (hydration-safe). HeroSection has min-h-screen, white bg, teal headline, CTA button linking to /briefs.</done>
</task>

<task type="auto">
  <name>Task 2: Build StatStrip and ThreePillars</name>
  <files>
    app/components/sections/StatStrip.tsx
    app/components/sections/ThreePillars.tsx
  </files>
  <action>
    Create app/components/sections/StatStrip.tsx as a 'use client' component:
    - Interface at top of file: `interface Stat { value: string; label: string; }`
    - Props: `{ stats: Stat[] }`
    - State: `const [index, setIndex] = useState(0)`
    - useEffect: if stats.length <= 1 return; setInterval every 4000ms advancing index by (i + 1) % stats.length; clear on cleanup; dependency array is [stats.length].
    - Guard: `const stat = stats[index]; if (!stat) return null;`
    - Outer section: `className="bg-slate-100 py-12 text-center"` (no Container — full-width strip)
    - Render: `<p className="text-4xl md:text-5xl font-serif font-bold text-teal-600">{stat.value}</p>` then `<p className="text-slate-600 mt-2 text-base md:text-lg">{stat.label}</p>`

    Create app/components/sections/ThreePillars.tsx as a Server Component (no 'use client'):
    - Import Container from '@/components/layout/Container'
    - Import Dna, TrendingUp, Globe from 'lucide-react'
    - Icons per locked decision: Dna for "Genomic Surveillance", TrendingUp for "Predictive Analytics", Globe for "One Health Governance"
    - Define pillars array as a const outside the component:
      ```typescript
      const pillars = [
        { icon: Dna, title: 'Genomic Surveillance' },
        { icon: TrendingUp, title: 'Predictive Analytics' },
        { icon: Globe, title: 'One Health Governance' },
      ] as const;
      ```
    - Outer section: `className="bg-white py-16"`
    - Inside Container: `<div className="grid grid-cols-1 md:grid-cols-3 gap-8 text-center">`
    - Each pillar: `<div className="flex flex-col items-center gap-4">` containing `<Icon size={48} className="text-teal-600" strokeWidth={1.5} />` and `<h3 className="font-serif font-bold text-xl text-navy-950">{title}</h3>`
    - No description text under titles — locked decision.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Check: No TypeScript errors in these two files. The only remaining errors should be missing module files for sections not yet created (FeaturedBrief, PartnerLogos, NewsletterSignup).

    Run: `npm run build`
    Check: Build succeeds — the missing section files will still cause errors; this verify is to confirm StatStrip and ThreePillars themselves have no build-time issues. If build fails ONLY due to missing section files from plans 02-03 and 02-04, that is expected.
  </verify>
  <done>StatStrip.tsx auto-rotates on 4s timer using useState/useEffect. ThreePillars.tsx renders 3-column grid (stacks to 1 col on mobile) with Dna, TrendingUp, Globe icons and bold titles only.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — errors ONLY for missing section module paths (FeaturedBrief, PartnerLogos, NewsletterSignup) — none in the 4 files created here
2. ConferenceBadge uses `useState<number | null>(null)` — confirm null initial value in source
3. StatStrip interval is 4000ms (4 seconds)
4. ThreePillars grid has `grid-cols-1 md:grid-cols-3` for mobile stacking
</verification>

<success_criteria>
- HeroSection: white bg, min-h-screen, teal headline, "Read the Latest Policy Brief" CTA linking to /briefs
- ConferenceBadge: 'use client', null initial state, 60s interval update, static fallback text when null
- StatStrip: 'use client', 4s rotation interval, bg-slate-100 strip
- ThreePillars: Server Component, 3 lucide icons (Dna/TrendingUp/Globe), responsive grid, no description text
</success_criteria>

<output>
After completion, create `.planning/phases/02-homepage-and-design-system/02-02-SUMMARY.md`
</output>
