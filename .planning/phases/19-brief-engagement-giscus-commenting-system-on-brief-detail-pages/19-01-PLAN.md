---
phase: 19-brief-engagement-giscus-commenting-system-on-brief-detail-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/comments.json
  - app/components/briefs/CommentForm.tsx
  - app/components/briefs/CommentList.tsx
  - app/lib/analytics.ts
autonomous: true
requirements:
  - BENG-01

must_haves:
  truths:
    - "A CommentForm React client component exists for anonymous GAS-backed comment submission"
    - "A CommentList React server component exists for rendering approved comments from static JSON"
    - "content/comments.json seed file exists to be populated by GitHub Actions"
    - "trackEvent generic helper exists in analytics.ts"
  artifacts:
    - path: "app/components/briefs/CommentForm.tsx"
      provides: "Client-side comment submission form using NEXT_PUBLIC_GAS_URL"
      exports: ["CommentForm"]
    - path: "app/components/briefs/CommentList.tsx"
      provides: "Server component rendering approved comments from content/comments.json"
      exports: ["CommentList"]
    - path: "content/comments.json"
      provides: "Seed empty object — populated by GitHub Actions fetch-comments cron"
      contains: "{}"
  key_links:
    - from: "app/components/briefs/CommentForm.tsx"
      to: "NEXT_PUBLIC_GAS_URL"
      via: "process.env.NEXT_PUBLIC_GAS_URL with formType: 'comment'"
      pattern: "GAS form routing"
    - from: "app/components/briefs/CommentList.tsx"
      to: "content/comments.json"
      via: "relative import '../../../content/comments.json'"
      pattern: "static JSON import"
---

<objective>
PIVOT: Original plan used Giscus (GitHub Discussions). Replaced with GAS anonymous commenting — same pattern as pledge/commitment forms in Phase 10.

Create CommentForm (client, GAS POST) and CommentList (server, static JSON import) components, plus seed content/comments.json. No GitHub account required for commenters.

Purpose: African health policymakers don't have GitHub accounts — GAS anonymous commenting removes the auth barrier entirely.
Output: Two new components + seed data file ready for brief detail page integration in Plan 02.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Seed content/comments.json</name>
  <files>content/comments.json</files>
  <action>Create `content/comments.json` with an empty object `{}`. This file will be populated by the fetch-comments GitHub Actions workflow with approved comments keyed by brief slug.</action>
  <done>content/comments.json exists and contains valid JSON `{}`.</done>
</task>

<task type="auto">
  <name>Task 2: Create CommentForm component</name>
  <files>app/components/briefs/CommentForm.tsx, app/lib/analytics.ts</files>
  <action>Create CommentForm — 'use client', name/email/comment fields, POST to NEXT_PUBLIC_GAS_URL with formType: 'comment'. Add trackEvent generic helper to analytics.ts.</action>
  <done>CommentForm.tsx exists, TypeScript clean, exports named CommentForm.</done>
</task>

<task type="auto">
  <name>Task 3: Create CommentList component</name>
  <files>app/components/briefs/CommentList.tsx</files>
  <action>Create CommentList — server component, imports content/comments.json via relative path, renders approved comments by slug or empty state message.</action>
  <done>CommentList.tsx exists, TypeScript clean, exports named CommentList.</done>
</task>

</tasks>
