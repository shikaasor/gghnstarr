---
phase: 17-lead-capture-pre-download-access-wall-collecting-name-role-email-and-audience-category-via-gas
plan: "04"
type: execute
wave: 3
depends_on: ["17-02", "17-03"]
files_modified: []
autonomous: false
requirements:
  - LEAD-01

must_haves:
  truths:
    - "Clicking a PDF download on /briefs shows the lead capture modal to a fresh browser"
    - "Submitting the form triggers a download without an extra click"
    - "Dismissing the modal without submitting shows no download and reveals the inline message"
    - "A second click on any gated download after submitting bypasses the modal entirely"
    - "PNG/JPEG assets on /awareness are not gated"
  artifacts:
    - path: "app/components/briefs/LeadCaptureModal.tsx"
      provides: "Verified working modal form"
    - path: "app/lib/lead-capture.ts"
      provides: "Verified localStorage helpers"
  key_links:
    - from: "Browser localStorage"
      to: "hasCompletedLeadCapture()"
      via: "gghn_lead_email key presence"
      pattern: "gghn_lead_email"
---

<objective>
Human verification of the complete lead capture flow across all four download surfaces. Claude runs the dev server; the user tests the wall, bypass, dismiss, and ungated-asset flows.

Purpose: Lead capture is a conversion-critical user flow — automated tests cannot substitute for interactive verification of modal UX, form submission, and download trigger behavior.
Output: Verified lead capture system ready for production.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final build verification</name>
  <files>app/lib/lead-capture.ts, app/components/briefs/LeadCaptureModal.tsx, app/components/briefs/DownloadButton.tsx, app/components/briefs/BriefCard.tsx, app/components/sections/FeaturedBrief.tsx, app/components/take-action/DownloadCard.tsx, app/components/take-action/ToolkitSection.tsx</files>
  <action>
    Run `npm run build` and confirm the static export completes with zero errors and zero TypeScript errors.

    Also run a quick grep to confirm:
    - `grep -r "formType.*lead-capture" app/` — should find exactly 1 match (in LeadCaptureModal.tsx)
    - `grep -r "gghn_lead_email" app/` — should find in lead-capture.ts only
    - `grep -r "LeadCaptureModal" app/` — should find 4+ import/usage sites (DownloadButton, BriefCard, FeaturedBrief, DownloadCard)

    If build fails or greps return unexpected results, diagnose and fix before surfacing the checkpoint.
  </action>
  <verify>
    `npm run build` exits 0.
    All three greps return expected matches.
  </verify>
  <done>Build clean, all four surfaces import and use LeadCaptureModal, formType and localStorage key are consistent.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of lead capture flow</name>
  <files></files>
  <action>
    Start dev server (`npm run dev`) and guide the user through interactive verification of the lead capture wall across all 4 PDF download surfaces.
  </action>
  <verify>
    User confirms all 6 test scenarios (A-F below) pass as described in what-built and how-to-verify sections.
  </verify>
  <done>User types "approved" after verifying modal appears, submit triggers download, dismiss closes without download, returning visitors bypass, PNG assets ungated.</done>

  <what-built>
    Complete lead capture wall across all 4 PDF download surfaces:
    - /briefs grid: BriefCard PDF and infographic PDF buttons
    - /briefs/[slug] detail pages: DownloadButton (primary download + outline variant)
    - Homepage: FeaturedBrief "Download PDF" button
    - /take-action toolkit: DownloadCard PDF and DOCX cards (PNG card should NOT be gated)

    Returning visitor bypass via localStorage (gghn_lead_email key).
    GAS submission with formType: 'lead-capture'.
    Silent GAS failure — download proceeds regardless.
    Dismiss without submit — modal closes, no download.
  </what-built>
  <how-to-verify>
    Start dev server: `npm run dev` then open http://localhost:3000

    **Test A — First visit wall (clear localStorage first):**
    1. Open DevTools → Application → Local Storage → delete gghn_lead_email if present
    2. Navigate to /briefs, click "Download PDF" on any brief card
    3. Expected: LeadCaptureModal appears centered over page with backdrop
    4. Check modal has: Email field (required), Audience Category dropdown (required), Name field (optional), Role dropdown (optional)
    5. Check modal has: title "Download AMR Resource", explanation subtitle, X button

    **Test B — Dismiss without submitting:**
    1. Click the X button or backdrop of the open modal
    2. Expected: modal closes, no file downloads
    3. Confirm gghn_lead_email is NOT in localStorage

    **Test C — Submit and auto-download:**
    1. Click Download PDF again to reopen modal
    2. Fill in: Email = test@example.com, Audience Category = Researcher
    3. Click submit button
    4. Expected: modal closes, PDF download initiates automatically (browser download bar appears or new tab opens with PDF)
    5. Confirm gghn_lead_email = "test@example.com" in localStorage

    **Test D — Returning visitor bypass:**
    1. Click any other PDF download button (different brief card, or FeaturedBrief on homepage)
    2. Expected: NO modal appears — download triggers directly
    3. Navigate to /take-action, click a PDF or DOCX download card
    4. Expected: NO modal — direct download

    **Test E — PNG not gated:**
    1. On /take-action, if there is a PNG toolkit card, click its download
    2. Expected: direct download with no modal

    **Test F — Homepage FeaturedBrief:**
    1. Clear localStorage (delete gghn_lead_email)
    2. Navigate to homepage, scroll to Featured Brief section, click "Download PDF"
    3. Expected: modal appears

    Type "approved" if all tests pass, or describe which test failed and what you observed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issue</resume-signal>
</task>

</tasks>

<verification>
All checkpoint tests A-F pass as described in how-to-verify.
</verification>

<success_criteria>
- Modal appears on first PDF download click across all 4 surfaces
- Returning visitors bypass modal (localStorage key present)
- Submit triggers automatic download regardless of GAS outcome
- Dismiss closes modal with no download
- PNG assets on /take-action are not gated
- npm run build exits clean
</success_criteria>

<output>
After completion, create `.planning/phases/17-lead-capture-pre-download-access-wall-collecting-name-role-email-and-audience-category-via-gas/17-04-SUMMARY.md`
</output>
