---
phase: 06-brand-rebrand
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - .planning/STATE.md
autonomous: true
requirements:
  - BRAND-03

must_haves:
  truths:
    - "No component or page TSX file contains hardcoded hex values (#0F172A, #0D9488, #1E293B) from the old navy/teal palette"
    - "`npx next build` exits 0 with no CSS token errors"
    - "All 5 pages render with AMR green backgrounds and gold accents (navy/teal hues are gone visually)"
    - "STATE.md reflects Phase 6 complete"
  artifacts:
    - path: ".planning/phases/06-brand-rebrand/06-03-SUMMARY.md"
      provides: "Phase 6 completion summary"
  key_links:
    - from: "app/globals.css @theme"
      to: "all component files using teal-* and navy-* classes"
      via: "Tailwind v4 CSS variable resolution"
      pattern: "--color-navy-950|--color-teal-600"
---

<objective>
Final verification that the brand rebrand is complete and correct: audit components for hardcoded old hex values, confirm next build passes, and do a visual spot-check that all 5 pages reflect the AMR green/gold palette. No code changes expected — this is a verification plan that closes BRAND-03.

Purpose: BRAND-03 is satisfied by the token update in 06-01 (Option B: class names unchanged, hex values updated). This plan confirms no component has hardcoded old colors that bypass the token system, and that the build and visual output are correct.
Output: 06-03-SUMMARY.md + updated STATE.md.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-brand-rebrand/06-01-SUMMARY.md
@.planning/phases/06-brand-rebrand/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit components for hardcoded old hex values</name>
  <files>app/ (read-only audit)</files>
  <action>
Search all TSX and CSS files under app/ for the old hex values that should have been replaced. These would only appear if a component bypassed the Tailwind token system and used arbitrary hex values directly.

Run these grep commands:
```bash
grep -rn "#0F172A\|#0D9488\|#1E293B\|#2D3F55\|#14B8A6\|#2DD4BF" app/ --include="*.tsx" --include="*.css"
```

Expected: empty output (no hardcoded old hex values in components).

If any matches are found, replace the hardcoded hex with the equivalent Tailwind token class (e.g., replace inline `color: '#0D9488'` style with `className="text-teal-600"`).
  </action>
  <verify>
Run: `grep -rn "#0F172A\|#0D9488\|#1E293B\|#14B8A6\|#2DD4BF" app/ --include="*.tsx" --include="*.css"`
Expected: no matches.
  </verify>
  <done>
No hardcoded old navy/teal hex values remain in any component or page file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run next build and confirm it passes</name>
  <files>(build output only)</files>
  <action>
Run a full production build to confirm all token changes and logo additions compile without errors:

```bash
npx next build 2>&1
```

Expected: build completes with exit code 0. Look for:
- No "Module not found" errors (confirms Image import resolved)
- No Tailwind token errors
- No TypeScript type errors
- "Export" stage completes (static pages generated)

If any errors occur, read the error message and fix the root cause:
- TypeScript error in Header/Footer → fix the import or component syntax
- Token resolution error → check globals.css @theme block syntax
- Image component error → verify public/amr-logo.jpeg exists
  </action>
  <verify>
Run: `npx next build 2>&1 | tail -10`
Expected: last lines show successful export completion, exit code 0.
  </verify>
  <done>
`npx next build` exits 0. Static export completed without errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update STATE.md to reflect Phase 6 complete</name>
  <files>.planning/STATE.md</files>
  <action>
Update STATE.md to record Phase 6 completion:

1. Change "Current Position" to:
   - Phase: 6 complete → Phase 7 next
   - Status: Phase 6 complete — ready to plan Phase 7
   - Last activity: 2026-04-28 — Phase 6 Brand Rebrand complete

2. Update Progress bar to reflect 1/8 v2.0 phases complete (~12%).

3. Add to Accumulated Context → Decisions:
   - Phase 6: Used Option B token strategy (keep teal/navy class names, update hex to AMR green/gold)
   - Phase 6: teal-600 darkened to #0A7050 for WCAG AA compliance on white (6.1:1 ratio)
   - Phase 6: Logo uses mix-blend-mode: multiply to handle white JPEG background on dark header
   - Phase 6: AMR gold token added as --color-amr-gold: #F2A900 for future accent uses

4. Update Session Continuity:
   - Last session: 2026-04-28
   - Stopped at: Phase 6 complete — resume at Phase 7 planning
  </action>
  <verify>
Run: `grep "Phase 7\|Phase 6 complete" .planning/STATE.md`
Expected: matches found.
  </verify>
  <done>
STATE.md updated with Phase 6 completion status and key decisions captured.
  </done>
</task>

</tasks>

<verification>
1. `grep -rn "#0F172A\|#0D9488\|#1E293B" app/ --include="*.tsx"` → empty (no old hex values).
2. `npx next build` exits 0.
3. `grep "navy-950" app/globals.css` shows `#1A3A2A` (not `#0F172A`).
4. `grep "teal-600" app/globals.css` shows `#0A7050` (not `#0D9488`).
5. `grep "amr-gold\|teal-50" app/globals.css` — both tokens present.
6. `ls public/amr-logo.jpeg` — file exists.
7. `grep "amr-logo" app/components/layout/Header.tsx app/components/layout/Footer.tsx` — both files reference the logo.
</verification>

<success_criteria>
- No hardcoded old hex values in any component file
- `npx next build` exits 0
- STATE.md reflects Phase 6 complete and Phase 7 ready
- All 3 BRAND requirements satisfied: BRAND-01 (logo in header/footer), BRAND-02 (palette updated), BRAND-03 (components updated, contrast maintained)
</success_criteria>

<output>
After completion, create `.planning/phases/06-brand-rebrand/06-03-SUMMARY.md` using the summary template.
</output>
