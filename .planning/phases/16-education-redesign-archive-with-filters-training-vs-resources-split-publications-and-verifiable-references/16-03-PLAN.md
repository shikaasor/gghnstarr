---
phase: 16-education-redesign-archive-with-filters-training-vs-resources-split-publications-and-verifiable-references
plan: 03
type: execute
wave: 3
depends_on:
  - "16-01"
  - "16-02"
files_modified:
  - app/education/page.tsx
  - app/components/education/EducationGrid.tsx
autonomous: false
requirements:
  - EDUC-01
  - EDUC-02

must_haves:
  truths:
    - "Navigating to /education shows the Training tab by default with no hash in URL"
    - "Navigating to /education#resources switches to the Resources tab displaying resource cards"
    - "The page renders without hydration mismatch errors in the browser console"
    - "next build completes without TypeScript or build errors"
    - "All 15 education items from content/education.json appear across the two tabs"
    - "The old EducationGrid component is retired and no longer imported by page.tsx"
  artifacts:
    - path: "app/education/page.tsx"
      provides: "Server Component reading content/education.json and passing to EducationTabs"
      contains: "readFileSync|education.json"
    - path: "app/components/education/EducationGrid.tsx"
      provides: "Retired (old Phase 8 component replaced)"
  key_links:
    - from: "app/education/page.tsx"
      to: "content/education.json"
      via: "readFileSync at build time"
      pattern: "education\\.json"
    - from: "app/education/page.tsx"
      to: "app/components/education/EducationTabs.tsx"
      via: "<EducationTabs items={items} />"
      pattern: "EducationTabs"
---

<objective>
Wire the education data layer and components together, replace the Phase 8 page.tsx entirely, and verify the full redesigned /education page works correctly in the browser.

Purpose: This is the integration and ship step. The Server Component reads the JSON data file at build time, passes the item array to the EducationTabs Client Component, and the full redesigned page goes live.
Output: Updated app/education/page.tsx (Server Component), retired EducationGrid.tsx, and a verified working /education page.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@app/lib/types.ts
@app/education/page.tsx
@app/components/education/EducationGrid.tsx
@.planning/phases/16-education-redesign-archive-with-filters-training-vs-resources-split-publications-and-verifiable-references/16-01-SUMMARY.md
@.planning/phases/16-education-redesign-archive-with-filters-training-vs-resources-split-publications-and-verifiable-references/16-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace page.tsx Server Component and retire EducationGrid</name>
  <files>
    app/education/page.tsx
    app/components/education/EducationGrid.tsx
  </files>
  <action>
    **app/education/page.tsx — full replacement:**

    Read content/education.json at build time using Node.js fs + path (static export, no runtime). Pass items to EducationTabs.

    The new page is a Server Component (no 'use client'). It reads the JSON file synchronously at build time, parses it as EducationItem[], and renders a hero section + EducationTabs. Metadata title remains "Education Library | GGHN STARR" with updated description covering training courses, webinars, publications, and reference materials.

    Import EducationTabs as default import from '@/components/education/EducationTabs'. Pass the full items array as the `items` prop. No other state or logic in page.tsx — all interactivity is in EducationTabs.

    **app/components/education/EducationGrid.tsx — retire:**

    Before modifying, check for any remaining imports with:
    grep -r "EducationGrid" app/ --include="*.tsx" --include="*.ts" -l

    If only the old page.tsx imported it (which is now replaced), replace the full file content with a retirement stub:

    // EducationGrid.tsx is retired as of Phase 16. Use EducationTabs instead.
    export {};

    After retiring EducationGrid, check whether the deprecated EducationResource type in app/lib/types.ts has any remaining references:
    grep -r "EducationResource" app/ --include="*.tsx" --include="*.ts"

    If zero references remain, remove the deprecated EducationResource, old AudienceType, and old ResourceFormat exports from types.ts. If any file still uses them, leave the deprecated JSDoc comment in place.
  </action>
  <verify>
    1. `npx tsc --noEmit` — zero errors
    2. `next build` — completes successfully (exit code 0)
    3. `grep -r "EducationGrid" app/ --include="*.tsx" -l` — page.tsx must not appear in results
  </verify>
  <done>page.tsx reads content/education.json and renders EducationTabs. next build passes. EducationGrid retired to stub.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of /education redesign</name>
  <action>
    Run `npm run dev` to start the development server, then open http://localhost:3000/education and perform all verification steps listed in how-to-verify. This is a human-in-the-loop verification step — no automated action required from Claude.
  </action>
  <files>none — human verification only</files>
  <verify>
    Open http://localhost:3000/education and confirm all 10 steps in how-to-verify pass. Type "approved" to unblock execution.
  </verify>
  <done>All 10 verification points confirmed by the user: tabs work, filters work, pagination works, hash sync works, Publication cards show authors/journal, "Source unverified" amber flag is visible on the ECHO webinar card, no hydration errors in console.</done>
  <what-built>
    Full /education page redesign:
    - Training tab with 3 seeded items (courses + webinar), active by default
    - Resources tab with 12 migrated items including 3 Publication-format cards with authors/journal
    - Four filter dimensions (audience, format, topic, year) with pill chips
    - Pagination at 12 items/page with prev/next + numbered pages
    - URL hash tab sync (#training / #resources) including browser back/forward
    - "Source unverified" amber flag on the ECHO webinar card
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open http://localhost:3000/education

    1. Default state: Training tab is active, 3 items visible (WHO Academy courses + ECHO webinar)
    2. Click "Resources" tab: switches to resources, URL becomes /education#resources, 12 resource cards appear
    3. Navigate back in browser (Back button): URL returns to /education#training, Training tab re-activates
    4. On Resources tab: click "Policymaker" audience pill — only policymaker-tagged items remain
    5. Add a second filter (e.g. "Download" format) — result set narrows further (AND logic across dimensions)
    6. Click "Clear filters" — all 12 resources return
    7. Find one of the three Publication-format cards (Cambridge or Frontiers article) — it should show authors + journal text below the title, and a distinct dark format badge
    8. Find the ECHO webinar card on Training tab — it should show a yellow "Source unverified" badge
    9. Switch from Resources to Training: confirm filter state cleared (no active pills on Training tab)
    10. Check browser console — zero hydration mismatch errors
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. `next build` — exit code 0, no missing module warnings
3. Manual browser verification across all 10 checkpoints above
4. Browser console shows no hydration mismatch errors on /education or /education#resources
</verification>

<success_criteria>
- /education page loads with Training tab active by default
- Tab switching via URL hash (#training / #resources) works correctly including browser back/forward
- All four filter dimensions work with correct AND-across / OR-within logic
- Pagination renders and navigates correctly
- Publication cards show authors + journal + distinct badge
- "Source unverified" amber flag visible on unverified items
- next build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-education-redesign-archive-with-filters-training-vs-resources-split-publications-and-verifiable-references/16-03-SUMMARY.md`
</output>
