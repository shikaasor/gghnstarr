---
phase: 08-awareness-hub-education-library
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified: []
autonomous: false
requirements:
  - AWRE-01
  - AWRE-02
  - EDUC-01
  - EDUC-02

must_haves:
  truths:
    - "Both /awareness and /education pages are reachable from the Header nav"
    - "Homepage AudienceCTAs cards link correctly to /awareness and /education"
    - "/awareness infographic grid renders all 3 JPEGs and lightbox works"
    - "/education tab filter works across all 4 tabs"
    - "npm run build succeeds with no errors"
  artifacts:
    - path: "app/awareness/page.tsx"
      provides: "Statically built /awareness route"
    - path: "app/education/page.tsx"
      provides: "Statically built /education route"
  key_links:
    - from: "Header nav"
      to: "/awareness and /education"
      via: "navLinks array in Header.tsx"
    - from: "AudienceCTAs"
      to: "/awareness and /education"
      via: "primaryHref and secondaryLinks with live: true"
---

<objective>
Visual verification checkpoint for Phase 8. Confirm both new pages render correctly, nav and homepage CTAs link to them, and the production build succeeds.

Purpose: User sign-off before Phase 8 is marked complete and Phase 9 begins.
Output: Approved or issues documented for gap closure.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-awareness-hub-education-library/08-02-SUMMARY.md
@.planning/phases/08-awareness-hub-education-library/08-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final build verification</name>
  <files></files>
  <action>
    Run the following verification commands and report results:

    1. `npx tsc --noEmit` — must return zero errors
    2. `npm run build` — must complete successfully with /awareness and /education appearing in the build output
    3. Confirm these files exist:
       - app/awareness/page.tsx
       - app/education/page.tsx
       - app/components/awareness/InfographicGrid.tsx
       - app/components/awareness/AccordionSection.tsx
       - app/components/education/EducationGrid.tsx
    4. Confirm AudienceCTAs.tsx contains at least 5 occurrences of `live: true`
    5. Confirm Header.tsx navLinks contains '/awareness' and '/education'

    If any check fails, fix the issue before proceeding to the checkpoint.
  </action>
  <verify>All 5 checks above pass with no errors.</verify>
  <done>Build succeeds, all files exist, nav and CTAs confirmed updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 8 visual verification</name>
  <files></files>
  <action>Start dev server with `npm run dev` and follow the verification steps in how-to-verify below.</action>
  <verify>User confirms all items in how-to-verify pass and types "approved".</verify>
  <done>Phase 8 visual sign-off received from user.</done>
  <what-built>
    Phase 8 complete: /awareness page with 3-column infographic grid, lightbox, and 3 real-content accordion explainers; /education page with tab-filtered resource grid (12 resources, 4 audiences); Header nav updated with Awareness and Education links; AudienceCTAs on homepage now live for /awareness and /education.
  </what-built>
  <how-to-verify>
    Run: npm run dev

    1. HEADER NAV
       - Confirm "Awareness" and "Education" links appear in the desktop nav
       - Confirm they also appear in the mobile hamburger menu

    2. HOMEPAGE AUDIENCE CTAs (http://localhost:3000)
       - Scroll to the "Find resources for your role" section
       - Confirm "AMR Awareness Hub" link under Policymakers card is clickable (not grey/disabled)
       - Confirm "Explore Resources" button under Healthcare Workers card is now a teal button (not grey)
       - Confirm "Learn What You Can Do" button under General Public card is now a teal button (not grey)
       - Confirm clicking these links navigates to /awareness or /education

    3. /awareness PAGE (http://localhost:3000/awareness)
       - Confirm 3 infographic cards render in a grid with titles and descriptions
       - Click one infographic — lightbox opens with enlarged image and close button
       - Press Escape or click X to close lightbox
       - Confirm 3 accordions are visible and collapsed by default ("What is AMR?", "Why Does Africa Face a Disproportionate Burden?", "What Can You Do?")
       - Click each accordion — it expands to show 3 paragraphs of real AMR content
       - Confirm a fact sheet / external link is visible at the bottom of each expanded section
       - Click a fact sheet link — it opens in a new tab

    4. /education PAGE (http://localhost:3000/education)
       - Confirm "All" tab is active by default and 12 resource cards are visible
       - Click "Policymaker" tab — cards visible are only those tagged for policymakers
       - Click "Healthcare Worker" tab — cards visible are only those tagged for healthcare workers
       - Click "General Public" tab — cards visible are only those tagged for general public
       - On any card: confirm source label, title, audience pill(s), and format label with icon are visible
       - Click a card — it opens an external URL in a new browser tab

    5. MOBILE (resize browser to 375px width or use DevTools mobile)
       - Confirm /awareness infographic grid collapses to 1 column on mobile
       - Confirm /education card grid collapses to 1 column on mobile
       - Confirm tab bar wraps cleanly on mobile
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 8 is verified when the human checkpoint is approved.
</verification>

<success_criteria>
- Build succeeds with zero TypeScript errors
- /awareness page renders 3 infographic cards + lightbox + 3 real-content accordions
- /education page renders 12 resource cards with working tab filter
- Header and AudienceCTAs updated and functional
- Human verifier types "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/08-awareness-hub-education-library/08-04-SUMMARY.md`

Also update STATE.md:
- Current Position → Phase 09-news-feed, Plan 09-01 (next)
- Status → Phase 8 complete — 4/4 plans done, visual sign-off received; Phase 9 ready to begin
- Last activity → 2026-04-29 — 08-04 Phase 8 visual verification approved
</output>
