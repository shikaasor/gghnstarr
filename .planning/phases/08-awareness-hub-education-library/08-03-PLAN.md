---
phase: 08-awareness-hub-education-library
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - app/education/page.tsx
  - app/components/education/EducationGrid.tsx
autonomous: true
requirements:
  - EDUC-01
  - EDUC-02

must_haves:
  truths:
    - "Navigating to /education renders a page without errors"
    - "All 12 resource cards are visible when the All tab is active"
    - "Clicking Policymaker tab shows only cards tagged for that audience"
    - "Clicking Healthcare Worker tab shows only cards tagged for that audience"
    - "Clicking General Public tab shows only cards tagged for that audience"
    - "Each card shows title, audience tag(s), format label with icon, and source/organization"
    - "Clicking a card opens the external resource in a new tab"
  artifacts:
    - path: "app/education/page.tsx"
      provides: "Server Component — education library page"
      contains: "EducationGrid"
    - path: "app/components/education/EducationGrid.tsx"
      provides: "Tab-filtered grid of education resource cards"
      contains: "use client"
  key_links:
    - from: "app/education/page.tsx"
      to: "app/components/education/EducationGrid.tsx"
      via: "Server Component passes educationResources array prop"
      pattern: "EducationGrid.*resources"
    - from: "app/components/education/EducationGrid.tsx"
      to: "external URLs (who.int, africacdc.org, etc.)"
      via: "anchor tags with target=_blank rel=noopener noreferrer"
      pattern: "target.*_blank.*noopener"

user_setup: []
---

<objective>
Build the /education page: create the EducationGrid client component with tab-based audience filter and resource cards, and wire it into the education page with all 12 curated resources embedded as constants.

Purpose: Satisfies EDUC-01 (dedicated /education page with audience filter) and EDUC-02 (resource cards with title, audience tags, format label, source, and working links).
Output: Working /education route with All | Policymaker | Healthcare Worker | General Public tab filter and 12 resource cards.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/lib/types.ts
@app/components/layout/Container.tsx
@app/components/briefs/BriefGrid.tsx
@.planning/phases/08-awareness-hub-education-library/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EducationGrid client component</name>
  <files>app/components/education/EducationGrid.tsx</files>
  <action>
    Create app/components/education/EducationGrid.tsx. This is a 'use client' component mirroring the BriefGrid pattern.

    The component receives an array of EducationResource objects and renders:
    - A tab bar: All | Policymaker | Healthcare Worker | General Public
    - A filtered grid of resource cards

    Tab style: active tab = `bg-teal-600 text-white`, inactive tab = `bg-slate-100 text-slate-600 hover:bg-slate-200` — pill/tab style matching the briefs library theme filter.

    Each card:
    - White background, rounded-lg, border border-slate-200, shadow-sm, hover:shadow-md
    - Title: font-serif text-navy-950 font-semibold
    - Source/org: text-slate-500 text-xs
    - Audience tag(s): small pill(s) — bg-teal-50 text-teal-700 text-xs px-2 py-0.5 rounded-full
    - Format label + icon: document icon for Article (FileText from lucide), download arrow for Download (Download from lucide), play button for Video (Play from lucide) — show icon + format text in text-slate-500 text-xs
    - Entire card is a link: `<a href={resource.url} target="_blank" rel="noopener noreferrer">` — wraps the full card

    A card tagged with multiple audiences (e.g., audiences: ['Policymaker', 'Healthcare Worker']) appears under both filter tabs and under "All".

    ```tsx
    'use client';
    import { useState } from 'react';
    import { FileText, Download, Play } from 'lucide-react';
    import type { EducationResource, AudienceType, ResourceFormat } from '@/lib/types';

    const FORMAT_ICONS: Record<ResourceFormat, React.ReactNode> = {
      Article: <FileText size={14} className="inline-block mr-1" />,
      Download: <Download size={14} className="inline-block mr-1" />,
      Video: <Play size={14} className="inline-block mr-1" />,
    };

    const TABS = ['All', 'Policymaker', 'Healthcare Worker', 'General Public'] as const;
    type Tab = typeof TABS[number];

    interface EducationGridProps {
      resources: EducationResource[];
    }

    export default function EducationGrid({ resources }: EducationGridProps) {
      const [activeTab, setActiveTab] = useState<Tab>('All');

      const filtered = activeTab === 'All'
        ? resources
        : resources.filter(r => r.audiences.includes(activeTab as AudienceType));

      return (
        <div>
          {/* Tab bar */}
          <div className="flex flex-wrap gap-2 mb-8" role="tablist" aria-label="Filter by audience">
            {TABS.map(tab => (
              <button
                key={tab}
                role="tab"
                aria-selected={activeTab === tab}
                onClick={() => setActiveTab(tab)}
                className={`px-4 py-2 rounded-full text-sm font-medium transition-colors focus:outline-none focus-visible:ring-2 focus-visible:ring-teal-600 ${
                  activeTab === tab
                    ? 'bg-teal-600 text-white'
                    : 'bg-slate-100 text-slate-600 hover:bg-slate-200'
                }`}
              >
                {tab}
              </button>
            ))}
          </div>

          {/* Resource grid */}
          <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-5">
            {filtered.map(resource => (
              <a
                key={resource.url}
                href={resource.url}
                target="_blank"
                rel="noopener noreferrer"
                className="group bg-white rounded-lg border border-slate-200 shadow-sm hover:shadow-md transition-shadow p-5 flex flex-col gap-3 focus:outline-none focus-visible:ring-2 focus-visible:ring-teal-600"
              >
                {/* Source */}
                <span className="text-xs text-slate-400 font-medium uppercase tracking-wide">
                  {resource.source}
                </span>

                {/* Title */}
                <h3 className="font-serif text-navy-950 font-semibold text-sm leading-snug group-hover:text-teal-700 transition-colors">
                  {resource.title}
                </h3>

                {/* Audience tags + format */}
                <div className="mt-auto flex flex-wrap items-center gap-2">
                  {resource.audiences.map(audience => (
                    <span
                      key={audience}
                      className="bg-teal-50 text-teal-700 text-xs px-2 py-0.5 rounded-full font-medium"
                    >
                      {audience}
                    </span>
                  ))}
                  <span className="text-slate-400 text-xs ml-auto flex items-center">
                    {FORMAT_ICONS[resource.format]}
                    {resource.format}
                  </span>
                </div>
              </a>
            ))}
          </div>

          {filtered.length === 0 && (
            <p className="text-slate-500 text-sm py-8 text-center">
              No resources found for this audience.
            </p>
          )}
        </div>
      );
    }
    ```
  </action>
  <verify>Run: npx tsc --noEmit — zero errors.</verify>
  <done>EducationGrid.tsx compiles with zero TypeScript errors. Tab filtering logic and card rendering are present.</done>
</task>

<task type="auto">
  <name>Task 2: Create education/page.tsx with 12 curated resources</name>
  <files>app/education/page.tsx</files>
  <action>
    Create app/education/page.tsx as a Server Component. Embed all 12 resources as a typed constant — no JSON file or fs.readFileSync needed.

    ```tsx
    import type { Metadata } from 'next';
    import { Container } from '@/components/layout/Container';
    import EducationGrid from '@/components/education/EducationGrid';
    import type { EducationResource } from '@/lib/types';

    export const metadata: Metadata = {
      title: 'Education Library | GGHN STARR',
      description: 'Audience-specific AMR learning materials for policymakers, healthcare workers, and the general public — curated from WHO, Africa CDC, and leading research institutions.',
    };

    const educationResources: EducationResource[] = [
      // Policymaker
      {
        title: 'African Union Framework for Antimicrobial Resistance Control 2020–2025',
        audiences: ['Policymaker'],
        format: 'Download',
        source: 'Africa CDC',
        url: 'https://africacdc.org/download/african-union-framework-for-antimicrobial-resistance-control-2020-2025/',
      },
      {
        title: 'African Union AMR Landmark Report: Voicing African Priorities on the Active Pandemic',
        audiences: ['Policymaker'],
        format: 'Download',
        source: 'Africa CDC',
        url: 'https://africacdc.org/download/african-union-amr-landmark-report-voicing-african-priorities-on-the-active-pandemic/',
      },
      {
        title: 'Global Antibiotic Resistance Surveillance Report 2025',
        audiences: ['Policymaker', 'Healthcare Worker'],
        format: 'Article',
        source: 'WHO',
        url: 'https://www.who.int/publications/i/item/9789240116337',
      },
      {
        title: 'Implementing the Global Action Plan on Antimicrobial Resistance',
        audiences: ['Policymaker'],
        format: 'Download',
        source: 'WOAH / WHO',
        url: 'https://www.woah.org/app/uploads/2024/01/implementing-the-global-action-plan-on-antimicrobial-resistance.pdf',
      },
      // Healthcare Worker
      {
        title: 'AMR Surveillance Guidance for the African Region',
        audiences: ['Healthcare Worker', 'Policymaker'],
        format: 'Download',
        source: 'Africa CDC',
        url: 'https://africacdc.org/download/antimicrobial-resistance-surveillance-guidance-for-the-african-region/',
      },
      {
        title: 'Knowledge, Attitudes and Practices on AMR Among Pharmacy Workers in 28 African Countries',
        audiences: ['Healthcare Worker'],
        format: 'Article',
        source: 'Africa Health Knowledge Hub',
        url: 'https://khub.africacdc.org/records/resource?id=152',
      },
      {
        title: 'Antimicrobial Stewardship in Africa: Policy Analysis Across Five Countries',
        audiences: ['Healthcare Worker', 'Policymaker'],
        format: 'Article',
        source: 'Cambridge / ASH&E',
        url: 'https://www.cambridge.org/core/journals/antimicrobial-stewardship-and-healthcare-epidemiology/article/antimicrobial-stewardship-in-africa-a-policy-analysis-of-national-action-plans-across-five-african-countries/EC7184C836CDF3B8F56DC6DA3A6ABC02',
      },
      {
        title: 'Combating AMR in Africa: Strategic Roadmap for Surveillance, Stewardship, and Research',
        audiences: ['Healthcare Worker', 'Policymaker'],
        format: 'Article',
        source: 'Frontiers',
        url: 'https://www.frontiersin.org/journals/cellular-and-infection-microbiology/articles/10.3389/fcimb.2025.1714021/full',
      },
      // General Public
      {
        title: 'Antimicrobial Resistance — WHO Fact Sheet',
        audiences: ['General Public', 'Healthcare Worker'],
        format: 'Article',
        source: 'WHO',
        url: 'https://www.who.int/news-room/fact-sheets/detail/antimicrobial-resistance',
      },
      {
        title: 'World AMR Awareness Week 2024: Educate. Advocate. Act Now.',
        audiences: ['General Public'],
        format: 'Article',
        source: 'WHO',
        url: 'https://www.who.int/campaigns/world-amr-awareness-week/2024',
      },
      {
        title: 'Enhancing General Public Knowledge of AMR in Africa: A Video-Based Educational Review',
        audiences: ['General Public'],
        format: 'Article',
        source: 'PubMed / JAC-AMR',
        url: 'https://pmc.ncbi.nlm.nih.gov/articles/PMC11775625/',
      },
      {
        title: 'Global Call to Action to Address Antimicrobial Resistance (October 2025)',
        audiences: ['General Public', 'Policymaker'],
        format: 'Download',
        source: 'WHO',
        url: 'https://cdn.who.int/media/docs/default-source/antimicrobial-resistance/amr-gcp-asa/global-call-to-action-to-address-amr---oct-2025.pdf',
      },
    ];

    export default function EducationPage() {
      return (
        <main>
          {/* Hero */}
          <section className="bg-teal-600 text-white py-14">
            <Container>
              <div className="max-w-2xl">
                <span className="inline-block bg-white/20 text-white text-xs font-medium px-3 py-1 rounded-full mb-4 uppercase tracking-wide">
                  Education Library
                </span>
                <h1 className="font-serif text-3xl md:text-4xl font-bold mb-4">
                  AMR Learning Resources
                </h1>
                <p className="text-teal-100 text-lg leading-relaxed">
                  Curated materials from WHO, Africa CDC, Fleming Fund, and leading research institutions — filtered for your role and professional context.
                </p>
              </div>
            </Container>
          </section>

          {/* Filtered grid */}
          <section className="py-14 bg-slate-50">
            <Container>
              <h2 className="font-serif text-2xl font-bold text-navy-950 mb-2">
                Browse Resources
              </h2>
              <p className="text-slate-600 text-sm mb-8">
                {educationResources.length} resources available. Filter by your audience to find the most relevant materials.
              </p>
              <EducationGrid resources={educationResources} />
            </Container>
          </section>
        </main>
      );
    }
    ```
  </action>
  <verify>
    Run: npx tsc --noEmit — zero errors.
    Run: npm run build — education page appears in static build output, no errors.
    Run: npm run dev and navigate to http://localhost:3000/education — 12 cards visible under All tab, filtering works per audience.
  </verify>
  <done>
    /education route builds statically. 12 resource cards render. Tab filter shows correct subsets per audience. All cards open external links in new tab with rel="noopener noreferrer".
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — build completes with app/education in output, no errors
3. Manual check at http://localhost:3000/education:
   - "All" tab active by default, 12 cards visible
   - "Policymaker" tab shows cards tagged Policymaker (including multi-audience cards)
   - "Healthcare Worker" tab shows cards tagged Healthcare Worker
   - "General Public" tab shows cards tagged General Public
   - Each card has title, source, audience pill(s), and format label with icon
   - Clicking a card navigates to the external URL in a new tab
</verification>

<success_criteria>
- /education page builds statically
- EducationGrid renders 12 cards under "All" filter
- Tab filter correctly includes multi-audience cards in each relevant tab
- Format icons render correctly (FileText, Download, Play from lucide-react)
- All external links use target="_blank" rel="noopener noreferrer"
</success_criteria>

<output>
After completion, create `.planning/phases/08-awareness-hub-education-library/08-03-SUMMARY.md`
</output>
