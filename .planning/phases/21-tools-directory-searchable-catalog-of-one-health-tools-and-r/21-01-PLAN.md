---
phase: 21-tools-directory-searchable-catalog-of-one-health-tools-and-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/tools/extract_oh_tools.py
  - content/oh-tools.json
  - app/lib/types.ts
autonomous: true
requirements: [TOOLDIR-01]
must_haves:
  truths:
    - "content/oh-tools.json contains exactly 50 tool records (no footnote rows)"
    - "Every record has a name, a numeric year, and arrays for organizationLevels/scopes/audienceLevels/audienceTypes"
    - "Embedded newlines in names/organizations are collapsed to single spaces"
    - "Messy/scheme-less URLs are normalized to https:// or set to empty string"
    - "ToolItem type is exported from app/lib/types.ts and the JSON validates against it"
  artifacts:
    - path: "resources/tools/extract_oh_tools.py"
      provides: "One-shot openpyxl extraction of rows 3-52 from Table 1 sheet"
      contains: "range(3, 53)"
    - path: "content/oh-tools.json"
      provides: "50 typed One Health tool records"
      min_lines: 50
    - path: "app/lib/types.ts"
      provides: "ToolItem interface + OH* token unions"
      contains: "export interface ToolItem"
  key_links:
    - from: "resources/tools/extract_oh_tools.py"
      to: "content/oh-tools.json"
      via: "json.dump write"
      pattern: "oh-tools\\.json"
    - from: "content/oh-tools.json"
      to: "app/lib/types.ts ToolItem"
      via: "shape conformance (id/name/year/organizationLevels/scopes/audienceLevels/audienceTypes/description/url)"
      pattern: "organizationLevels"
---

<objective>
Build the data layer for the Tools Directory: a one-shot Python extraction script that reads the 50 One Health tools from Table 1 of the OHHLEP inventory spreadsheet into a typed `content/oh-tools.json`, plus the `ToolItem` TypeScript type that the JSON conforms to.

Purpose: Establish the single source of truth (`content/oh-tools.json`) that the page and components in Plan 21-02 will read at build time. No runtime data fetching — fully compatible with `output: 'export'`.
Output: `resources/tools/extract_oh_tools.py`, `content/oh-tools.json` (exactly 50 records), and the `ToolItem` + `OHOrganizationLevel`/`OHAudienceType`/`OHScope` types appended to `app/lib/types.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-tools-directory-searchable-catalog-of-one-health-tools-and-r/21-RESEARCH.md
@.planning/phases/21-tools-directory-searchable-catalog-of-one-health-tools-and-r/21-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ToolItem type and OH token unions to app/lib/types.ts</name>
  <files>app/lib/types.ts</files>
  <read_first>
    - app/lib/types.ts (see existing EducationItem block lines 84-103 and NewsArticle block ending line 118 — follow the same convention: union types above the interface, inline `// field` comments, no default export)
    - .planning/phases/21-tools-directory-searchable-catalog-of-one-health-tools-and-r/21-RESEARCH.md (verified token unions and column map, lines 258-282)
  </read_first>
  <action>
    Append a new "Phase 21: One Health Tools Directory" block after the NewsArticle interface (end of file). Export three string-literal union types: `OHOrganizationLevel` with members 'Quadripartite' | 'National' | 'International/Regional' | 'NGO' | 'Academic'; `OHAudienceType` with members 'Multisectoral' | 'Policymakers' | 'Animal health' | 'Laboratory' | 'Environment' | 'Public health'; `OHScope` with members 'Assessment' | 'Implementation' | 'Monitoring' | 'Action Plans' | 'Prioritisation'. Then export `interface ToolItem` with fields: `id: string` (slug from name), `name: string`, `year: number`, `organization: string`, `organizationLevels: OHOrganizationLevel[]`, `scopes: OHScope[]`, `audienceLevels: string[]`, `audienceTypes: OHAudienceType[]`, `description: string`, `url: string`. Do not modify the existing EducationItem, NewsArticle, or AudienceType definitions.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - app/lib/types.ts contains `export interface ToolItem`
    - app/lib/types.ts contains `export type OHOrganizationLevel`, `export type OHAudienceType`, and `export type OHScope`
    - ToolItem declares fields organizationLevels, scopes, audienceLevels, audienceTypes (all array types), plus id, name, year, organization, description, url
    - `npx tsc --noEmit` exits 0 (no type errors introduced)
  </acceptance_criteria>
  <done>ToolItem and the three OH token unions are exported from app/lib/types.ts and the project type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create extract_oh_tools.py and generate content/oh-tools.json with exactly 50 records</name>
  <files>resources/tools/extract_oh_tools.py, content/oh-tools.json</files>
  <read_first>
    - resources/tools/extract_education.py (template — copy slugify, the openpyxl load idiom, the dedup-by-slug + content-hash-suffix pattern, and the `__main__` count-print/write idiom; DROP the CURATED_PATH machinery on lines 17-18 and 206-216 since tools have no curated baseline)
    - .planning/phases/21-tools-directory-searchable-catalog-of-one-health-tools-and-r/21-RESEARCH.md (column map lines 71-93, Pitfalls 1/2/4/5 lines 201-224)
    - .planning/phases/21-tools-directory-searchable-catalog-of-one-health-tools-and-r/21-PATTERNS.md (extract_oh_tools.py adaptation guidance lines 22-103)
  </read_first>
  <action>
    Create `resources/tools/extract_oh_tools.py`. Set `XLSX_PATH = Path(__file__).parent / "Table 1_One Health Tools Inventory_13Feb2024_ohjpa_Annex2_v4-1.xlsx"` and `OUTPUT_PATH = Path(__file__).parent.parent.parent / "content" / "oh-tools.json"`. Load with `openpyxl.load_workbook(XLSX_PATH, data_only=True)` and select the sheet by name `wb["Table 1 OHHLEP Tools Inventory"]` (NOT `wb.active`). Iterate rows explicitly with `for r in range(3, 53):` reading `ws.cell(r, col).value` — this excludes the title (row 1), header (row 2), and footnote/citation rows (54-64) per RESEARCH Pitfall 1. Skip a row if its cleaned name is empty OR its year is None (defense-in-depth against footnotes).

    Add a `clean()` helper `re.sub(r"\s+", " ", str(v).strip()) if v is not None else ""` and apply it to every text field (Pitfall 4 — C1 name and C3 organization contain embedded `\n`). Copy `slugify()` verbatim from extract_education.py; build `id` from the cleaned name and dedup with the md5 hash-suffix pattern.

    Map columns 1-indexed: C1→name (cleaned), C2→year as `int` (the row's year value), C3→organization (cleaned), C4→organizationLevels (split on `,`, clean each, normalize `International/\nRegional`→`International/Regional`, drop empties), C5→scopes (split on `,`, clean, drop empties), C8→audienceLevels (split on `,`, clean, drop empties), C9→audienceTypes (split on `,`, clean, drop empties), C11→description (cleaned), C13→url (cleaned then normalized). Skip C6/C7/C10/C12 (numeric codes / long prose).

    Write a `fix_url()` that returns `""` for empty input, returns the first `https?://\S+` match if the cell contains a buried URL in prose (Pitfall 2, e.g. R6 "Github.com/EIDSS\n\n..."), prefixes a bare `www.` or scheme-less host (e.g. R47 "www.who.int/...") with `https://`, and returns `""` if no URL-like token is found. Build each record as a dict matching the ToolItem field order: id, name, year, organization, organizationLevels, scopes, audienceLevels, audienceTypes, description, url. In `__main__`, `print(len(items))`, write the array to OUTPUT_PATH with `json.dump(items, f, ensure_ascii=False, indent=2)` + trailing newline. Then run the script to generate content/oh-tools.json.
  </action>
  <verify>
    <automated>python resources/tools/extract_oh_tools.py</automated>
  </verify>
  <acceptance_criteria>
    - `python resources/tools/extract_oh_tools.py` prints `50` (exactly 50 records, no footnotes)
    - resources/tools/extract_oh_tools.py contains `range(3, 53)` and `wb["Table 1 OHHLEP Tools Inventory"]`
    - resources/tools/extract_oh_tools.py contains a `re.sub(r"\s+", " "` whitespace-collapse helper
    - content/oh-tools.json is a JSON array of length 50; running `python -c "import json;d=json.load(open('content/oh-tools.json'));print(len(d), all(isinstance(t['year'],int) and t['name'] and isinstance(t['organizationLevels'],list) for t in d))"` prints `50 True`
    - No record's `name` contains a newline character; every `url` is either empty string or starts with `http`
  </acceptance_criteria>
  <done>extract_oh_tools.py produces content/oh-tools.json with exactly 50 ToolItem-shaped records, newlines collapsed and URLs normalized.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spreadsheet cell content → JSON → static HTML | Build-time data from a committed local file; not user-supplied at runtime |
| extracted URL fields → ToolCard href (Plan 21-02) | Untrusted destination strings originating from spreadsheet authors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | extract_oh_tools.py URL field (C13) | mitigate | fix_url() only emits `https://`-prefixed URLs or empty string; scheme-less/buried URLs normalized, no `javascript:`/`data:` schemes pass through |
| T-21-02 | Tampering | openpyxl parse of local xlsx | accept | File is committed to the repo (not user-supplied); openpyxl reads cell values only, no macro execution; low risk |
| T-21-03 | Injection | description/name cell text → JSON | mitigate | Text fields stored as plain strings; React escapes by default in Plan 21-02 (no dangerouslySetInnerHTML); whitespace collapsed, no executable content |
| T-21-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed this phase (openpyxl already present, verified Phase 20); RESEARCH Package Legitimacy Audit confirms zero installs — slopcheck N/A |
</threat_model>

<verification>
- `python resources/tools/extract_oh_tools.py` prints exactly `50`
- `npx tsc --noEmit` exits 0 with ToolItem in scope
- content/oh-tools.json parses as a 50-element array with all required ToolItem fields populated
</verification>

<success_criteria>
- content/oh-tools.json exists with exactly 50 records conforming to the exported ToolItem type
- The extraction script is committed, idempotent (re-running produces the same 50 records), and excludes footnote rows
- All URL fields are either empty or https-normalized; no embedded newlines remain in text fields
</success_criteria>

<output>
Create `.planning/phases/21-tools-directory-searchable-catalog-of-one-health-tools-and-r/21-01-SUMMARY.md` when done
</output>
