---
phase: 07-content-and-analytics
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - app/components/sections/AudienceCTAs.tsx
  - app/page.tsx
autonomous: false
requirements:
  - HOME-01

must_haves:
  truths:
    - "The homepage shows three side-by-side audience cards directly below the hero section"
    - "Each card has a distinct icon, empathetic headline, primary CTA button, and two secondary text links"
    - "Minister card primary CTA links to /briefs"
    - "Healthcare Worker and General Public primary CTAs use href='#' (future pages not yet live)"
    - "Future-page secondary links are visually present but rendered as href='#' with disabled styling"
    - "Section background is light grey (bg-slate-100), consistent with NewsletterSignup section"
  artifacts:
    - path: "app/components/sections/AudienceCTAs.tsx"
      provides: "Three audience-segmented CTA cards component"
      contains: "Landmark"
    - path: "app/page.tsx"
      provides: "AudienceCTAs inserted after HeroSection"
      contains: "AudienceCTAs"
  key_links:
    - from: "app/page.tsx"
      to: "app/components/sections/AudienceCTAs.tsx"
      via: "import and render after HeroSection"
      pattern: "AudienceCTAs"
    - from: "app/components/sections/AudienceCTAs.tsx"
      to: "lucide-react"
      via: "Landmark, Stethoscope, Users icon imports"
      pattern: "Landmark.*Stethoscope.*Users"
---

<objective>
Build the audience-segmented CTA section and insert it into the homepage directly below the hero. Three equal-column cards route ministers, healthcare workers, and general public to their most relevant content — making policymakers immediately feel seen when they land on the site.

Purpose: The homepage currently shows generic content sections. Audience-segmented CTAs make the first 30 seconds on the site immediately relevant — a minister sees their card, a healthcare worker sees theirs. This is the primary conversion mechanism for Phase 7.

Output:
- `app/components/sections/AudienceCTAs.tsx` — new section component (NEW)
- `app/page.tsx` — AudienceCTAs inserted after HeroSection
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/page.tsx
@app/components/sections/NewsletterSignup.tsx
@app/components/layout/Container.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AudienceCTAs component and wire into homepage</name>
  <files>app/components/sections/AudienceCTAs.tsx, app/page.tsx</files>
  <action>
**Step A — Create `app/components/sections/AudienceCTAs.tsx` (NEW):**

This is a Server Component (no interactivity required — all links are static). Do NOT add 'use client'.

Use lucide-react icons (already installed at ^1.6.0):
- Ministers: `Landmark` (government/parliament building)
- Healthcare Workers: `Stethoscope`
- General Public: `Users`

Use Container component from `@/components/layout/Container` for consistent page-width alignment (same pattern as other section components).

Background: `bg-slate-100` — same light grey as the NewsletterSignup section (consistent visual rhythm on the page).

**Disabled link pattern for future pages (Phase 8 /awareness, Phase 10 /take-action):**
- Use `href="#"` with `aria-disabled="true"`
- Visual styling: `text-slate-400 cursor-not-allowed pointer-events-none` — visually present but clearly inactive
- Do NOT add "(coming soon)" text — the user did not request this and it is noisy

**Copy tone: empathetic register — "Find resources for your role" not directive command tone.**

```tsx
// app/components/sections/AudienceCTAs.tsx
import Link from 'next/link';
import { Landmark, Stethoscope, Users, LucideIcon } from 'lucide-react';
import { Container } from '@/components/layout/Container';

interface SecondaryLink {
  label: string;
  href: string;
  live: boolean;  // false = future page, renders disabled
}

interface AudienceCard {
  icon: LucideIcon;
  headline: string;
  subtext: string;
  primaryLabel: string;
  primaryHref: string;
  primaryLive: boolean;
  secondaryLinks: SecondaryLink[];
}

const audiences: AudienceCard[] = [
  {
    icon: Landmark,
    headline: 'For Policymakers & Ministers',
    subtext: 'Find evidence-backed briefs to inform national AMR strategy and budget decisions.',
    primaryLabel: 'Browse Policy Briefs',
    primaryHref: '/briefs',
    primaryLive: true,
    secondaryLinks: [
      { label: 'AMR Awareness Hub', href: '#', live: false },
      { label: 'Take Action', href: '#', live: false },
    ],
  },
  {
    icon: Stethoscope,
    headline: 'For Healthcare Workers',
    subtext: 'Access stewardship resources and clinical guidance relevant to your role on the frontline.',
    primaryLabel: 'Explore Resources',
    primaryHref: '#',
    primaryLive: false,
    secondaryLinks: [
      { label: 'Policy Briefs', href: '/briefs', live: true },
      { label: 'Take Action', href: '#', live: false },
    ],
  },
  {
    icon: Users,
    headline: 'For the General Public',
    subtext: 'Learn what antimicrobial resistance means for your community and what you can do.',
    primaryLabel: 'Learn What You Can Do',
    primaryHref: '#',
    primaryLive: false,
    secondaryLinks: [
      { label: 'Policy Briefs', href: '/briefs', live: true },
      { label: 'AMR Awareness Hub', href: '#', live: false },
    ],
  },
];

export default function AudienceCTAs() {
  return (
    <section className="bg-slate-100 py-16">
      <Container>
        <div className="text-center mb-10">
          <h2 className="font-serif text-2xl md:text-3xl text-navy-950 font-bold mb-3">
            Find resources for your role
          </h2>
          <p className="text-slate-600 max-w-xl mx-auto">
            AMR touches every sector. Navigate to the content most relevant to how you work.
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
          {audiences.map((audience) => {
            const Icon = audience.icon;
            return (
              <div
                key={audience.headline}
                className="bg-white rounded-lg p-6 shadow-sm border border-slate-200 flex flex-col"
              >
                <div className="w-12 h-12 rounded-full bg-teal-50 flex items-center justify-center mb-4 flex-shrink-0">
                  <Icon size={24} className="text-teal-600" />
                </div>

                <h3 className="font-serif text-navy-950 font-bold text-lg mb-2">
                  {audience.headline}
                </h3>
                <p className="text-slate-600 text-sm leading-relaxed mb-6 flex-grow">
                  {audience.subtext}
                </p>

                {/* Primary CTA */}
                {audience.primaryLive ? (
                  <Link
                    href={audience.primaryHref}
                    className="inline-flex items-center justify-center bg-teal-600 hover:bg-teal-500 text-white font-medium px-5 py-2.5 rounded transition-colors text-sm mb-4"
                  >
                    {audience.primaryLabel}
                  </Link>
                ) : (
                  <span
                    aria-disabled="true"
                    className="inline-flex items-center justify-center bg-slate-300 text-slate-500 font-medium px-5 py-2.5 rounded text-sm mb-4 cursor-not-allowed"
                  >
                    {audience.primaryLabel}
                  </span>
                )}

                {/* Secondary links */}
                <div className="flex gap-4 justify-center">
                  {audience.secondaryLinks.map((link) =>
                    link.live ? (
                      <Link
                        key={link.label}
                        href={link.href}
                        className="text-sm text-teal-600 hover:text-teal-500 underline underline-offset-2"
                      >
                        {link.label}
                      </Link>
                    ) : (
                      <span
                        key={link.label}
                        aria-disabled="true"
                        className="text-sm text-slate-400 cursor-not-allowed"
                      >
                        {link.label}
                      </span>
                    )
                  )}
                </div>
              </div>
            );
          })}
        </div>
      </Container>
    </section>
  );
}
```

**Step B — Update `app/page.tsx`:**
1. Add import: `import AudienceCTAs from '@/components/sections/AudienceCTAs';`
2. Insert `<AudienceCTAs />` directly after `<HeroSection conferenceDate={site.conferenceDate} />` and before `<StatStrip stats={site.stats} />`

Updated page.tsx should read:
```tsx
import { getSiteContent, getFeaturedBrief } from '@/lib/content';
import HeroSection from '@/components/sections/HeroSection';
import AudienceCTAs from '@/components/sections/AudienceCTAs';
import StatStrip from '@/components/sections/StatStrip';
import ThreePillars from '@/components/sections/ThreePillars';
import FeaturedBrief from '@/components/sections/FeaturedBrief';
import PartnerLogos from '@/components/sections/PartnerLogos';
import NewsletterSignup from '@/components/sections/NewsletterSignup';

export default function HomePage() {
  const site = getSiteContent();
  const featured = getFeaturedBrief();

  return (
    <>
      <HeroSection conferenceDate={site.conferenceDate} />
      <AudienceCTAs />
      <StatStrip stats={site.stats} />
      <ThreePillars />
      {featured && <FeaturedBrief brief={featured} />}
      <PartnerLogos partners={site.partners} />
      <NewsletterSignup />
    </>
  );
}
```
  </action>
  <verify>
1. `npx tsc --noEmit` — exits 0
2. `npm run build` — completes without errors
3. `grep "AudienceCTAs" app/page.tsx` — confirms import and render
4. `grep "Landmark.*Stethoscope\|Stethoscope.*Landmark" app/components/sections/AudienceCTAs.tsx` — confirms icon imports
  </verify>
  <done>AudienceCTAs component exists with 3 equal-column cards (Landmark/Stethoscope/Users icons, empathetic copy, primary buttons, secondary links). Inserted in homepage after HeroSection. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 7 visually — briefs library, audience CTAs, infographics, fonts</name>
  <what-built>
Phase 7 complete — all three plans executed:
- Plan 07-01: Type schema fix, GA4 analytics helpers, font wiring (Montserrat + Inter), BriefCard conditional infographic button
- Plan 07-02: 15 real brief JSON entries, PDF/JPEG assets in public/, DownloadButton client component, brief detail page infographic section, NewsletterSignup analytics
- Plan 07-03: AudienceCTAs section on homepage (3 audience cards)
  </what-built>
  <how-to-verify>
Run `npm run dev` and check the following:

1. **Homepage audience cards** — Visit http://localhost:3000
   - Three side-by-side cards visible directly below the hero
   - Cards: "For Policymakers & Ministers" (building icon), "For Healthcare Workers" (stethoscope), "For the General Public" (users icon)
   - "Browse Policy Briefs" button on minister card links to /briefs
   - Healthcare Worker and General Public primary buttons appear greyed/disabled
   - Section background is light grey, consistent with newsletter section at bottom

2. **Briefs library** — Visit http://localhost:3000/briefs
   - 15 brief cards visible (not 3)
   - Real titles visible (e.g. "Multi Country PEA", "Why countries struggle with AMR")
   - No placeholder/synthetic content
   - Brief cards WITHOUT infographic PDFs show only one download button (PDF only, no Infographic button)

3. **Brief detail page** — Visit a brief that has an infographic (the Rwanda-specific brief)
   - PDF download button present at top of page
   - Infographic image renders inline below Key Messages
   - Clicking the image opens full-size in new browser tab

4. **Typography** — Check any page
   - Headings use Montserrat (bold, geometric sans-serif)
   - Body text uses Inter

5. **GA4** (optional — requires real GA4 Measurement ID in .env.local):
   - If NEXT_PUBLIC_GA4_MEASUREMENT_ID is set to a real G-XXXXX value, open GA4 realtime dashboard
   - Navigate the site — pageview events should fire
   - Click a PDF download button — pdf_download event should appear in GA4 realtime

6. **Build** — Run `npm run build` — must complete without errors, 15 brief pages in output
  </how-to-verify>
  <action>Human visual verification — run npm run dev and test each item listed in how-to-verify above.</action>
  <verify>Run npm run build — must complete without errors.</verify>
  <done>All visual checks approved: 3 audience CTA cards on homepage, 15 brief cards in library, infographic inline on Rwanda brief, Montserrat headings visible, build clean.</done>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` — static export completes, all 15 brief slugs in generateStaticParams output
2. Homepage renders AudienceCTAs after HeroSection
3. All 3 audience cards present with correct icons, headlines, and link states
4. Minister primary CTA routes to /briefs
5. Disabled future-page links use href="#" with visual disabled styling
</verification>

<success_criteria>
- AudienceCTAs component renders 3 equal-column cards on homepage
- Cards appear directly below hero section, above StatStrip
- Light grey (bg-slate-100) section background — consistent with newsletter section
- Icons: Landmark (minister), Stethoscope (HCW), Users (public) from lucide-react
- Primary CTAs: /briefs live, /awareness and /take-action rendered as href="#" disabled
- Empathetic copy tone throughout
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-and-analytics/07-03-SUMMARY.md`
</output>
