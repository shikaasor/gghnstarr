---
phase: 10-take-action-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/components/take-action/ToolkitSection.tsx
  - app/components/take-action/DownloadCard.tsx
  - public/toolkit/.gitkeep
autonomous: false
requirements:
  - ACTN-04

must_haves:
  truths:
    - "The /take-action page shows a full-width Advocacy Toolkit section below the form cards"
    - "The toolkit section has an introductory heading and 1-2 sentence blurb explaining its purpose"
    - "Three download cards are visible in a 3-column grid on desktop, stacking to 1 column on mobile"
    - "Each card shows a title, short description, and a format label (e.g. [PDF])"
    - "Each card has a Download button that resolves to a file in /toolkit/ and triggers a file save (not a new tab)"
    - "public/toolkit/ directory exists and is tracked in git (contains .gitkeep)"
  artifacts:
    - path: "app/components/take-action/ToolkitSection.tsx"
      provides: "Server component rendering the toolkit section with heading, blurb, and DownloadCard grid"
      exports: ["default ToolkitSection"]
    - path: "app/components/take-action/DownloadCard.tsx"
      provides: "Reusable download card component with title, description, format badge, and download anchor"
      exports: ["DownloadCard"]
    - path: "public/toolkit/.gitkeep"
      provides: "Ensures public/toolkit/ directory is committed and ready to receive real asset files"
  key_links:
    - from: "app/components/take-action/ToolkitSection.tsx"
      to: "app/components/take-action/DownloadCard.tsx"
      via: "array.map() rendering DownloadCard"
      pattern: "DownloadCard"
    - from: "app/components/take-action/DownloadCard.tsx"
      to: "/toolkit/*.pdf"
      via: "<a href download> attribute"
      pattern: "download="
---

<objective>
Build the Advocacy Toolkit section for the /take-action page: a DownloadCard component, the ToolkitSection server component with three placeholder asset cards, and the public/toolkit/ directory ready to receive real files.

Purpose: Provides the third action path — downloadable AMR advocacy materials (fact sheet, letter template, social media card) — completing the /take-action page.
Output: ToolkitSection component with 3-column download card grid, DownloadCard component, public/toolkit/ directory with .gitkeep. Ends with human visual verification of the complete /take-action page.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-take-action-page/10-RESEARCH.md
@app/components/briefs/DownloadButton.tsx
@app/take-action/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: DownloadCard component, ToolkitSection, public/toolkit/ directory</name>
  <files>
    app/components/take-action/DownloadCard.tsx
    app/components/take-action/ToolkitSection.tsx
    public/toolkit/.gitkeep
  </files>
  <action>
    Create `public/toolkit/.gitkeep` — empty file to track the directory in git. This is where real asset files (amr-fact-sheet.pdf, amr-letter-template.docx, amr-social-card.png) will be dropped by the user.

    Create `app/components/take-action/DownloadCard.tsx` — pure display component (no 'use client' needed):
    - Props interface:
      ```ts
      interface DownloadCardProps {
        title: string;
        description: string;
        format: string;       // 'PDF', 'DOCX', 'PNG', etc.
        href: string;         // e.g. '/toolkit/amr-fact-sheet.pdf'
        filename: string;     // download filename attribute
      }
      ```
    - Render: white bg card, rounded-lg, border border-slate-200, shadow-sm, p-5, flex flex-col gap-3
    - Header row: title (font-serif, navy-950, font-semibold) and format badge on same row — format badge: `text-xs bg-slate-100 text-slate-600 px-2 py-0.5 rounded font-mono shrink-0` with text `[{format}]`
    - Description: text-slate-600 text-sm leading-relaxed flex-grow
    - Download anchor: `<a href={href} download={filename}>` — styled as bordered button: `inline-flex items-center gap-2 border border-teal-600 text-teal-600 hover:bg-teal-50 font-medium px-4 py-2 rounded transition-colors text-sm self-start`
    - Include Download icon from lucide-react (size 14) before "Download" text
    - IMPORTANT: href must be a relative path starting with '/toolkit/' — NOT an absolute external URL. The `download` attribute only works for same-origin URLs. Since assets live in public/toolkit/, they are served from the same origin.

    Create `app/components/take-action/ToolkitSection.tsx` — Server Component (no 'use client'):
    - Import DownloadCard
    - Define a typed array of toolkit assets:
      ```ts
      const toolkitAssets = [
        {
          title: 'AMR Fact Sheet',
          description: 'A one-page overview of antimicrobial resistance in Africa — key statistics, causes, and consequences. Share with your network.',
          format: 'PDF',
          href: '/toolkit/amr-fact-sheet.pdf',
          filename: 'amr-fact-sheet.pdf',
        },
        {
          title: 'Advocacy Letter Template',
          description: 'A customizable letter template for healthcare workers and advocates to request action from local health authorities on AMR.',
          format: 'DOCX',
          href: '/toolkit/amr-letter-template.docx',
          filename: 'amr-letter-template.docx',
        },
        {
          title: 'Social Media Card',
          description: 'A shareable graphic for social media platforms promoting AMR awareness. Optimized for WhatsApp, Twitter/X, and LinkedIn.',
          format: 'PNG',
          href: '/toolkit/amr-social-card.png',
          filename: 'amr-social-card.png',
        },
      ];
      ```
    - Render:
      - Section heading: `<h2 className="font-serif text-2xl md:text-3xl text-navy-950 font-bold mb-3">Advocacy Toolkit</h2>`
      - Blurb: `<p className="text-slate-600 max-w-2xl mb-10">Equip yourself and your community with materials to communicate the urgency of AMR in Africa. Download, share, and adapt these resources freely.</p>`
      - Card grid: `<div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">` mapping over toolkitAssets to render DownloadCard for each
    - NOTE: Replace the stub ToolkitSection.tsx created in Plan 01 Task 1 (if it exists as a placeholder returning null) with this full implementation.

    Verify `app/take-action/page.tsx` already imports ToolkitSection from '@/components/take-action/ToolkitSection' — no import change needed since the stub is replaced in-place.
  </action>
  <verify>
    Run `npm run build` — zero TypeScript errors.
    Run `npm run dev`, visit http://localhost:3000/take-action — scroll to the bottom of the page.
    Confirm "Advocacy Toolkit" heading and blurb paragraph are visible.
    Confirm three download cards are visible in a grid: "AMR Fact Sheet", "Advocacy Letter Template", "Social Media Card".
    Each card shows title, description, format badge ([PDF], [DOCX], [PNG]), and Download button.
    Inspect a Download button's href attribute in DevTools — confirm it is /toolkit/amr-fact-sheet.pdf (same-origin path, not absolute external URL).
  </verify>
  <done>
    ToolkitSection renders three DownloadCard components in a responsive grid. All download anchors use same-origin /toolkit/ paths with the download attribute. public/toolkit/ directory exists and is git-tracked. ToolkitSection renders without errors in the /take-action page shell.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 10 visual verification</name>
  <files></files>
  <action>Start dev server with `npm run dev` and follow the verification steps in how-to-verify below.</action>
  <verify>User confirms all items in how-to-verify pass and types "approved".</verify>
  <done>Phase 10 visual sign-off received from user.</done>
  <what-built>
    Phase 10 complete — /take-action page with all three sections:
    - Hero section (dark navy background, heading, subtext)
    - Two form cards side-by-side (Pledge pre-expanded by default; Commitment collapses; click to expand)
    - Expand/collapse interaction with session lock after submission
    - ActionToast success notification on form submit
    - Advocacy Toolkit section (3-column download card grid, introductory blurb)
    - Header nav "Take Action" as a gold-colored filled button (desktop + mobile)
    - AudienceCTAs homepage deep links now live (/take-action#pledge, /take-action#commitment)
  </what-built>
  <how-to-verify>
    Run: npm run dev

    1. TAKE ACTION PAGE — LAYOUT (http://localhost:3000/take-action)
       - Confirm dark navy hero section at the top with "Take Action" heading
       - Confirm two form cards in a side-by-side grid below the hero on desktop
       - Confirm Pledge card is pre-expanded showing all four form fields (name, country, role, commitment statement)
       - Confirm Commitment card is collapsed (shows only title + description + chevron icon)

    2. FORM CARD INTERACTIONS
       - Click the Commitment card header — it should expand and show its four fields; Pledge card should collapse
       - Click the Pledge card header — it should expand and Commitment card should collapse
       - Direct URL: visit http://localhost:3000/take-action#commitment — page should load with Commitment card expanded

    3. FORM SUBMISSION (error state test)
       - Fill in the Pledge form with test data
       - Click Submit — form should show an inline error below the button (GAS URL is a placeholder, so this will fail)
       - Confirm the form stays open after the error; no card collapse occurs on error

    4. MOBILE LAYOUT
       - Resize browser to < 768px
       - Confirm two form cards stack vertically (full-width, one above the other)
       - Confirm Advocacy Toolkit cards also stack to single-column

    5. ADVOCACY TOOLKIT SECTION
       - Scroll to the bottom of /take-action
       - Confirm "Advocacy Toolkit" heading and blurb are visible
       - Confirm three download cards are visible: AMR Fact Sheet [PDF], Advocacy Letter Template [DOCX], Social Media Card [PNG]
       - Confirm each card has a Download button (clicking will 404 since files are placeholders — this is expected)

    6. HEADER NAV — TAKE ACTION BUTTON
       - On desktop: confirm "Take Action" appears as a visually distinct gold/yellow filled button in the header
       - On mobile (hamburger open): confirm "Take Action" appears with gold/amber styling, distinct from plain nav links

    7. HOMEPAGE AUDIENCE CTAs (http://localhost:3000)
       - Scroll to the "Find resources for your role" section
       - Confirm "Take Action" under the Policymakers card is a clickable teal underlined link (not a grey disabled span)
       - Click it — should navigate to /take-action#pledge with Pledge card expanded
       - Confirm "Take Action" under the Healthcare Workers card links to /take-action#commitment
  </how-to-verify>
  <resume-signal>Type "approved" to proceed, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes with zero errors
- ToolkitSection renders 3 DownloadCard components with correct /toolkit/ hrefs
- public/toolkit/.gitkeep exists and is committed
- Full /take-action page passes visual inspection (hero + form cards + toolkit)
- Header nav "Take Action" button is visually distinguished (gold fill)
- AudienceCTAs deep links are live and correctly targeted
</verification>

<success_criteria>
1. DownloadCard component accepts title, description, format, href, filename props and renders correctly
2. ToolkitSection displays 3 asset cards with an introductory heading and blurb
3. All download anchors use same-origin /toolkit/ paths with the download attribute (not cross-origin URLs)
4. public/toolkit/ directory is created and tracked in git
5. Complete /take-action page passes human visual verification (all three sections visible, form card interactions work)
</success_criteria>

<output>
After completion, create `.planning/phases/10-take-action-page/10-02-SUMMARY.md` following the summary template.
</output>
