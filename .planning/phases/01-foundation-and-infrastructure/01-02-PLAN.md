---
phase: 01-foundation-and-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/lib/types.ts
  - app/lib/content.ts
  - content/briefs-index.json
  - content/experts.json
  - content/site.json
autonomous: true
requirements:
  - FOUN-02

must_haves:
  truths:
    - "TypeScript interfaces for Brief, Expert, and SiteContent exist and match the locked schema exactly"
    - "lib/content.ts exports getAllBriefs, getBriefBySlug, getExperts, and getSiteContent"
    - "getAllBriefs() returns 3 sample briefs sorted by weekNumber"
    - "getBriefBySlug('week-01-amr-governance') returns the correct brief object"
    - "getExperts() returns at least 1 expert record"
    - "getSiteContent() returns the SiteContent object with tagline, partners, and contactEmail"
    - "JSON files are valid (no parse errors) and match their TypeScript interfaces"
  artifacts:
    - path: "app/lib/types.ts"
      provides: "Brief, Expert, SiteContent TypeScript interfaces"
      exports:
        - Brief
        - Expert
        - SiteContent
    - path: "app/lib/content.ts"
      provides: "Content functions reading from JSON files at build time"
      exports:
        - getAllBriefs
        - getBriefBySlug
        - getExperts
        - getSiteContent
    - path: "content/briefs-index.json"
      provides: "Seed data — 3 brief entries"
      contains: "weekNumber"
    - path: "content/experts.json"
      provides: "Expert records (single source of truth)"
      contains: "authorId"
    - path: "content/site.json"
      provides: "Site-wide content (tagline, partners, contact)"
      contains: "tagline"
  key_links:
    - from: "app/lib/content.ts"
      to: "content/briefs-index.json"
      via: "fs.readFileSync at build time"
      pattern: "briefs-index\\.json"
    - from: "content/briefs-index.json"
      to: "content/experts.json"
      via: "authorId cross-reference"
      pattern: "authorId"
    - from: "app/lib/content.ts"
      to: "app/lib/types.ts"
      via: "TypeScript import"
      pattern: "from.*types"
---

<objective>
Implement the content data layer: TypeScript interfaces for all content types, build-time content functions that read from JSON files, and three JSON data files seeded with sample data.

Purpose: All pages in Phases 2-4 consume data through this layer. Getting the interfaces and functions right now prevents schema-breaking changes later. The Brief interface fields are LOCKED by user decision — implement them exactly.
Output: `app/lib/types.ts`, `app/lib/content.ts`, and three JSON files in `content/` with sample data.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-and-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-foundation-and-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TypeScript interfaces and content functions</name>
  <files>
    app/lib/types.ts
    app/lib/content.ts
  </files>
  <action>
    Create the `app/lib/` directory and two files.

    **File 1: `app/lib/types.ts`**

    All Brief fields are LOCKED by user decision. Implement them exactly — do not add, remove, or rename any field. Expert and SiteContent have required fields plus discretion fields documented below.

    ```typescript
    // app/lib/types.ts

    // LOCKED — all fields mandated by CONTEXT.md decisions
    export interface Brief {
      slug: string;                    // URL-safe identifier: "week-01-amr-governance"
      title: string;                   // Full brief title
      weekNumber: number;              // Publication week (1, 2, 3...)
      publicationDate: string;         // ISO date string: "2026-03-24"
      authorId: string;                // References Expert.id in experts.json
      keyTakeaway: string;             // One-sentence key insight
      executiveSummary: string;        // 100-150 word summary
      keyMessages: string[];           // 3-7 bullet points
      pdfUrl: string;                  // "/briefs/week-01-amr-governance.pdf"
      infographicPdfUrl: string;       // "/infographics/week-01-infographic.pdf"
      thumbnailUrl: string;            // "/images/thumbnails/week-01.jpg"
      themes: string[];                // Subset of: "Governance" | "Laboratory Systems" |
                                       // "Predictive Analytics" | "One Health" | "Stewardship"
    }

    // REQUIRED: id, name, bio, photoUrl, specialties required by Phase 4
    // DISCRETION: title, organization, linkedinUrl — use judgment
    export interface Expert {
      id: string;                      // Referenced by Brief.authorId
      name: string;
      title: string;                   // Professional title
      organization: string;            // Institutional affiliation
      bio: string;                     // 200-word professional bio
      photoUrl: string;                // "/images/experts/{slug}.jpg"
      specialties: string[];           // e.g. ["AMR Surveillance", "Genomics"]
      linkedinUrl?: string;            // Optional LinkedIn profile URL
    }

    // DISCRETION — structure serves getSiteContent(); designed for Phase 2 homepage needs
    export interface SiteContent {
      siteTitle: string;
      tagline: string;                 // "Evidence. Advocacy. Action."
      conferenceDate: string;          // "June 28, 2026"
      conferenceLocation: string;      // Used in hero badge
      partners: Array<{
        name: string;
        logoUrl: string;               // "/images/partners/{slug}.png"
        url?: string;                  // Optional partner website
      }>;
      contactEmail: string;
      linkedinUrl?: string;
      footerTagline: string;           // Short tagline shown in footer
    }
    ```

    **File 2: `app/lib/content.ts`**

    This file reads JSON at build time using Node.js `fs`. It runs on the build server only — never at runtime in the static export. The `content/` directory is at the project root (same level as `app/`).

    ```typescript
    // app/lib/content.ts
    import fs from 'fs';
    import path from 'path';
    import type { Brief, Expert, SiteContent } from './types';

    const CONTENT_DIR = path.join(process.cwd(), 'content');

    export function getAllBriefs(): Brief[] {
      const raw = fs.readFileSync(path.join(CONTENT_DIR, 'briefs-index.json'), 'utf-8');
      const briefs: Brief[] = JSON.parse(raw);
      return briefs.sort((a, b) => a.weekNumber - b.weekNumber);
    }

    export function getBriefBySlug(slug: string): Brief | undefined {
      return getAllBriefs().find((b) => b.slug === slug);
    }

    export function getExperts(): Expert[] {
      const raw = fs.readFileSync(path.join(CONTENT_DIR, 'experts.json'), 'utf-8');
      return JSON.parse(raw) as Expert[];
    }

    export function getSiteContent(): SiteContent {
      const raw = fs.readFileSync(path.join(CONTENT_DIR, 'site.json'), 'utf-8');
      return JSON.parse(raw) as SiteContent;
    }
    ```

    NOTE: `process.cwd()` returns the project root during `next build`. This is the correct approach for a static export — do not use `import()` or dynamic imports for JSON files, as those patterns have different resolution behavior.
  </action>
  <verify>
    Run `npx tsc --noEmit` — must pass with no type errors.
    Check that both files exist: `ls app/lib/types.ts app/lib/content.ts`
  </verify>
  <done>
    `app/lib/types.ts` exports Brief (all 12 locked fields), Expert, and SiteContent interfaces. `app/lib/content.ts` exports getAllBriefs, getBriefBySlug, getExperts, and getSiteContent. TypeScript compilation passes with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create JSON seed data files</name>
  <files>
    content/briefs-index.json
    content/experts.json
    content/site.json
  </files>
  <action>
    Create the `content/` directory at the project root and populate three JSON files with realistic sample data. The data must fully conform to the TypeScript interfaces defined in Task 1.

    **File 1: `content/experts.json`**

    Create this file first — briefs reference experts by `authorId`. Use 2 expert records to support the 3 sample briefs (briefs can share authors). IDs must match the `authorId` values in briefs-index.json exactly.

    ```json
    [
      {
        "id": "olawale-oladipo",
        "name": "Dr. Olawale Oladipo",
        "title": "Lead Epidemiologist",
        "organization": "GGHN (Good Governance for Health in Nigeria)",
        "bio": "Dr. Olawale Oladipo is Lead Epidemiologist at GGHN, where he leads the STARR Africa AMR Modeling Initiative. He specializes in antimicrobial resistance surveillance and predictive epidemiological modeling across sub-Saharan Africa. With over 15 years of experience in infectious disease epidemiology, Dr. Oladipo has collaborated with WHO AFRO, Africa CDC, and the Fleming Fund to design surveillance frameworks that bridge laboratory data and national AMR policy. He holds a PhD in Epidemiology from the University of Lagos and an MSc from the London School of Hygiene and Tropical Medicine. His current work focuses on deploying SEIR and Bayesian transmission models to project AMR burden under different antibiotic stewardship scenarios for use by African health ministers.",
        "photoUrl": "/images/experts/olawale-oladipo.jpg",
        "specialties": ["AMR Surveillance", "Predictive Epidemiology", "Bayesian Modeling", "One Health"],
        "linkedinUrl": "https://linkedin.com/in/olawale-oladipo"
      },
      {
        "id": "amina-ibrahim",
        "name": "Dr. Amina Ibrahim",
        "title": "Senior Research Scientist",
        "organization": "Africa CDC — Division of Laboratory Systems",
        "bio": "Dr. Amina Ibrahim is Senior Research Scientist at the Africa CDC Division of Laboratory Systems, focusing on genomic surveillance infrastructure for antimicrobial resistance across the African continent. She leads the STARR initiative's laboratory systems workstream, coordinating whole-genome sequencing capacity building in 12 African Union member states. Dr. Ibrahim trained in clinical microbiology at the University of Nairobi and completed postdoctoral research in genomic epidemiology at the Wellcome Sanger Institute. Her policy work centres on translating genomic AMR data into actionable national action plans, and she serves as a technical advisor to the WHO AFRO AMR country support programme. She is a co-author of the GGHN STARR methodology framework underpinning all published policy briefs.",
        "photoUrl": "/images/experts/amina-ibrahim.jpg",
        "specialties": ["Genomic Surveillance", "Laboratory Systems", "Clinical Microbiology", "Policy Translation"],
        "linkedinUrl": "https://linkedin.com/in/amina-ibrahim-africacdc"
      }
    ]
    ```

    **File 2: `content/briefs-index.json`**

    Seed with 3 briefs covering different themes. The `authorId` values MUST match `id` values in experts.json exactly.

    ```json
    [
      {
        "slug": "week-01-amr-governance-frameworks",
        "title": "Strengthening National AMR Governance Frameworks in West Africa",
        "weekNumber": 1,
        "publicationDate": "2026-03-10",
        "authorId": "olawale-oladipo",
        "keyTakeaway": "Countries with legislated AMR national action plans show 34% lower broad-spectrum antibiotic consumption compared to countries with only voluntary guidelines.",
        "executiveSummary": "This policy brief examines governance structures for antimicrobial resistance (AMR) management across 15 West African nations. Drawing on NIPAD platform surveillance data and WHO AFRO country assessments, we find a significant correlation between legally mandated national AMR action plans and reduced broad-spectrum antibiotic consumption. Countries with legislation in place — including Ghana, Senegal, and Côte d'Ivoire — demonstrate substantially lower consumption indices than neighboring countries relying on voluntary frameworks. Our predictive models project that without governance strengthening, the region faces a 40% increase in AMR-attributable mortality by 2035. We recommend immediate legislative action to formalize AMR oversight committees with dedicated budget lines, mandatory hospital antibiotic stewardship programs, and cross-border surveillance data-sharing agreements under the Africa CDC continental AMR framework.",
        "keyMessages": [
          "Legislated AMR national action plans reduce broad-spectrum antibiotic consumption by an average of 34% compared to voluntary guidelines",
          "Only 4 of 15 surveyed West African countries have legally binding AMR governance frameworks as of 2026",
          "NIPAD platform data shows AMR-attributable mortality is increasing at 8% annually in countries without structured governance",
          "Cross-border data sharing under Africa CDC frameworks reduces surveillance blind spots that drive AMR spread",
          "Dedicated AMR budget lines (minimum 0.5% of health ministry budgets) correlate with effective stewardship program implementation"
        ],
        "pdfUrl": "/briefs/week-01-amr-governance-frameworks.pdf",
        "infographicPdfUrl": "/infographics/week-01-amr-governance-frameworks-infographic.pdf",
        "thumbnailUrl": "/images/thumbnails/week-01-amr-governance-frameworks.jpg",
        "themes": ["Governance", "Stewardship"]
      },
      {
        "slug": "week-02-laboratory-systems-capacity",
        "title": "Accelerating AMR Laboratory Surveillance Capacity in East Africa",
        "weekNumber": 2,
        "publicationDate": "2026-03-17",
        "authorId": "amina-ibrahim",
        "keyTakeaway": "Whole-genome sequencing surveillance in regional hub laboratories can achieve continental AMR pathogen coverage at one-quarter the cost of building national capacity in every country.",
        "executiveSummary": "Effective AMR surveillance requires culture-based and genomic laboratory capacity that most African countries currently lack. This brief presents a hub-and-spoke model for East Africa, where three regional reference laboratories (Kenya, Ethiopia, Tanzania) equipped with whole-genome sequencing infrastructure serve as analytical hubs for nine surrounding countries. STARR modeling using GlobalPPS data demonstrates that this architecture achieves 87% pathogen coverage at 24% of the capital cost of independent national build-outs. The Fleming Fund has demonstrated proof-of-concept in the Western Pacific; we provide a costed adaptation roadmap for East Africa. Policy-makers are urged to endorse the Africa CDC Regional Integrated Surveillance and Laboratory Network (RISLNET) expansion proposal at the June 2026 Inter-Ministerial Conference.",
        "keyMessages": [
          "A hub-and-spoke regional laboratory model achieves 87% AMR pathogen coverage at 24% of the cost of independent national capacity",
          "Three strategically positioned reference laboratories in Kenya, Ethiopia, and Tanzania can serve all nine East African Community member states",
          "Whole-genome sequencing turnaround of under 72 hours is achievable with current platform costs and existing trained personnel",
          "RISLNET expansion requires political endorsement at ministerial level to unlock Fleming Fund co-financing commitments",
          "Specimen transport cold-chain protocols are the critical operational bottleneck — standard SOPs are available from Africa CDC and ready for adoption"
        ],
        "pdfUrl": "/briefs/week-02-laboratory-systems-capacity.pdf",
        "infographicPdfUrl": "/infographics/week-02-laboratory-systems-capacity-infographic.pdf",
        "thumbnailUrl": "/images/thumbnails/week-02-laboratory-systems-capacity.jpg",
        "themes": ["Laboratory Systems", "One Health"]
      },
      {
        "slug": "week-03-predictive-analytics-amr-burden",
        "title": "Predictive Modeling of AMR Burden Under Antibiotic Policy Scenarios",
        "weekNumber": 3,
        "publicationDate": "2026-03-24",
        "authorId": "olawale-oladipo",
        "keyTakeaway": "STARR SEIR-Bayesian models project that a 20% reduction in unnecessary outpatient antibiotic prescriptions could prevent 1.2 million AMR deaths across Africa by 2035.",
        "executiveSummary": "The GGHN STARR initiative deploys a coupled SEIR-Bayesian transmission model calibrated against WHONET and GlobalPPS surveillance data to project AMR burden trajectories under alternative antibiotic policy scenarios. This brief presents scenario analyses for three intervention packages — enhanced diagnostic stewardship, community prescribing guidelines, and hospital formulary restrictions — modeled across 22 African countries. Results indicate that combined interventions yield non-linear synergistic effects: simultaneous implementation of all three packages is projected to prevent 40% more deaths than the sum of individual interventions. The NIPAD platform visualizations underpinning these projections are available to national health ministries upon request. We call on ministers to commission country-level scenario analyses to inform national AMR investment plans ahead of the June 2026 conference.",
        "keyMessages": [
          "A 20% reduction in unnecessary outpatient antibiotic prescriptions is projected to prevent 1.2 million AMR deaths by 2035 across the continent",
          "Combined diagnostic stewardship, community guidelines, and hospital formulary restrictions show 40% greater impact than sum of individual interventions",
          "SEIR-Bayesian models are now calibrated for 22 African countries using WHONET and GlobalPPS surveillance data as of Q1 2026",
          "Country-specific scenario analyses are available to health ministries via the NIPAD platform — contact GGHN STARR to request access",
          "Investment in AMR interventions shows a 12:1 return ratio when modeled against averted hospitalizations and productivity losses"
        ],
        "pdfUrl": "/briefs/week-03-predictive-analytics-amr-burden.pdf",
        "infographicPdfUrl": "/infographics/week-03-predictive-analytics-amr-burden-infographic.pdf",
        "thumbnailUrl": "/images/thumbnails/week-03-predictive-analytics-amr-burden.jpg",
        "themes": ["Predictive Analytics", "Stewardship", "One Health"]
      }
    ]
    ```

    **File 3: `content/site.json`**

    ```json
    {
      "siteTitle": "GGHN STARR",
      "tagline": "Evidence. Advocacy. Action.",
      "conferenceDate": "June 28, 2026",
      "conferenceLocation": "Abuja, Nigeria",
      "partners": [
        {
          "name": "GGHN (Good Governance for Health in Nigeria)",
          "logoUrl": "/images/partners/gghn.png",
          "url": "https://gghn.org.ng"
        },
        {
          "name": "Fleming Fund",
          "logoUrl": "/images/partners/fleming-fund.png",
          "url": "https://www.flemingfund.org"
        },
        {
          "name": "Africa CDC",
          "logoUrl": "/images/partners/africa-cdc.png",
          "url": "https://africacdc.org"
        },
        {
          "name": "WHO AFRO",
          "logoUrl": "/images/partners/who-afro.png",
          "url": "https://www.afro.who.int"
        }
      ],
      "contactEmail": "starr@gghn.org.ng",
      "linkedinUrl": "https://linkedin.com/company/gghn-nigeria",
      "footerTagline": "GGHN STARR Africa AMR Modeling Initiative"
    }
    ```

    After creating all three files, create placeholder directories for the static assets referenced by the JSON. These directories keep the asset paths functional before real files are added:

    ```bash
    mkdir -p public/briefs public/infographics
    mkdir -p public/images/experts public/images/partners public/images/thumbnails
    ```

    Add a `.gitkeep` to each empty directory so they are tracked:
    ```bash
    touch public/briefs/.gitkeep public/infographics/.gitkeep
    touch public/images/experts/.gitkeep public/images/partners/.gitkeep public/images/thumbnails/.gitkeep
    ```
  </action>
  <verify>
    1. Validate JSON syntax for all three files:
       ```bash
       node -e "require('./content/briefs-index.json')" && echo "briefs OK"
       node -e "require('./content/experts.json')" && echo "experts OK"
       node -e "require('./content/site.json')" && echo "site OK"
       ```
    2. Verify authorId cross-references match: the `authorId` values in briefs-index.json ("olawale-oladipo", "amina-ibrahim") must appear as `id` values in experts.json.
    3. Verify brief count: `node -e "console.log(require('./content/briefs-index.json').length)"` must output `3`
    4. Run `npm run build` — must complete without errors
    5. All required locked fields in each brief: slug, title, weekNumber, publicationDate, authorId, keyTakeaway, executiveSummary, keyMessages, pdfUrl, infographicPdfUrl, thumbnailUrl, themes
  </verify>
  <done>
    Three valid JSON files exist in `content/`. `npm run build` passes. All 3 brief records have the exact 12 locked fields. Expert IDs in experts.json match authorId values in briefs-index.json. Asset placeholder directories exist in `public/`.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `npx tsc --noEmit` — no TypeScript errors
2. `npm run build` — exits 0
3. `node -e "const {getAllBriefs} = require('./app/lib/content'); console.log(getAllBriefs().length)"` — should output 3 (note: this may need `ts-node` or compile first; alternatively verify via build passing)
4. JSON files parse cleanly with `node -e "require('./content/briefs-index.json')"`
5. Brief slugs are URL-safe (lowercase, hyphens, no spaces)
6. All authorId values in briefs-index.json have matching id entries in experts.json
</verification>

<success_criteria>
- `app/lib/types.ts` exports Brief with all 12 locked fields, Expert, and SiteContent
- `app/lib/content.ts` exports getAllBriefs (sorts by weekNumber), getBriefBySlug, getExperts, getSiteContent
- `content/briefs-index.json` contains exactly 3 valid brief records with all required fields
- `content/experts.json` contains 2 expert records with IDs matching authorId references
- `content/site.json` contains partners array with all 4 partners (GGHN, Fleming Fund, Africa CDC, WHO AFRO)
- `npm run build` passes — the data layer does not introduce any build errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-infrastructure/01-02-SUMMARY.md`
</output>
