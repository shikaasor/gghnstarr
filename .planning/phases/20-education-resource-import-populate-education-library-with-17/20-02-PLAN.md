---
phase: 20-education-resource-import-populate-education-library-with-17
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - app/components/education/EducationCard.tsx
  - app/components/education/EducationFilters.tsx
  - app/components/education/EducationTabs.tsx
autonomous: false
requirements: []
must_haves:
  truths:
    - "Each education card shows a description excerpt and a WHO region badge"
    - "A region filter row appears in the filters and narrows results by region"
    - "Region filtering composes with the existing audience/format/topic/year filters (AND across dimensions)"
    - "Cards with no year do not render an empty/NaN year"
    - "next build passes with zero errors and the /education page renders 187 records across both tabs"
  artifacts:
    - path: "app/components/education/EducationCard.tsx"
      provides: "Description excerpt block + region badge"
      contains: "item.description"
    - path: "app/components/education/EducationFilters.tsx"
      provides: "Region filter row (regions prop + onRegionChange)"
      contains: "regions"
    - path: "app/components/education/EducationTabs.tsx"
      provides: "Region filter state, availableRegions derivation, region match in filter logic"
      contains: "selectedRegions"
  key_links:
    - from: "app/components/education/EducationTabs.tsx"
      to: "app/components/education/EducationFilters.tsx"
      via: "regions / selectedRegions / onRegionChange props"
      pattern: "onRegionChange"
    - from: "app/components/education/EducationTabs.tsx"
      to: "EducationItem.region"
      via: "availableRegions derivation + regionMatch in filtered useMemo"
      pattern: "item\\.region"
    - from: "app/components/education/EducationCard.tsx"
      to: "EducationItem.description"
      via: "rendered description excerpt + region badge"
      pattern: "item\\.region"
---

<objective>
Render the new description and region fields on each education card, and add region as a new filter dimension wired through EducationFilters and EducationTabs. Then verify the full /education page visually.

Purpose: Plan 20-01 delivers the data; this plan makes the 172 imported records' description and region visible and filterable, completing the phase goal. Depends on 20-01 because the components consume the new type fields and the 187-record JSON.
Output: Updated EducationCard (description excerpt + region badge), EducationFilters (region row), EducationTabs (region state/derivation/filtering), and a human visual sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-education-resource-import-populate-education-library-with-17/20-RESEARCH.md

# Components to modify (read all three before editing — they are tightly coupled by props):
@app/components/education/EducationCard.tsx
@app/components/education/EducationFilters.tsx
@app/components/education/EducationTabs.tsx

<interfaces>
<!-- After 20-01, the type provides: -->
WHORegion = 'AFRO' | 'EURO' | 'PAHO' | 'EMRO' | 'WPRO' | 'SEARO' | 'All regions'
EducationItem now has: description?: string, region?: WHORegion, year?: number (year is optional)

<!-- Existing filter wiring pattern in EducationTabs/EducationFilters that region must mirror exactly: -->
<!-- EducationTabs holds: selectedAudiences/Formats/Topics/Years state + availableX useMemos + filtered useMemo (AND across dimensions, OR within). -->
<!-- EducationFilters receives: {dimension}[] options, selected{Dimension}[], on{Dimension}Change, onClearAll. Each row renders only when its options array is non-empty. -->
<!-- toggleValue<T>(current, value) helper already exists in EducationFilters for multi-select toggling. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render description excerpt + region badge on EducationCard; guard optional year</name>
  <files>app/components/education/EducationCard.tsx</files>
  <action>
    Add a description block: when `item.description` is present, render it below the title as a 2-3 line excerpt (use a small muted text style consistent with existing card text, e.g. text-xs/text-sm text-slate-600, with `line-clamp-3` so long descriptions truncate cleanly). Add a region badge: when `item.region` is present, render it as a small pill in the format-badge/source row or the footer (visually distinct from the audience pills — e.g. a slate/outline style), showing the region token. Guard the existing year render: the footer currently shows `{item.year}` unconditionally; wrap it so it only renders when `item.year` is defined (year is now optional — avoid showing `undefined`/blank). Keep all existing card structure (format badge, title link, publication metadata, platform, unverified flag, audience pills) intact.
  </action>
  <verify>
    <automated>cd "C:\Users\HP_PC\Documents\gghn\gghnstarr" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^#" | grep -c "EducationCard" || echo "0 errors"</automated>
  </verify>
  <done>Card renders description excerpt (line-clamped) when present, a region badge when present, and the year only when defined; no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add region filter dimension to EducationFilters and EducationTabs</name>
  <files>app/components/education/EducationFilters.tsx, app/components/education/EducationTabs.tsx</files>
  <action>
    EducationFilters: import WHORegion; add props `regions: WHORegion[]`, `selectedRegions: WHORegion[]`, `onRegionChange: (v: WHORegion[]) => void`. Add a "Region" filter row mirroring the existing Format/Topic rows exactly (same pill styling, gated on `regions.length > 0`, using the existing `toggleValue` helper). Include region in the `hasActiveFilters` check so "Clear filters" appears when only a region is selected.
    EducationTabs: import WHORegion; add `selectedRegions` state (WHORegion[]); add an `availableRegions` useMemo deriving sorted unique `item.region` values from tabItems (filter out undefined before dedupe); add `regionMatch` to the `filtered` useMemo (selectedRegions.length === 0 || (item.region && selectedRegions.includes(item.region))) and AND it with the other dimension matches; add selectedRegions to its dependency array. Reset selectedRegions in both `handleTabClick` and the `onClearAll` callback, and reset currentPage to 1 in a new `onRegionChange` handler. Pass regions/selectedRegions/onRegionChange down to EducationFilters.
  </action>
  <verify>
    <automated>cd "C:\Users\HP_PC\Documents\gghn\gghnstarr" && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "^#" | grep -cE "Education(Tabs|Filters)" || echo "0 errors" && npx next build 2>&1 | tail -3</automated>
  </verify>
  <done>EducationFilters renders a Region row; EducationTabs tracks selectedRegions, derives availableRegions, applies regionMatch in the AND filter, and resets it on tab switch / clear-all; next build passes with zero errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of /education page (cards, region filter, pagination)</name>
  <action>Human-only visual verification — no code changes. Confirm the /education page renders the 187 records correctly after the 20-01 data import and the Task 1-2 UI changes, including cards, the new region filter, and pagination.</action>
  <what-built>The /education page now serves 187 records: cards show description excerpts and WHO region badges, and a Region filter row composes with the existing audience/format/topic/year filters. Pagination spans the larger resources tab.</what-built>
  <how-to-verify>
    1. Run `npm run dev` (or serve the static export) and open http://localhost:3000/education
    2. Confirm the Resources tab shows many cards (≈157 imported + curated resources) and the Training tab shows the courses/training items.
    3. On several cards confirm: a description excerpt is visible (truncated, not overflowing), a WHO region badge is shown, the source/format badges still render, and cards without a year do NOT show a blank/undefined year.
    4. Use the new Region filter: select one region (e.g. AFRO) and confirm the result count drops and only matching cards remain; combine with a Format filter and confirm AND behavior (both constraints apply); click "Clear filters" and confirm all return.
    5. Page through the Resources tab pagination (Next / numbered buttons) and confirm it works without errors. Note if the page-number row looks visually cramped (~13 pages) — report it, but it is non-blocking.
  </how-to-verify>
  <resume-signal>Type "approved", or describe any rendering/filter/pagination issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time JSON -> client render | Data is developer-committed and read at build time; client-side filtering operates on trusted static data. External `url`/`doi` links open in new tabs with `rel="noopener noreferrer"` (already in EducationCard). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03 | Tampering | external link click (target=_blank) | accept | Existing `rel="noopener noreferrer"` on the title link is retained; new region/description markup adds no new links. |
| T-20-04 | Information Disclosure | rendered description text | accept | Descriptions are public resource metadata from the AMR repository; no PII. |
| T-20-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (UI-only edits to existing components). No supply-chain surface. |
</threat_model>

<verification>
- `npx tsc --noEmit` reports zero errors across the three components.
- `next build` completes with zero errors and statically renders /education.
- Region filter narrows results and composes (AND) with other filters; clear-all resets including region.
- Cards render description excerpt + region badge; year hidden when absent.
- Human visual checkpoint approved.
</verification>

<success_criteria>
- EducationCard renders description excerpt and region badge; year guarded for optional value.
- EducationTabs includes region as a new filter dimension wired end to end.
- next build passes with zero errors.
- Human visual verification confirms cards, filters (including region), and pagination work.
</success_criteria>

<output>
Create `.planning/phases/20-education-resource-import-populate-education-library-with-17/20-02-SUMMARY.md` when done.
</output>
