---
phase: 19-brief-engagement-giscus-commenting-system-on-brief-detail-pages
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - app/briefs/[slug]/page.tsx
  - public/giscus.json
autonomous: false

requirements:
  - BENG-01
  - BENG-02

user_setup:
  - service: giscus
    why: "Giscus requires GitHub Discussions to be enabled on the repo, the Giscus GitHub App installed, and repoId/categoryId obtained from giscus.app before the commenting section goes live."
    dashboard_config:
      - task: "Enable GitHub Discussions on gghnstarr repo"
        location: "GitHub → shikaasor/gghnstarr → Settings → Features → check 'Discussions'"
      - task: "Install Giscus GitHub App"
        location: "https://github.com/apps/giscus → Install → select shikaasor/gghnstarr"
      - task: "Get repoId and categoryId"
        location: "https://giscus.app → enter shikaasor/gghnstarr → copy repoId and categoryId from generated snippet"

must_haves:
  truths:
    - "Every brief detail page shows a Discussion section below the Prev/Next navigation"
    - "The Giscus iframe loads when a user scrolls to the Discussion section"
    - "The Discussion section is hidden when the page is printed"
    - "The commenting widget is scoped per-brief via pathname mapping (each slug gets its own Discussion thread)"
    - "The giscus.json origin restriction prevents the widget loading on unauthorized domains"
  artifacts:
    - path: "app/briefs/[slug]/page.tsx"
      provides: "Brief detail page with GiscusComments wired below Prev/Next nav"
      contains: "GiscusComments"
    - path: "public/giscus.json"
      provides: "Domain allowlist restricting Giscus iframe to official origins"
      contains: "origins"
  key_links:
    - from: "app/briefs/[slug]/page.tsx"
      to: "app/components/briefs/GiscusComments.tsx"
      via: "import { GiscusComments } from '@/components/briefs/GiscusComments'"
      pattern: "GiscusComments"
    - from: "public/giscus.json"
      to: "Giscus iframe"
      via: "served at /giscus.json — Giscus checks this file to validate allowed origins"
      pattern: "origins"
---

<objective>
Wire GiscusComments into the brief detail page (below Prev/Next nav), create public/giscus.json for origin hardening, then guide the user through the one-time GitHub setup so repoId/categoryId placeholders can be filled in and the commenting widget goes live.

Purpose: All brief pages automatically gain a discussion thread per slug. GitHub Discussions provides permanent, searchable comment history without any server infrastructure.
Output: Updated page.tsx (GiscusComments imported + rendered), public/giscus.json, and verified live commenting on brief pages.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-brief-engagement-giscus-commenting-system-on-brief-detail-pages/19-01-SUMMARY.md
@app/briefs/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire GiscusComments into brief detail page</name>
  <files>app/briefs/[slug]/page.tsx</files>
  <action>
    Edit `app/briefs/[slug]/page.tsx`:

    1. Add import at top of file (after existing imports):
       ```tsx
       import { GiscusComments } from '@/components/briefs/GiscusComments';
       ```

    2. Add `<GiscusComments />` AFTER the closing `</nav>` of the Prev/Next navigation block, still inside the outer `<Container>` but OUTSIDE the `max-w-3xl` content div. The resulting structure at the bottom of the page should be:

       ```tsx
         {/* Prev/Next navigation */}
         <nav className="mt-16 pt-8 border-t border-slate-200 flex justify-between gap-4 no-print">
           {/* ...existing nav content unchanged... */}
         </nav>

         {/* Giscus commenting — one Discussion thread per brief slug */}
         <GiscusComments />

       </Container>
       ```

    Do NOT move GiscusComments inside the `max-w-3xl` content div — it must span the full Container width for the iframe to display comfortably.
    Do NOT wrap with React.Suspense or dynamic() — not needed for static export.
    Do NOT add any loading state or skeleton — the `loading="lazy"` prop on the Giscus component handles deferred loading.

    The page is a Server Component; GiscusComments is a Client Component — this is the correct pattern (same as DownloadButton and InfographicBlock already used on this page).
  </action>
  <verify>Run `npx tsc --noEmit` — no errors. Run `grep -n "GiscusComments" app/briefs/\[slug\]/page.tsx` — should show both the import line and the JSX usage line.</verify>
  <done>page.tsx imports GiscusComments and renders it after the Prev/Next nav. TypeScript compilation is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create public/giscus.json for origin hardening</name>
  <files>public/giscus.json</files>
  <action>
    Create `public/giscus.json` with the following content:

    ```json
    {
      "origins": [
        "https://gghnstarr.vercel.app",
        "https://www.gghnstarr.org",
        "http://localhost:3000"
      ]
    }
    ```

    This file is served at `/giscus.json` by Next.js static export. The Giscus iframe checks this file to restrict which domains are allowed to load the widget. This prevents the commenting section appearing on unauthorized forks or mirror sites.

    Assumptions (document in SUMMARY):
    - Production domain assumed to be `gghnstarr.vercel.app` — update if custom domain differs
    - `www.gghnstarr.org` included as likely custom domain — remove if not in use
    - `localhost:3000` included for local dev verification
  </action>
  <verify>Run `cat public/giscus.json` — should show valid JSON with an `origins` array containing the Vercel and localhost URLs.</verify>
  <done>public/giscus.json exists, contains valid JSON with origins array. File is served from /giscus.json on the deployed site.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: GitHub setup + fill in repoId/categoryId placeholders</name>
  <files>app/components/briefs/GiscusComments.tsx</files>
  <action>
    This is a human-required action. Claude cannot access GitHub settings UI or giscus.app to retrieve repoId/categoryId. The user must complete the steps below to replace the PLACEHOLDER values in GiscusComments.tsx with real Giscus credentials.
  </action>
  <verify>Run `grep -v PLACEHOLDER app/components/briefs/GiscusComments.tsx | grep -E "repoId|categoryId"` — both lines should show real values (not PLACEHOLDER strings). Then run `npm run build` to confirm clean static export.</verify>
  <done>GiscusComments.tsx has real repoId and categoryId values, npm run build passes, and the Giscus widget loads on a brief detail page in local dev.</done>
  <what-built>
    GiscusComments.tsx is wired into every brief detail page. The component currently uses PLACEHOLDER_REPO_ID and PLACEHOLDER_CATEGORY_ID strings — the commenting section will not load until real values are filled in. This checkpoint walks through the one-time GitHub setup.
  </what-built>
  <how-to-verify>
    Complete these steps in order:

    **Step 1 — Enable GitHub Discussions:**
    - Go to https://github.com/shikaasor/gghnstarr/settings
    - Under "Features", tick the "Discussions" checkbox
    - Click Save

    **Step 2 — Install the Giscus GitHub App:**
    - Go to https://github.com/apps/giscus
    - Click "Install"
    - Select the `shikaasor/gghnstarr` repository
    - Confirm installation

    **Step 3 — Get repoId and categoryId:**
    - Go to https://giscus.app
    - In the "Repository" field enter: `shikaasor/gghnstarr`
    - Under "Discussion Category" select: `General`
    - The page will generate a `<script>` tag — copy the `data-repo-id` value (this is your repoId) and the `data-category-id` value (this is your categoryId)

    **Step 4 — Fill in placeholders in GiscusComments.tsx:**
    - Open `app/components/briefs/GiscusComments.tsx`
    - Replace `PLACEHOLDER_REPO_ID` with the actual repoId value
    - Replace `PLACEHOLDER_CATEGORY_ID` with the actual categoryId value
    - Save the file

    **Step 5 — Run local verification:**
    - Run `npm run dev`
    - Open http://localhost:3000/briefs/brief-week-6-addressing-antimicrobial-resistance-amr-in-ghana (or any brief slug)
    - Scroll to the bottom — you should see a "Discussion" section with a Giscus comment box loading
    - The Prev/Next nav above it should still display correctly
    - Try printing the page (Ctrl+P / Cmd+P) — the Discussion section should NOT appear in the print preview

    **Step 6 — Run production build verification:**
    - Run `npm run build` — should complete with no errors
  </how-to-verify>
  <resume-signal>Type "approved" once comments are loading locally and the build passes, or describe any issues encountered.</resume-signal>
</task>

</tasks>

<verification>
1. `grep -n "GiscusComments" app/briefs/\[slug\]/page.tsx` — shows import + usage
2. `cat public/giscus.json` — shows valid origins JSON
3. `npx tsc --noEmit` — no errors
4. `npm run build` — static export completes without errors
5. Manual: Navigate to any brief detail page locally — Discussion section appears below Prev/Next nav
6. Manual: Print preview of brief page — Discussion section hidden
7. Manual: Giscus iframe loads and shows comment box (requires GitHub setup in Task 3 to be complete)
</verification>

<success_criteria>
Every brief detail page at /briefs/[slug] renders a Discussion section below the Prev/Next navigation. The Giscus iframe uses pathname mapping (one thread per brief), loads lazily, hides on print, and repoId/categoryId are filled with real values from giscus.app. npm run build completes without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/19-brief-engagement-giscus-commenting-system-on-brief-detail-pages/19-02-SUMMARY.md`
</output>
