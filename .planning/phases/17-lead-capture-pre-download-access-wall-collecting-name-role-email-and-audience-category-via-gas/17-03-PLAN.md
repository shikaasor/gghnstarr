---
phase: 17-lead-capture-pre-download-access-wall-collecting-name-role-email-and-audience-category-via-gas
plan: "03"
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - app/components/sections/FeaturedBrief.tsx
  - app/components/take-action/DownloadCard.tsx
  - app/components/take-action/ToolkitSection.tsx
autonomous: true
requirements:
  - LEAD-01

must_haves:
  truths:
    - "The Featured Brief download button on the homepage shows the modal to first-time visitors"
    - "PDF and DOCX downloads in the advocacy toolkit are gated; PNG/JPEG toolkit assets are free"
    - "The gated prop on DownloadCard defaults to false so existing ungated cards require no changes at call sites"
    - "Returning submitters bypass the modal on both surfaces"
  artifacts:
    - path: "app/components/sections/FeaturedBrief.tsx"
      provides: "Homepage featured brief section with gated PDF download"
      exports: ["default (FeaturedBrief)"]
    - path: "app/components/take-action/DownloadCard.tsx"
      provides: "Toolkit download card with optional gating via gated prop"
      exports: ["DownloadCard"]
  key_links:
    - from: "app/components/sections/FeaturedBrief.tsx"
      to: "app/components/briefs/LeadCaptureModal.tsx"
      via: "conditional render when showModal=true"
      pattern: "LeadCaptureModal"
    - from: "app/components/take-action/DownloadCard.tsx"
      to: "app/components/briefs/LeadCaptureModal.tsx"
      via: "conditional render when gated=true and showModal=true"
      pattern: "LeadCaptureModal"
---

<objective>
Convert FeaturedBrief (homepage PDF download) and DownloadCard (advocacy toolkit) to gated download components. DownloadCard gets an optional `gated` prop — PDF/DOCX callers pass gated=true; PNG callers omit it.

Purpose: Completes the four-surface coverage of the lead capture wall. Runs in parallel with Plan 02 (different files, no conflicts).
Output: Modified FeaturedBrief.tsx (gated) + modified DownloadCard.tsx (conditionally gated)
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@app/components/sections/FeaturedBrief.tsx
@app/components/take-action/DownloadCard.tsx
@app/components/take-action/ToolkitSection.tsx
@app/lib/lead-capture.ts
@app/components/briefs/LeadCaptureModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert FeaturedBrief homepage download to gated</name>
  <files>app/components/sections/FeaturedBrief.tsx</files>
  <action>
    FeaturedBrief is currently a Server Component (no 'use client'). It needs to become a Client Component to support modal state. Add 'use client' at the top.

    Add state and effect:
    ```ts
    const [alreadySubmitted, setAlreadySubmitted] = useState(false);
    const [showModal, setShowModal] = useState(false);

    useEffect(() => {
      setAlreadySubmitted(hasCompletedLeadCapture());
    }, []);
    ```

    Replace the existing `<a href={brief.pdfUrl} download ...>` with a `<button>` element:
    - Same className as the existing `<a>` (bg-teal-600 hover:bg-teal-500 text-white font-medium px-6 py-3 rounded transition-colors inline-flex items-center gap-2)
    - type="button"
    - onClick: if alreadySubmitted → window.open(brief.pdfUrl, '_blank', 'noopener,noreferrer') else setShowModal(true)
    - Keep Download icon and "Download PDF" label unchanged

    Add LeadCaptureModal below the `<a>` Container (outside the section tag or inside — just ensure it mounts at the component root):
    ```tsx
    {showModal && (
      <LeadCaptureModal
        href={brief.pdfUrl}
        onClose={() => setShowModal(false)}
      />
    )}
    ```

    Import hasCompletedLeadCapture from '@/lib/lead-capture', LeadCaptureModal from '@/components/briefs/LeadCaptureModal', useState and useEffect from 'react'.

    The FeaturedBrief section itself is statically rendered at build time via getFeaturedBrief() in page.tsx — making this component a Client Component is fine because it receives the brief as a prop and performs no server-only operations.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no errors in FeaturedBrief.tsx.
    Run `npm run build` — succeeds.
    Check file has 'use client' at top.
  </verify>
  <done>FeaturedBrief.tsx is a Client Component with gated download button: first-time visitors see modal, returning submitters get direct window.open. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add gated prop to DownloadCard for toolkit assets</name>
  <files>app/components/take-action/DownloadCard.tsx</files>
  <action>
    DownloadCard currently renders a direct `<a href={href} download={filename}>` link. Add optional gating.

    Update props interface — add one optional field:
    ```ts
    interface DownloadCardProps {
      title: string;
      description: string;
      format: string;
      href: string;
      filename: string;
      gated?: boolean;  // default false — PNG/JPEG callers omit this
    }
    ```

    Add 'use client' at the top (needed for useState and useEffect).

    Add state and effect (same pattern as other components):
    ```ts
    const [alreadySubmitted, setAlreadySubmitted] = useState(false);
    const [showModal, setShowModal] = useState(false);

    useEffect(() => {
      if (gated) setAlreadySubmitted(hasCompletedLeadCapture());
    }, [gated]);
    ```

    Download element logic:
    - If `!gated`: render the original `<a href={href} download={filename} ...>Download</a>` unchanged
    - If `gated`: render a `<button type="button" ...>` with same className as the existing `<a>` that onClick: alreadySubmitted → window.open(href, '_blank', 'noopener,noreferrer') else setShowModal(true)

    Add modal below the card div:
    ```tsx
    {gated && showModal && (
      <LeadCaptureModal href={href} onClose={() => setShowModal(false)} />
    )}
    ```

    Import hasCompletedLeadCapture from '@/lib/lead-capture' and LeadCaptureModal from '@/components/briefs/LeadCaptureModal'.

    Now update the DownloadCard call sites. The call site is `app/components/take-action/ToolkitSection.tsx` — NOT take-action/page.tsx. ToolkitSection renders DownloadCards from a `toolkitAssets` array. Open ToolkitSection.tsx and inspect how the cards are rendered:

    - If cards are rendered via array spread (`{...asset}`): add a `gated` field to each entry in the `toolkitAssets` array. Set `gated: true` for entries where the format is 'PDF' or 'DOCX'. Omit `gated` (or set `gated: false`) for PNG/JPEG entries.
    - If cards are rendered as explicit JSX props: add `gated={true}` directly to each PDF/DOCX `<DownloadCard>` element.

    The PNG social card asset should NOT have `gated` (or should have `gated={false}`) — image assets are explicitly free of gating per user decision.
  </action>
  <verify>
    Run `npx tsc --noEmit` — no errors.
    Run `npm run build` — succeeds.
    Verify gated prop is wired in ToolkitSection: `grep -n "gated" app/components/take-action/ToolkitSection.tsx` — must show gated={true} or gated: true on PDF/DOCX entries and no gated on PNG entries.
  </verify>
  <done>DownloadCard.tsx has 'use client', accepts optional gated prop (default false), renders gated button when gated=true and direct download link when gated=false. PDF/DOCX entries in ToolkitSection.tsx have gated={true} or gated: true; PNG entry has no gated prop. Build passes.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors across all modified files
- `npm run build` — succeeds
- FeaturedBrief.tsx: has 'use client', renders button not anchor for download
- DownloadCard.tsx: has 'use client', gated prop present in interface, PNG cards have no gated prop at call sites
</verification>

<success_criteria>
- FeaturedBrief: client component, gated download button, modal on first visit, direct open for returning visitors
- DownloadCard: gated prop added (optional, default false), PDF/DOCX toolkit cards gated, PNG cards ungated
- All 4 PDF download surfaces now covered: DownloadButton (Plan 02), BriefCard (Plan 02), FeaturedBrief (this plan), DownloadCard (this plan)
</success_criteria>

<output>
After completion, create `.planning/phases/17-lead-capture-pre-download-access-wall-collecting-name-role-email-and-audience-category-via-gas/17-03-SUMMARY.md`
</output>
