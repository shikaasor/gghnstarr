---
phase: 08-awareness-hub-education-library
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/lib/types.ts
  - app/components/layout/Header.tsx
  - app/components/sections/AudienceCTAs.tsx
autonomous: true
requirements:
  - AWRE-01
  - AWRE-02
  - EDUC-01
  - EDUC-02

must_haves:
  truths:
    - "EducationResource and AudienceType types are importable from app/lib/types.ts"
    - "Header nav renders Awareness and Education links on desktop and mobile"
    - "AudienceCTAs awareness and education primary CTAs are live links (not disabled spans)"
  artifacts:
    - path: "app/lib/types.ts"
      provides: "AudienceType, ResourceFormat, EducationResource type exports"
      contains: "EducationResource"
    - path: "app/components/layout/Header.tsx"
      provides: "Nav links to /awareness and /education"
      contains: "awareness"
    - path: "app/components/sections/AudienceCTAs.tsx"
      provides: "Enabled CTAs linking to /awareness and /education"
      contains: "/awareness"
  key_links:
    - from: "app/components/sections/AudienceCTAs.tsx"
      to: "/awareness"
      via: "primaryHref and live: true for awareness/education cards"
      pattern: "href.*awareness.*live.*true"
---

<objective>
Lay the shared foundation for Phase 8: add new TypeScript types, extend Header nav, and enable the disabled CTAs in AudienceCTAs that have been dormant since Phase 7.

Purpose: Both new pages depend on the EducationResource type; both pages must be reachable from the nav and homepage CTAs. This plan makes those connections live before the pages themselves exist.
Output: Updated types.ts, Header.tsx with two new nav items, AudienceCTAs.tsx with enabled /awareness and /education links.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@app/lib/types.ts
@app/components/layout/Header.tsx
@app/components/sections/AudienceCTAs.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EducationResource types to types.ts</name>
  <files>app/lib/types.ts</files>
  <action>
    Append three new exports to the END of app/lib/types.ts (after the existing SiteContent interface). Do NOT modify any existing interfaces.

    Add exactly:

    ```typescript
    // Phase 8: Education Library types
    export type AudienceType = 'Policymaker' | 'Healthcare Worker' | 'General Public';
    export type ResourceFormat = 'Article' | 'Download' | 'Video';
    export interface EducationResource {
      title: string;
      audiences: AudienceType[];
      format: ResourceFormat;
      source: string;
      url: string;
    }
    ```
  </action>
  <verify>Run: npx tsc --noEmit from the project root. Zero errors expected.</verify>
  <done>types.ts exports AudienceType, ResourceFormat, EducationResource with no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Awareness and Education nav links to Header.tsx</name>
  <files>app/components/layout/Header.tsx</files>
  <action>
    In app/components/layout/Header.tsx, update the navLinks array to add two entries after '/briefs':

    ```typescript
    const navLinks = [
      { href: '/', label: 'Home' },
      { href: '/briefs', label: 'Briefs' },
      { href: '/awareness', label: 'Awareness' },
      { href: '/education', label: 'Education' },
      { href: '/methodology', label: 'Methodology' },
      { href: '/experts', label: 'Experts' },
      { href: '/contact', label: 'Contact' },
    ];
    ```

    No other changes to Header.tsx — the desktop and mobile nav both map over navLinks, so both get the new items automatically.
  </action>
  <verify>Run: npx tsc --noEmit. Zero errors expected.</verify>
  <done>Header renders "Awareness" and "Education" nav links between Briefs and Methodology on both desktop and mobile.</done>
</task>

<task type="auto">
  <name>Task 3: Enable awareness and education CTAs in AudienceCTAs.tsx</name>
  <files>app/components/sections/AudienceCTAs.tsx</files>
  <action>
    In app/components/sections/AudienceCTAs.tsx, update the audiences array to enable the 3 disabled links that now have live pages:

    1. Policymakers card — secondary link "AMR Awareness Hub":
       Change: `{ label: 'AMR Awareness Hub', href: '#', live: false }`
       To:     `{ label: 'AMR Awareness Hub', href: '/awareness', live: true }`

    2. Healthcare Workers card — primary CTA "Explore Resources":
       Change: `primaryHref: '#', primaryLive: false`
       To:     `primaryHref: '/education', primaryLive: true`

    3. General Public card — primary CTA "Learn What You Can Do":
       Change: `primaryHref: '#', primaryLive: false`
       To:     `primaryHref: '/awareness', primaryLive: true`

    4. General Public card — secondary link "AMR Awareness Hub":
       Change: `{ label: 'AMR Awareness Hub', href: '#', live: false }`
       To:     `{ label: 'AMR Awareness Hub', href: '/awareness', live: true }`

    Do NOT change the "Take Action" links — those still point to '#' with live: false (Phase 10).
    Do NOT change the Healthcare Worker "Take Action" secondary link.
    Do NOT change any styling or JSX structure.
  </action>
  <verify>Run: npx tsc --noEmit. Zero errors expected. Grep the file: grep "live: true" app/components/sections/AudienceCTAs.tsx — should show at least 5 occurrences (existing + 4 new).</verify>
  <done>Policymakers "AMR Awareness Hub" secondary, Healthcare Workers "Explore Resources" primary, and General Public "Learn What You Can Do" primary and "AMR Awareness Hub" secondary are all live: true with correct hrefs.</done>
</task>

</tasks>

<verification>
Run `npx tsc --noEmit` — zero errors.
Run `npm run build` — static export completes without errors (the /awareness and /education routes don't exist yet, so 404s are expected if accessed, but the build itself must succeed since those pages aren't referenced in generateStaticParams).
</verification>

<success_criteria>
- types.ts exports AudienceType, ResourceFormat, EducationResource
- Header.tsx navLinks includes href: '/awareness' and href: '/education'
- AudienceCTAs.tsx has 4 newly-enabled links pointing to /awareness and /education
- npx tsc --noEmit passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-awareness-hub-education-library/08-01-SUMMARY.md`
</output>
