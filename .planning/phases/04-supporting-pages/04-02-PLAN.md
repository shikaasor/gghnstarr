---
phase: 04-supporting-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - content/experts.json
  - content/briefs-index.json
  - app/experts/page.tsx
  - app/components/experts/ExpertCard.tsx
autonomous: true
requirements:
  - EXPT-01

must_haves:
  truths:
    - "The /experts page displays three expert cards — Dr. Olawale A., Dr. Samson A., and Piringar Mercy Niyang — in a 3-column desktop grid"
    - "Each card shows a photo placeholder (grey bg), name, title, institutional affiliation, area of specialization, and a 2-3 sentence bio"
    - "No outbound links appear on any card"
    - "The experts.json file contains correct real-world data for all three experts, and the old 'amina-ibrahim' entry is replaced by 'samson-a'"
    - "briefs-index.json week-02 brief authorId is updated from 'amina-ibrahim' to 'samson-a'"
  artifacts:
    - path: "content/experts.json"
      provides: "Source of truth for expert profiles — 3 entries with correct names, titles, orgs, bios, specialties"
      contains: "samson-a"
    - path: "app/components/experts/ExpertCard.tsx"
      provides: "Pure display Server Component rendering one expert profile card"
      exports: ["ExpertCard"]
    - path: "app/experts/page.tsx"
      provides: "Server Component that reads experts via getExperts() and renders the grid"
      contains: "getExperts"
  key_links:
    - from: "app/experts/page.tsx"
      to: "content/experts.json"
      via: "getExperts() content function using fs.readFileSync + process.cwd()"
      pattern: "getExperts"
    - from: "app/experts/page.tsx"
      to: "app/components/experts/ExpertCard.tsx"
      via: "maps experts array and renders one ExpertCard per expert"
      pattern: "ExpertCard"
---

<objective>
Update experts.json with the three real STARR team members, fix the stale authorId reference in briefs-index.json, create ExpertCard component, and build the /experts page as a Server Component grid.

Purpose: Policymakers evaluating STARR's credibility need to see who is behind the modelling. Accurate expert profiles with real credentials build institutional trust.
Output: Corrected content/experts.json + content/briefs-index.json, new app/components/experts/ExpertCard.tsx, updated app/experts/page.tsx.
</objective>

<execution_context>
@C:/Users/HP_PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/HP_PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/experts.json
@content/briefs-index.json
@app/components/layout/Container.tsx
@app/experts/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite content/experts.json and fix briefs-index.json authorId</name>
  <files>content/experts.json, content/briefs-index.json</files>
  <action>
    Replace content/experts.json entirely with three expert entries derived from starr_expertises.txt. Remove the old "amina-ibrahim" entry. Write bios as 2-3 sentences for card display — not the full CV text from the source document.

    **Expert 1 — Dr. Olawale A. (keep/update):**
    ```json
    {
      "id": "olawale-a",
      "name": "Dr. Olawale A.",
      "title": "One Health Laboratory Advisor",
      "organization": "Georgetown University Fleming Fund AMR Project, Rwanda",
      "photoUrl": "/images/experts/olawale-a.jpg",
      "specialties": ["Genomic Surveillance", "Laboratory Systems", "AMR Surveillance Intelligence", "Fleming Fund Rwanda"],
      "bio": "Dr. Olawale A. holds qualifications in Medical Laboratory Science, Medical Microbiology, Field Epidemiology, and a PhD in Molecular Genomics and Medical Virology. He leads AMR surveillance and laboratory systems strengthening as One Health Laboratory Advisor for the Georgetown University Fleming Fund AMR Project in Rwanda, working alongside ASLM, SeqAfrica, and AMROH. His AMR-focused research has been published in peer-reviewed journals and presented at international scientific conferences."
    }
    ```

    **Expert 2 — Dr. Samson A. (replaces Dr. Amina Ibrahim):**
    ```json
    {
      "id": "samson-a",
      "name": "Dr. Samson A.",
      "title": "Mathematical & Predictive Modeling Lead",
      "organization": "GGHN STARR Initiative",
      "photoUrl": "/images/experts/samson-a.jpg",
      "specialties": ["Mathematical Modeling", "Predictive Analytics", "NIPAD Platform", "GlobalPPS Surveillance"],
      "bio": "Dr. Samson A. applies advanced mathematical and machine learning modeling frameworks — including SEIR compartmental models, Bayesian hierarchical forecasting, Random Forest algorithms, and agent-based simulations — to infectious disease epidemiology and AMR. He developed the Nigeria Immunization Predictive Analytics Dashboard (NIPAD), an R/Shiny platform that embeds predictive analytics in national health systems for district-level risk mapping and scenario planning. His GlobalPPS implementation experience across Nigerian and Rwandan health facilities anchors STARR's AMR consumption surveillance."
    }
    ```

    **Expert 3 — Piringar Mercy Niyang (new entry):**
    ```json
    {
      "id": "piringar-mercy-niyang",
      "name": "Piringar Mercy Niyang",
      "title": "Technical Lead — One Health Governance",
      "organization": "GGHN STARR Initiative",
      "photoUrl": "/images/experts/piringar-mercy-niyang.jpg",
      "specialties": ["One Health Governance", "GLASS Reporting", "Africa CDC Frameworks", "AMR Surveillance Systems"],
      "bio": "Piringar Mercy Niyang brings expertise across microbiology, public health, project leadership, and field implementation in multiple African health systems. As Technical Lead for the Fleming Fund Rwanda Country Grant (2024-2026), she oversees AMR surveillance, One Health governance, and system integration across human, animal, and environmental sectors, working with Rwanda's Ministry of Health and Ministry of Agriculture. Her career includes directing the US CDC SERS Project's multistate AMR reporting system — covering 12 Nigerian states and two rounds of GLASS reporting — and designing integrated AMR surveillance systems in Botswana."
    }
    ```

    Then update content/briefs-index.json: find the entry with `"authorId": "amina-ibrahim"` (week-02-laboratory-systems-capacity) and change it to `"authorId": "samson-a"`. Leave all other fields unchanged.

    Note: the Expert interface in the codebase may have `linkedinUrl` as a field — omit it from all three entries (no outbound links per locked decision) and verify TypeScript still accepts the type. If the Expert TypeScript interface requires linkedinUrl, make it optional (`linkedinUrl?: string`) in the content functions file.
  </action>
  <verify>
    - Read content/experts.json and confirm 3 entries with ids: olawale-a, samson-a, piringar-mercy-niyang
    - Read content/briefs-index.json week-02 entry and confirm authorId is "samson-a"
    - Run `npx tsc --noEmit` — no type errors about Expert fields
  </verify>
  <done>experts.json has 3 correct entries; briefs-index.json week-02 authorId is "samson-a"; TypeScript passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create ExpertCard component and update experts page</name>
  <files>app/components/experts/ExpertCard.tsx, app/experts/page.tsx</files>
  <action>
    **Create app/components/experts/ExpertCard.tsx** as a Server Component (no 'use client'). Accept a single prop of type `Expert` (imported from the content types). Render:

    - Outer card: `<article>` with classes `bg-white rounded-lg overflow-hidden shadow-sm border border-slate-100`
    - Photo area: A `<div className="relative bg-slate-100 aspect-square">` (relative positioning required for Next.js Image fill mode). Inside, render a Next.js `<Image>` component — import it from `'next/image'`. Use `fill` prop, `src={expert.photoUrl}`, `alt={expert.name}`, and `className="object-cover"`. The `fill` prop makes the image fill the relative-positioned wrapper; `object-cover` maintains aspect ratio. The bg-slate-100 background is visible automatically when the image path doesn't resolve (no JS onError needed).

    Example:
    ```tsx
    import Image from 'next/image';
    // ...
    <div className="relative bg-slate-100 aspect-square">
      <Image
        src={expert.photoUrl}
        alt={expert.name}
        fill
        className="object-cover"
      />
    </div>
    ```

    Note: Using Next.js `<Image>` (not a plain `<img>`) per locked user decision — real headshot photos will be provided and Image provides automatic optimization.

    - Card body: `<div className="p-5 flex flex-col gap-2">`
      - Name: `<h2 className="font-serif text-lg font-semibold text-navy-950">{expert.name}</h2>`
      - Title: `<p className="text-sm font-medium text-teal-600">{expert.title}</p>`
      - Organization: `<p className="text-sm text-slate-500">{expert.organization}</p>`
      - Specialties: `<ul className="flex flex-wrap gap-1 mt-1">` where each specialty is a `<li className="text-xs bg-slate-100 text-slate-600 px-2 py-0.5 rounded-full">{specialty}</li>`
      - Divider: `<hr className="border-slate-100 my-1" />`
      - Bio: `<p className="text-sm text-slate-600 leading-relaxed">{expert.bio}</p>`

    No links on the card — self-contained per locked decision.

    **Update app/experts/page.tsx** — replace placeholder content entirely. Keep it a Server Component (no 'use client'). Import `getExperts` from `@/lib/content` (adjust import path to match actual content functions location found in the codebase). Import `ExpertCard` from `@/components/experts/ExpertCard`. Import `Container` from `@/components/layout/Container`.

    Keep the existing `export const metadata` for the page title. Update it to: `export const metadata = { title: 'Our Experts — GGHN STARR' }`.

    Page structure:
    ```tsx
    const experts = getExperts();

    return (
      <>
        <section className="bg-white pt-16 pb-8">
          <Container>
            <h1 className="font-serif text-3xl font-bold text-navy-950 mb-4">Our Experts</h1>
            <p className="text-slate-600 max-w-2xl">
              The GGHN STARR initiative is led by specialists in predictive modeling, genomic surveillance, 
              and One Health governance — each bringing field implementation experience across African health systems.
            </p>
          </Container>
        </section>
        <section className="py-12 bg-slate-50">
          <Container>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
              {experts.map((expert) => (
                <ExpertCard key={expert.id} expert={expert} />
              ))}
            </div>
          </Container>
        </section>
      </>
    );
    ```

    **Find the correct import path for getExperts:** Check the actual file location — likely `lib/content.ts` or `app/lib/content.ts`. Use `grep -r "export function getExperts" /c/Users/HP_PC/Documents/gghn/starr --include="*.ts"` to confirm before writing the import.

    **Handle Expert type compatibility:** If the Expert TypeScript type includes `linkedinUrl` as required, make it optional in the type definition file (likely lib/types.ts or similar) so experts.json entries without it still typecheck.

    **next.config image domains:** If Next.js Image requires domain configuration for local /images/ paths, check next.config.js/ts. Local public/ paths do not need domain config, but confirm the image is served from public/images/experts/ and the photoUrl starts with /images/experts/.
  </action>
  <verify>
    - `npx next build` exits 0
    - `npx tsc --noEmit` exits 0
    - Navigate to /experts — three cards visible in a grid
    - Each card shows name, title, organization, specialties as pills, and bio text
    - Photo area shows grey background (images not present yet) — the relative wrapper with bg-slate-100 provides fallback
    - No links visible on any card
    - Inspect rendered HTML: photo element is a Next.js Image (renders as `<img>` with next/image attributes, not a bare `<img>`)
  </verify>
  <done>Three expert cards render at /experts with correct data from updated experts.json. Photos use Next.js Image component with fill + relative wrapper. No TypeScript errors. Build clean.</done>
</task>

</tasks>

<verification>
- `npx next build` exits with code 0
- /experts page displays 3 cards: Dr. Olawale A., Dr. Samson A., Piringar Mercy Niyang
- Card layout is 3-column on desktop (md:grid-cols-3), 1-column on mobile
- ExpertCard photo uses Next.js `<Image fill>` inside a `relative` wrapper (not a bare `<img>`)
- No outbound links on any card
- Brief week-02 author still resolves (authorId "samson-a" matches updated experts.json)
- TypeScript: `npx tsc --noEmit` exits clean
</verification>

<success_criteria>
Policymakers visiting /experts see three complete expert profiles with correct credentials derived from source documents. Expert photos use Next.js Image with proper fill layout. The site builds without errors. The stale amina-ibrahim data is removed from both experts.json and briefs-index.json.
</success_criteria>

<output>
After completion, create `.planning/phases/04-supporting-pages/04-02-SUMMARY.md` following the summary template.
</output>
